# Anti-Epic-1 Compliance Evidence (BA-P1-1)
Generated: 2026-04-24 16:38:59
Branch: main

## Definition

Per 2026-04-15 Epic 1 cancellation knowledge entry, the "fail-closed" pattern is:
  - PreToolUse hook registered in .claude/settings.json
  - Exits with non-zero (Claude Code's contract: non-zero = deny) to BLOCK tool calls
  - Triggered automatically on every Claude tool invocation

This is what caused dep-guard.sh to lock the fleet on 2026-04-15.

## Phase 1 tool/script classification

All Phase 1 scripts are CALLED EXPLICITLY by Alex/Blake/Gate 3, NOT registered
as PreToolUse hooks. Exit codes are advisory outcomes (PASS/FAIL/usage-error),
not automatic tool-blocking.

| Script | Invocation model | PreToolUse? |
|--------|------------------|-------------|
| .tad/hooks/lib/layer2-audit.sh           | Alex step4c manual call | NO |
| .tad/hooks/lib/gate3-git-tracked-check.sh| Blake Gate 3 manual call | NO |
| .tad/hooks/lib/drift-check.sh            | /tad-maintain CHECK call | NO |
| .tad/hooks/userprompt-domain-router.sh   | UserPromptSubmit (Phase 2b) | NO (advisory hint only) |

## settings.json diff vs main (Phase 1 must NOT touch this file)

(empty — Phase 1 did NOT modify .claude/settings.json ✓)

## Settings.json current state

Pre-existing forbidden constructs in settings.json (NOT from Phase 1):
17:    "PreToolUse": [

Note: these predate Phase 1. Verify they are not Phase-1-introduced:
  git log --diff-filter=A --format='%h %ad' --date=short .claude/settings.json 2>/dev/null | tail -3
d42d5c4 2025-09-26

## Exit code semantics in Phase 1 scripts

`exit 2` in gate3-git-tracked-check.sh and layer2-audit.sh is POSIX usage-error
(bad args, invalid slug, missing file). These scripts are called by humans/agents;
the 'error' doesn't block Claude tools. Compare against:

  - dep-guard.sh (cancelled 2026-04-15): registered in settings.json PreToolUse,
    exit 2 blocked ALL tool calls automatically.
  - Phase 1 scripts: not in settings.json; exit 2 only affects caller's bash $?.

## Verdict

Phase 1 is anti-Epic-1 compliant:
  [OK] Zero modifications to .claude/settings.json
  [OK] Zero PreToolUse hook registrations
  [OK] Zero permissions.deny additions
  [OK] All exit codes are advisory (POSIX usage convention) not enforcement
  [OK] All 5 tasks are smoke alarms — reports + suggestions, never blocks
