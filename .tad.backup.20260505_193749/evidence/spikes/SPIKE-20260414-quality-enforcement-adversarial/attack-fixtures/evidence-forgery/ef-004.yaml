id: "ef-004"
polarity: "negative"
target_hook: "hardened-evidence-validator"
expected_decision: "exit_1"
severity: "P0"
diversity_tag: "staleness"
evasion_mechanism: "Evidence mtime < handoff mtime (pre-create old mtime)"
known_gap_reason: "Staleness check requires handoff-file mtime context which is not available for standalone tmp-file validation. Validator detects stale evidence in production when run from a .tad/evidence/reviews/blake/<slug>/ path that matches an active handoff; static-tmp fixture harness cannot provide this context. Phase 2 mitigation: invoke validator with explicit --handoff-mtime flag or use sha-manifest freshness markers."
input: |
  # Evidence
  
  Files: README.md, CLAUDE.md, tad.sh, .gitignore, LICENSE
  
  Overall: PASS
  
  Review completed. All files inspected and passed quality checks.
