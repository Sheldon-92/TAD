# Code Review Skill

> 来源: obra/superpowers，已适配 TAD 框架

## 触发条件

当 Claude 完成功能开发、修复 Bug、或准备提交代码时，自动应用此 Skill。

---

## 核心原则

**"Review early, review often."**（早审查，勤审查）

---

## 何时需要代码审查

### 必须审查 ✅
- 任务完成后
- 主要功能开发后
- 合并到主分支前

### 建议审查 💡
- 卡住时寻求帮助
- 重构前评估方案
- 复杂 Bug 修复后

---

## 审查流程

### Step 1: 准备审查范围

```bash
# 获取基准提交
git merge-base HEAD main

# 获取当前提交
git rev-parse HEAD

# 查看变更
git diff main...HEAD --stat
```

### Step 2: 自我审查清单

在请求他人审查前，先自查：

#### 功能正确性
- [ ] 代码实现了需求吗？
- [ ] 边界情况处理了吗？
- [ ] 错误处理完整吗？

#### 代码质量
- [ ] 命名清晰有意义吗？
- [ ] 函数职责单一吗？
- [ ] 有重复代码吗？

#### 测试覆盖
- [ ] 有单元测试吗？
- [ ] 测试覆盖了主要路径吗？
- [ ] 测试覆盖了边界情况吗？

#### 安全考虑
- [ ] 输入验证了吗？
- [ ] SQL 注入防护了吗？
- [ ] XSS 防护了吗？
- [ ] 敏感数据保护了吗？

### Step 3: 请求审查

**审查请求模板**：
```markdown
## 变更说明
[简述做了什么，为什么这样做]

## 变更范围
- 基准提交: [commit hash]
- 当前提交: [commit hash]
- 影响文件: [文件列表]

## 测试说明
[如何验证这些变更]

## 关注点
[希望审查者特别关注的地方]
```

---

## 处理审查反馈

### 反馈优先级

| 级别 | 处理方式 |
|------|----------|
| 🔴 Critical | 立即修复，不能合并 |
| 🟡 Important | 合并前必须修复 |
| 🟢 Minor | 记录下来，可以后续处理 |
| 💭 Suggestion | 讨论后决定是否采纳 |

### 反馈响应原则

1. **感谢反馈** - 即使不同意
2. **理解意图** - 确保理解问题本质
3. **解释决策** - 如果有不同意见，说明原因
4. **快速响应** - Critical 和 Important 立即处理

---

## 代码审查清单

### 架构层面
```
□ 符合项目架构模式吗？
□ 依赖方向正确吗？
□ 模块边界清晰吗？
□ 是否引入了不必要的复杂度？
```

### 实现层面
```
□ 算法效率合理吗？
□ 数据结构选择合适吗？
□ 有潜在的性能问题吗？
□ 并发处理正确吗？
```

### 可维护性
```
□ 代码易于理解吗？
□ 注释必要且准确吗？
□ 有硬编码的值吗？
□ 日志和监控足够吗？
```

---

## 与 TAD 框架的集成

在 TAD 的开发流程中，代码审查发生在：

```
Alex 设计 → Blake 实现 → Code Review → Gate 验证
                              ↓
                        [ 此 Skill ]
```

**TAD Gate 审查要点**：
1. 是否符合 Alex 的设计方案
2. 是否遵循了 MQ 的约定
3. 是否有完整的测试覆盖
4. 是否更新了相关文档

---

## 常见问题模式

### 🚨 红旗代码
```javascript
// 硬编码的魔法数字
if (users.length > 100) { ... }

// 过长的函数
function doEverything() { /* 200 行 */ }

// 深层嵌套
if (a) { if (b) { if (c) { if (d) { ... } } } }

// 注释掉的代码
// function oldFunction() { ... }
```

### ✅ 好的实践
```javascript
// 命名常量
const MAX_USERS_PER_PAGE = 100;
if (users.length > MAX_USERS_PER_PAGE) { ... }

// 单一职责
function validateUser() { ... }
function saveUser() { ... }

// 早期返回
if (!a) return;
if (!b) return;
// 主逻辑
```

---

*此 Skill 确保 Claude 在关键节点进行代码审查，提高代码质量。*
