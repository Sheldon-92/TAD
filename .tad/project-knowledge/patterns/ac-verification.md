# AC Verification Patterns (Layer 2)

> Reusable patterns for acceptance criteria design, verification command correctness, and dry-run discipline.

---

### Alex Handoff AC Design Rules - 2026-04-14
- **Discovery**: (1) AC lists become the operational contract — anything not in ACs is effectively optional. Alex MUST explicitly list ALL required evidence files. (2) "≥N triggers" with a concrete list is ambiguous — use "All of A, B, C must PASS" form. (3) When 3 co-mandated ACs are mutually exclusive by construction (byte-preservation + optimization + behavioral invariant), add AC Conflict Matrix sub-step. (4) AC verification commands need pre-ship smoke test — Alex MUST dry-run each on representative existing artifacts before shipping handoff.
- **Action**: Use imperative AC form. Dry-run verification commands. Add AC Conflict Matrix for structural ACs.
- **failure_mode**: Naive default: write ACs as descriptive prose ("system should handle X") without dry-running verification commands on real artifacts. Why wrong: ACs become the operational contract — anything ambiguous or untested becomes an unenforceable requirement that surfaces as a false PASS or false FAIL only during implementation.

### AC Verification Drift Pattern - 2026-04-25 (recurring through 4 phases)
- **Discovery**: Alex specifies AC verification commands without testing them on real artifacts. Failures surface only when Blake runs the literal command. Three sub-patterns: (1) sentinel/marker substring leak, (2) output-shape assumption mismatch (single vs multi-file grep), (3) expert reviewer scope mismatch. Root cause: mental simulation of regex is insufficient. §9.2 "Verified Output" column should be MANDATORY-FILLED by Alex before handoff ships.
- **Action**: Every non-trivial AC verification command MUST be dry-run on a representative existing artifact during handoff drafting. Paste actual command output in §9.2.
- **failure_mode**: Naive default: mentally simulate regex/grep commands instead of running them on real files. Why wrong: mental simulation misses sentinel substring leaks, output-shape mismatches (single vs multi-file grep), and scope errors that only surface when Blake runs the literal command — wasting an entire implementation cycle.

### AC Self-Leak from Removal Rationale - 2026-04-27
- **Discovery**: When a grep-substring AC verifies "no occurrence of X" and the impl adds a rationale comment containing X (e.g., handoff slug containing the forbidden word), the comment self-leaks. Fix: reference META artifacts (deprecation.yaml entry, ADR id) not the handoff slug.
- **Action**: Removal rationale pointers must reference META artifacts, never the removed-feature name verbatim.
- **failure_mode**: Naive default: include the forbidden substring in the rationale comment explaining why it was removed (e.g., "removed feature-X" contains "feature-X"). Why wrong: a grep-based "no occurrence of X" AC catches the rationale comment itself, causing a false FAIL that blocks implementation even though the actual feature code is correctly removed.

### Recurring failure: tsc missing type - 2026-05-19
- **Context**: Detected by dream-scanner from v2 trace analysis. Pattern 'tsc: missing type' appeared in ≥2 reflexion_diagnosis events across TAD handoff cycles.
- **Discovery**: TypeScript type-checking failures (missing type declarations) are a recurring root cause of Blake Layer 1 failures. This pattern triggers reflexion cycles that could be avoided with upfront type completeness checks in handoff ACs.
- **Action**: When writing handoff ACs for TypeScript projects, include an explicit AC: "npx tsc --noEmit passes with zero errors before Layer 1." Consider adding type coverage threshold to Gate 3 v2 checklist.
- **failure_mode**: Naive default: omit tsc type-check from handoff ACs, assuming the implementer will naturally ensure type completeness. Why wrong: missing type declarations are a recurring root cause of Blake Layer 1 failures, triggering avoidable reflexion cycles that waste tokens and time.

### Behavioral-Fixture Discrimination: Anti-Slop = Threshold/Named-Rule, Not Vocabulary - 2026-05-31
- **Context**: P5 authored 14 behavioral fixtures (1 per installed capability pack) for `pack-eval-runner.sh`. Each fixture greps a captured agent output for "markers" proving the agent APPLIED a pack rule. The anti-gaming risk: a marker a no-pack frontier LLM would also emit makes the fixture validation theater (architecture.md YOLO audit).
- **Discovery**: The reliable discriminator is the SAME as the anti-slop formula (architecture.md "Capability Pack Quality Bar"): a marker is pack-specific iff it is a NAMED rule or a SPECIFIC threshold/number the pack introduces — NOT a domain noun. Concrete contrast that survived spot-check: markers like "self-enhancement bias 10-15%", "TruffleHog exit 183", "ACX RMS -23 to -18 dB", "n=550 a11y / 30-50%", "Four-Gate fastest-fail-first", "Judge ≠ Optimizer", "60-30-10 + 3-level token". NOT "make it secure", "add more tests", "use a TTS tool", or any word from the input scenario. Two fast sanity gates per fixture: (1) every Verification Command MUST be `grep -oE 'a|b|c' {out} | sort -u | wc -l` — NEVER `grep -c` (P4 lint Rule A: grep -c + sort -u | wc -l always returns 1). (2) every fixture needs ≥1 `[structural]` marker (proves the agent produced the rule's OUTPUT SHAPE, e.g. per-photo first_frame/last_frame decomposition or D1-D10 decision IDs, not just mentioned the vocabulary). The list in the Anti-Slop Check is load-bearing: it documents the generic terms deliberately EXCLUDED from the grep pattern.
- **Action**: When authoring a behavioral fixture, pull markers from the pack's specific numbers/named-rules/exit-codes/thresholds, never domain nouns or input words. Use `grep -oE … | sort -u | wc -l`. Require ≥1 [structural] marker that verifies output SHAPE. Self-check each marker: "would an agent WITHOUT this pack emit this?" — if yes, replace it.
- **Grounded in**: .tad/scripts/pack-eval-runner.sh, .claude/skills/*/examples/*.md (16 fixtures), .tad/templates/pack-example-fixture.md, COMPLETION-20260531-tad-lean-trustworthy-phase5.md AC5.3
- **failure_mode**: Naive default: use domain nouns or generic vocabulary (e.g., "make it secure", "use a TTS tool") as behavioral fixture markers. Why wrong: a frontier LLM without the pack loaded will also emit those terms, making the fixture pass without the pack — pure validation theater that cannot distinguish pack-influenced output from baseline.

### `node --check` on a `.workflow.js` File is a Structural False-FAIL — Use the Wrapped-Body Check - 2026-06-07
- **Context**: HANDOFF-20260607-surplus-scan-phase1 AC1 specified `node --check .claude/workflows/surplus-scan.workflow.js` must exit 0. It exited 1 with `SyntaxError: Illegal return statement`.
- **Discovery**: TAD workflow files use **top-level `return`** statements (the workflow runtime wraps the file body in a function before executing, and injects `agent`/`parallel`/`phase`/`log`/`budget` as locals). Bare `node --check` parses the file as a plain ESM module where top-level `return` is illegal — so it ALWAYS exits 1 for EVERY workflow in the repo. Verified empirically: `loop-discover`, `epic-audit`, `tournament-design`, AND `surplus-scan` all exit 1 under bare `node --check`. An AC of the form "`node --check <workflow>` exit 0" is therefore a structural false-FAIL by construction — it can never pass for a correctly-written workflow. The runtime-faithful syntax check is: strip the `export const meta` ESM line (`sed 's/^export const meta/const meta/'`), wrap the remainder in `async function __wf(){ ... }` with the injected globals declared as `let`, then `node --check` that — exit 0 confirms the body is valid JS including the legal top-level `return`.
- **Action**: Never write an AC that runs bare `node --check` on a `.workflow.js` file. For workflow syntax validation use the wrapped-body check: `{ echo 'async function __wf(){'; echo 'let args,agent,parallel,phase,log,budget;'; sed 's/^export const meta/const meta/' <wf>; echo '}'; } | node --check /dev/stdin`. This is the workflow-file extension of the existing "dry-run verification commands on real artifacts" rule — the AC author must dry-run the check against an existing known-good workflow (loop-discover) and see it behave as expected before shipping the AC.
- **Grounded in**: .claude/workflows/surplus-scan.workflow.js, .claude/workflows/loop-discover.workflow.js (top-level return convention), COMPLETION-20260607-surplus-scan-phase1.md Note A
- **failure_mode**: Naive default: use bare `node --check` to syntax-validate a `.workflow.js` file. Why wrong: workflow files use top-level `return` (legal inside the runtime wrapper but illegal as standalone JS), so `node --check` ALWAYS exits 1 on correct workflow files — a false gate that trains the operator to ignore it, hiding real syntax errors.

### Workflow `agent({schema})` Requires Top-Level `type: 'object'` — a Top-Level Array Schema is Rejected by the StructuredOutput API - 2026-06-08
- **Context**: Gate 3 live run of surplus-scan.workflow.js FAILED instantly — every reader + generator returned `API Error: 400 tools.6.custom.input_schema.type: Input should be 'object'` with 0 tokens. Two call sites passed `schema: { type: 'array', items: CANDIDATE_SCHEMA }`.
- **Discovery**: The `agent(prompt, { schema })` StructuredOutput tool turns `schema` into an Anthropic tool `input_schema`, which the API requires to have TOP-LEVEL `type: 'object'`. A top-level array schema is rejected at request validation — BEFORE the agent runs (0 tokens consumed), so it fails fast and total, not partially. Fix: wrap the array under an object key — `{ type: 'object', properties: { candidates: { type: 'array', items: CANDIDATE_SCHEMA } }, required: ['candidates'] }` — and read results via `result.candidates`. The prompt must also instruct the agent to return `{"candidates": [...]}` not a bare array. Note: verifier `grep -c "type: 'array', items: CANDIDATE_SCHEMA"` is NOT 0 after the fix because the wrapper definition legitimately nests that substring; the load-bearing check is `grep -nE "schema: \{ type: 'array'"` (no top-level array at any CALL site) being empty.
- **Action**: Any workflow `agent(…, {schema})` that wants a list output MUST wrap it as `{ items_key: { type: 'array', items: ITEM } }` with top-level `type: 'object'`; never pass a top-level array schema. Read back via `result.items_key`. **NOTE: epic-audit.workflow.js:80 has the same latent top-level-array bug** (not yet triggered) — fix when next touched.
- **Grounded in**: .claude/workflows/surplus-scan.workflow.js (CANDIDATES_SCHEMA wrapper, scan + generate call sites), .claude/workflows/epic-audit.workflow.js:80 (latent)
- **failure_mode**: Naive default: pass a top-level array schema (`{ type: 'array', items: ... }`) to `agent({schema})` for list outputs. Why wrong: the Anthropic StructuredOutput API requires top-level `type: 'object'` — a top-level array is rejected at request validation (0 tokens consumed, instant API 400), causing total workflow failure before any agent runs.

### Workflow `{name:}` Invocation Can Load a STALE Cached Copy — Use `scriptPath` to Test Fresh Edits - 2026-06-08
- **Context**: Surplus Epic Phase 1 Gate 3. After fixing a schema bug in `.claude/workflows/surplus-scan.workflow.js` and committing, two consecutive `Workflow({name:'surplus-scan'})` runs failed with the IDENTICAL pre-fix error (0 tokens, instant API 400). The committed file was correct; the per-run runtime copy (`surplus-scan-wf_*.js`) still contained the OLD code.
- **Discovery**: `Workflow({name:'X'})` resolved `X` to a cached/stale snapshot, not the freshly-edited source file — so the fix never actually ran. Invoking `Workflow({scriptPath:'<abs path to .claude/workflows/X.workflow.js>'})` force-loads the real file (the tool result's "Script file:" line then points at the actual path, not a `-wf_*` copy). Confirmed by grepping the runtime copy vs the committed file: call sites differed.
- **Action**: When iterating on a workflow and re-testing, invoke via `scriptPath` (absolute path to the committed `.workflow.js`), NOT `{name:}`. Verify the tool result's "Script file:" line points at your real path. Cross-check `grep "schema:" <runtime-copy>` vs the committed file if a fix "didn't take."
- **Compounding lesson (validation theater)**: 4 expert reviews (2 design + 2 impl) all PASSED, yet the first live run still caught 2 real bugs (top-level-array schema rejected by StructuredOutput API; stale-copy). Static review + `grep`/`node --check` cannot see API-level schema rejection or runtime arg propagation. Only a live workflow run is ground truth for a `.workflow.js`. (Reinforces YOLO-audit "validation theater" + honest_partial.)
- **Also surfaced**: workflow `args` (`date`/`output_path`) did not propagate in direct invocation → `dateStamp` defaulted to `'undated'`; robust fix is for the invoking SKILL to OWN the output filename rather than trust the workflow's returned `plan_path`.
- **Grounded in**: .claude/workflows/surplus-scan.workflow.js, EPIC-20260607-surplus-burn-mode.md, this session's Gate 3 (runs wf_fa778ee9 / wf_e1f59ad4 stale, wf_82ab1266 scriptPath PASS)
- **failure_mode**: Naive default: invoke a workflow by name (`Workflow({name:'X'})`) after editing the file, assuming it picks up the latest version. Why wrong: named resolution can cache a stale snapshot — edits never take effect, and debugging chases phantom bugs in the old code while the fix sits unused on disk.

### Shell Case-Glob Backslash and AC Shell-Expansion Consistency - 2026-06-09
- **Context**: Migration manifest schema v1 (HANDOFF-20260609-migration-schema-phase1). Validator snippet used `*'\\'*` to match a single backslash in a bash case statement. AC2a used `for t in '\\'; do grep -cF -- "\`$t\`" file; done` to verify the backslash token appeared in the document.
- **Discovery**: Two independent shell quoting pitfalls compounded: (1) In bash `case`, `*'\\'*` matches a literal TWO-character `\\` sequence, NOT a single backslash. The correct single-backslash case pattern is `*\\*` (unquoted). (2) In `for t in '\\'`, the variable `$t` is literally `\\` (two chars), so the AC grep searches for `` `\\` `` (backtick + double-backslash + backtick). The document must therefore use `\\` between backticks to pass the AC, even though the forbidden CHARACTER is a single `\`. These are complementary: the case pattern must use `*\\*` to match single backslashes in paths, while the AC grep must find `\\` in backticks in the documentation. Neither is wrong individually — the issue is that their quoting behaviors must be independently correct and consistent with each other.
- **Action**: (1) For bash `case` patterns matching a single backslash: use `*\\*` (no quotes around the backslash). (2) For AC grep targets involving backslash: the document must use `\\` between backticks to match the `'\\'` shell-expansion. (3) When writing both a validator snippet AND an AC that verifies the snippet's documentation, dry-run BOTH independently — a correct case pattern and a correct AC grep can still disagree on the document's literal content.
- **Grounded in**: .tad/evidence/designs/migration-manifest-schema-v1.md (validator snippet + Step 2), HANDOFF-20260609-migration-schema-phase1.md AC2a
- **failure_mode**: Naive default: assume shell quoting of backslashes in a `case` pattern and in an AC `grep` command will behave identically. Why wrong: `*'\\'*` matches a literal two-character `\\` (not a single `\`), and `for t in '\\'` expands `$t` to `\\` — so the case pattern and the AC grep silently disagree on what they're matching, causing either false PASSes or false FAILs depending on document content.

### Fail-Safe-Default Requirements Need an Undecidable-Input AC - 2026-06-10
- **Context**: Gate 4 acceptance of the codex-parity step3b gate. Handoff FR2 mandated "when the heuristic cannot decide → agents-newer (STOP), false-positive preferred" for a destructive rsync --delete direction guard. Implementation inverted it: default claude-newer, STOP only on positive agents-newer evidence. Blake's 11 ACs all passed and his Layer 2 review missed it; three independent Gate 4 verifiers (AC adversarial probe, code-reviewer, security-auditor) converged on the same root cause, security reproducing latent data loss (spaced filename → parse fail → fail-open to the destructive direction).
- **Discovery**: ACs that only exercise DECIDABLE inputs (clean drift on one side, orphan, clean tree) cannot distinguish "defaults to STOP, promotes on proof" from "defaults to proceed, demotes on evidence" — both pass identical decidable-case tests. The fail-safe default is only observable under undecidable/degraded input (parse failure, non-git tree, mixed commit, deleted-side file). A spec line "when unsure → X" is untested unless some AC constructs the "unsure" state.
- **Action**: For any requirement of the form "when undecidable/unknown → safe default", add an AC that constructs an undecidable input and asserts the safe outcome. Cheap deterministic instance for git-heuristic tools: copy the trees to a NON-GIT temp dir, introduce drift, assert the destructive path REFUSES (heuristic blind → must STOP). Also: a "documented-limitation comment" is not a fix for a fail-open — reviewers should flag comment-only fixes on load-bearing safety paths.
- **Amendment (2026-06-10, Gate 4 rounds 2-3 of the same task)**: One representative undecidable-input AC is NOT enough — Blake's AC12 (non-git tmpdir) passed while the spaced-filename undecidable case still destroyed data, because the fix inverted the DIRECTION seed but left the promotion gate (`all_claude_newer=true` + skip-on-parse-failure) optimistic. Two refinements: (a) enumerate ALL input classes where the heuristic goes blind (non-git, parse failure, mixed commit, deleted-side) and write one AC per class; (b) when reviewing a fail-safe fix, check EVERY variable in the decision chain — a fix that flips the seed but not the exit condition migrates the fail-open instead of closing it.
- **failure_mode**: Naive default: test fail-safe defaults only with decidable/clean inputs (e.g., clear drift, clean orphan). Why wrong: both "default to STOP" and "default to proceed" produce identical outputs on decidable inputs — the fail-safe is only observable under undecidable/degraded input (parse failure, non-git tree), so decidable-only ACs cannot distinguish a safe implementation from a dangerous one.

### A "Real-Browser E2E Passed" Claim Is Confounded If the Test Path Doesn't Enforce the Property Under Test - 2026-06-14
- **Context**: Gate 4 acceptance of AI-Native Reading Companion Phase 3 (localhost co-read bridge). Blake's completion report claimed a REAL Chrome end-to-end (send→reply renders; XSS inert) and 15/15 ACs PASS. The independent Gate 3 review (security-auditor + code-reviewer) then found **P0**: the strict CSP (`default-src 'self'`, no `script-src`/nonce) would make an *enforcing* browser refuse the reader's OWN inline `<script>`/`<style>` → the chat panel/SSE never run → feature dead. The earlier "worked in Chrome" was real but ran on a **non-enforcing-CSP path** (the `file://` / served-without-strict-CSP route), so it never exercised the very property (CSP enforcement) that was broken.
- **Discovery**: An e2e/behavioral claim is only as strong as whether the test ENVIRONMENT actually enforces the property being asserted. "Rendered fine in a browser" silently passes when the browser path doesn't apply the production CSP — a false green that looks like the strongest possible evidence (a live browser). This is the verification-integrity sibling of "禁止纸面验收": the test wasn't paper, but the environment confounded it. Independent adversarial review on the *enforcing* path is what caught it; a single self-reported happy-path browser run did not. The fix (per-response CSP nonce injected into the in-memory HTML, on-disk file nonce-free) also has to be verified on the enforcing path — and here even the fix's final browser-nav step was denied, so it fell back to a nonce-match assertion, leaving the real-browser *visual* confirmation genuinely UNVERIFIED and explicitly carried forward to first real use.
- **Action**: (1) When an AC asserts a behavioral/e2e property, name the ENVIRONMENT that enforces it and verify on that path — for CSP/security headers, a browser that actually enforces the header, not a `file://` or stripped-header route. (2) Treat "worked in a browser" as INSUFFICIENT for a property the browser may not be enforcing on that path; require the enforcing-path run or mark the AC UNVERIFIED honestly. (3) When a self-reported real-browser e2e exists, still run independent adversarial review — it caught a feature-dead P0 that the live-browser claim missed. (4) If the final enforcing-path step can't be run (nav denied), say so and carry it forward; a nonce-match/structural fallback is not the same evidence as a rendered page.
- **Grounded in**: .tad/evidence/yolo/ai-native-reading-companion/phase3-completion.md (Gate 3 Fix Round P0#1), .claude/skills/reading-companion/tools/bridge-server.py (_inject_nonce + per-response CSP), EPIC-20260613-ai-native-reading-companion.md Phase 3 detail block
- **failure_mode**: Naive default: trust a self-reported "worked in a browser" e2e claim as proof that a security property (CSP, CORS, etc.) is enforced. Why wrong: the browser path may not enforce the property under test (e.g., `file://` ignores CSP), producing a false green that looks like the strongest possible evidence while the feature is actually dead under production enforcement.

### A Pre-Declared Exclusion Must Be Restated in Every Gate Metric Definition That Inherits It - 2026-07-02
- **Context**: trajectory-eval-p2 calibration. Phase 1 pre-declared "D4 is data-poor — excluded from 止损判定". The §4.4 metrics contract encoded the exclusion explicitly in the PRIMARY gate (within-1 over D1,D2,D3,D5) but the contrast-pair gate's mean definition said only "与配对规则一致" (consistent with pairing rules). At Gate 3 this produced a verdict-flipping ambiguity: contrast = 1.75 PASS (D4-excluded) vs 1.40 FAIL (D4-included). Blake resolved via the pre-declaration and self-reported the ambiguity; Gate 4 verified no metric-shopping (round 1 FAILed under both readings) and accepted.
- **Discovery**: A pre-declared exclusion propagates reliably only where it is RESTATED INLINE. A downstream metric that references another metric's pairing rules inherits the mechanics (numeric-vs-numeric pairing) but NOT the scoped exclusions, because the exclusion was syntactically attached to the primary metric's dim list, not to the pairing rule itself. When the ambiguous metric is a gate, the ambiguity is verdict-flipping by construction. Two saving graces made this recoverable rather than corrupting: (a) the exclusion was PRE-declared before any data existed (so applying it is not post-hoc fitting), (b) the implementer surfaced the ambiguity instead of silently picking the passing reading.
- **Action**: When writing a metrics contract with N gate metrics: restate every exclusion (advisory dims, data-poor dims, UNRECOVERABLE handling) inline in EACH gate metric's definition — never rely on "consistent with the other metric's rules". At review time, test each gate metric definition by asking "compute this with and without each pre-declared exclusion — do any two readings flip the verdict?" If yes and the text doesn't force one reading, the contract is incomplete.
- **failure_mode**: Naive default: define exclusions once (in the primary metric or a preamble) and let sibling metric definitions inherit by reference. Why wrong: the reference inherits pairing mechanics but not scoped exclusions; the implementer must choose a reading at runtime, and under gate pressure the passing reading is the tempting one — a verdict-flipping ambiguity that looks like a spec detail until it decides PASS vs FAIL.
- **Grounded in**: HANDOFF-20260702-trajectory-eval-p2.md §4.4 + gate4_delta, COMPLETION-20260702-trajectory-eval-p2.md KA #2 (independent convergence), .tad/evidence/acceptance-tests/trajectory-eval-p2/gate4-acceptance-report.md (双口径重算)

### A `node --check` Syntax-Gate Is a FALSE GATE on Workflow Files (Top-Level return+await); Wrap the Body in an async Function First - 2026-06-14
- **Context**: Fixing 3 top-level-array StructuredOutput schemas across `epic-audit`/`loop-discover` workflows (`.claude/workflows/*.workflow.js`). The handoff's AC4 specified `node --input-type=module --check < file` (and naive `node --check`) to syntax-verify the edits. A Gate-2 reviewer ran it empirically: it **fails on the UNMODIFIED files** with `SyntaxError: Illegal return statement` — exit 1 for clean and broken alike. A gate that red-lights correct code is worse than no gate (the operator learns to ignore it; a real syntax error hides in the noise).
- **Discovery**: `.workflow.js` files are NOT standalone ES modules. The Workflow runtime evaluates each file body **inside an async function**, so they legally use module-level top-level `return` AND top-level `await` AND start with `export const meta`. `node --check` / `--input-type=module` treat the file as a top-level script/module where top-level `return` is illegal → false FAIL. (The handoff author guessed the cause was "ESM + top-level await" but the load-bearing illegality is top-level `return`.) A syntax gate must reproduce the REAL evaluation context. Verified working command: prepend `async function __wf__(agent,log,args,parallel,phase,pipeline,budget,workflow){`, strip the line-1 `export ` (`sed '1s/^export const /const /'`), append `}`, pipe to `node --check --input-type=commonjs`. This PASSES all clean workflow files and CATCHES an injected mid-file syntax error (line number reported). A `.js`-extension temp-file variant works too (a temp without `.js` throws `ERR_UNKNOWN_FILE_EXTENSION`).
- **Action**: (1) Before trusting ANY verifier in an AC, run it against (a) a known-GOOD input — it must pass, and (b) a known-BAD input — it must fail. A verifier that fails clean inputs is a false gate; fix it before the AC ships. (2) To syntax-check TAD `.workflow.js` files, use the async-wrap command above — never bare `node --check`/`--input-type=module`. (3) When you fix a bug CLASS (here: top-level-array schema), grep the whole `.claude/workflows/` tree for siblings — this sweep turned 1 reported site (epic-audit:80) into 3 (loop-discover:80 + :111, the latter the core discovery loop = whole workflow broken).
- **Grounded in**: HANDOFF-20260614-fix-workflow-toplevel-array-schemas.md Gate-2 Audit Trail (AC4 false-gate) + §9.1 AC4 verified command; surplus-scan.workflow.js:73-76 (the documented top-level-array contract)
- **failure_mode**: Naive default: trust a verifier command in an AC without first running it against both a known-GOOD input (must pass) and a known-BAD input (must fail). Why wrong: a verifier that fails on correct input (like `node --check` on workflow files) is a false gate — the operator learns to ignore it, so when a real syntax error occurs, the gate has already been dismissed as noise.

### Workflow `args` Are Not Injected in EITHER `name:` OR `scriptPath:` Mode — Hardcode a Default or Go Conductor-Manual - 2026-06-13, re-confirmed 2026-07-05
- **Discovery**: `Workflow({name:'yolo-epic', args:{...}})` failed twice with instant 0-agent `{"error":"missing required args"}` (29ms/7ms) on 2026-06-13, regardless of args as string or object. On 2026-07-05 surplus-scan confirmed the `scriptPath` invocation ALSO fails to inject args — the script-side `args` global stays empty in both modes. Working workaround for few-scalar-args cases: Edit the persisted script to hardcode the required arg as a default (e.g., `if (!dateStamp) dateStamp = '2026-07-05'`), then re-run via scriptPath with no args — one-shot success (64 candidates, 4 agents). NOTE: `.claude/skills/alex/references/yolo-execution-protocol.md` L33 still instructs "args MUST be a JSON object, NOT a stringified JSON string" as if injection works — the protocol contradicts observed harness behavior and needs an Alex fix.
- **Action**: After 2 consecutive arg-injection failures, stop retrying the Workflow path. Few scalar args → hardcode defaults into the persisted script, re-run via scriptPath. Complex args (large arrays) → Conductor-manual fallback: spawn 1 implement sub-agent (general-purpose, run_in_background) per handoff → SendMessage for iterations → ≥2 independent reviewers → Conductor personally runs ACs for Gate 3.
- **failure_mode**: Naive default: keep re-invoking the Workflow with differently-shaped args (string vs object, name vs scriptPath) assuming a formatting mistake. Why wrong: args are not plumbed into the script global in either invocation mode in this harness — every retry fails identically at 0 agents; only hardcoded defaults or Conductor-manual orchestration makes progress.
- **Grounded in**: feedback_yolo-epic-workflow-args.md, surplus-scan run 2026-07-05, .claude/skills/alex/references/yolo-execution-protocol.md L20-40

### Verification Commands That Read the Git Index Are Vacuous Before Staging - 2026-07-12
- **Discovery**: An AC ran a `git ls-files`-derived grep before anything was staged; the empty enumerated set made the grep scan nothing and the AC PASSed trivially. Re-run after `git add` (29 files in the index) produced the non-vacuous result; an independent direct 29-file scan corroborated. Generalizes: any verifier whose input set is enumerated from repo state (index, staged set, tag, branch diff) passes trivially when that state is empty or not yet populated.
- **Action**: For any AC built on `git ls-files`/index/diff-set enumeration: assert the enumerated set is NON-EMPTY first (e.g., `test $(git ls-files -- <paths> | wc -l) -gt 0`) or sequence the AC explicitly AFTER the populating step (git add/commit). Prefer verifiers that fail loudly on empty input sets.
- **failure_mode**: Naive default: run an index-derived verification command at whatever point is convenient and read PASS at face value. Why wrong: pre-staging, the enumerated set is empty, so the scan runs on nothing and PASSes vacuously — the check certifies nothing while looking green, exactly the false-green class Gate 3 exists to prevent.
- **Grounded in**: .tad/evidence/journal/memory-redirect-capture-layer-2026-07-12.md finding 6 (AC10)

### Design-Agent AC Commands Systematically Assume a Machine That Isn't This One — Dry-Run Every AC on the LIVE Host Before Handoff Lock - 2026-07-13
- **Context**: EPIC-20260712-native-capability-adoption, YOLO run: 2 of the 3 freshly-designed phase handoffs shipped P0-grade AC-runnability defects, caught only because design reviewers executed the commands live. P2: `python3 -c "import yaml"` (pyyaml absent from Homebrew python3 — the sole structural gate would false-FAIL every valid deliverable) + `grep -L` with an empty `$(...)` file-arg list (reads STDIN → hangs a non-interactive Gate 3). P4: `git diff --stat | grep -c '.js'` (substring matches `.jsonl` trace files → false-positive 3 at BASELINE) + the same pyyaml assumption AGAIN in a different AC.
- **Discovery**: The defect class is uniform: the design agent writes verification commands against a GENERIC POSIX/CPython machine, not the actual host (no pyyaml; BSD userland; `.jsonl` audit files in the tree; repo has no tsc/npm gates). It repeats across independent design agents in the same Epic — this is a systematic generation bias, not a one-off. The complementary discipline already exists for verifiers (known-GOOD must pass / known-BAD must fail); what was missing is running it AT DESIGN TIME on baseline state: a post-impl AC that returns non-zero/hangs/passes-trivially ON THE UNMODIFIED REPO is broken by construction and can be caught before Blake ever starts.
- **Action**: (1) Every AC command must be executed once against the live baseline during design review (pre-impl rows verify recorded values; post-impl rows must fail-CLEANLY-for-the-right-reason — "file missing", not ModuleNotFoundError/STDIN-hang/false-positive-count). (2) Ban list for this host, cite in handoffs: `import yaml` (use yq or stdlib parse), unguarded `$(grep -l ...)` arg lists (empty-set guard mandatory), unanchored extension greps (`\.js` → `\.js$` via `--name-only`). (3) Grounding files should carry a "Layer 1 substitutes for this repo" block — P1's grounding did and P1 shipped zero AC-runnability defects; P2/P4 designs drifted where grounding was silent.
- **failure_mode**: Naive default: trust that a syntactically-plausible AC command runs on the target machine because it runs "anywhere". Why wrong: host-specific gaps (missing python modules, BSD vs GNU semantics, repo artifacts like .jsonl) make the command false-FAIL, hang, or false-PASS at baseline — a Gate 3 executed on broken verifiers either blocks good work or green-lights nothing-checked.
- **Grounded in**: .tad/evidence/yolo/native-capability-adoption/phase2-design-review-cr.md (P0-1/P0-2), phase4-design-review-cr.md (P0-1/P0-2, P1-1..P1-4), EPIC-COMPLETION.md KA(b)

### Section-Scoped Checks Must Share the Governed Location's Exact Boundary Semantics - 2026-08-03
- **Context**: lite-review-hardening (Codex Blake). Two independent instances in one task: (a) AC8's evidence extractor `awk '/^## 执行证据$/,/^## /'` — the end pattern also matched the start heading, so the extracted "section" was just the heading and the check falsely reported missing evidence on a correct report; (b) a mandated reviewer-prompt sentence existed in a later Completion-template copy but was absent from the required L3 prompt location — whole-file grep would have passed; only the spec reviewer's location-scoped check caught it. Same defect class proven earlier the same day at Gate 2: a one-line append inside `## Forbidden` satisfied every whole-file-grep AC.
- **Discovery**: A section-scoped verification is only as good as its boundary expression. Two failure directions: range expressions whose end pattern can match the start line (extractor returns the heading alone → false negative on correct content), and anchors scoped wider than the governed location (text lands anywhere → false positive on wrong placement). `grep -cE 'a|b'` adds a third: it counts LINES, so prose pairing two required terms on one line under-counts and fails a compliant implementation — an AC that fails correct work pressures the operator to weaken the check.
- **Action**: (1) Extract sections with a stateful awk (start AFTER the heading, stop at next heading), never `/start/,/end/` where end can match start. (2) Anchor ACs to the governed section via its own heading + a position assertion (line-number between enclosing headings), never whole-file grep. (3) Assert multi-term presence per-term in a loop (names the missing clause), never a combined `grep -cE` tally. (4) Dry-run every AC against a simulated CORRECT implementation before shipping the contract — an AC that reds a correct build is worse than no AC.
- **failure_mode**: Naive default: verify "the text landed" with whole-file grep and verify sections with `/start/,/end/` ranges. Why wrong: whole-file grep passes with text in the wrong (even forbidden) location; ranges whose end matches start silently truncate to the heading; line-counting tallies fail compliant prose — all three were empirically demonstrated within a single day (2026-08-02/03).
- **Grounded in**: .tad/evidence/journal/lite-review-hardening-2026-08-03.md, .tad/archive/handoffs/HANDOFF-20260803-lite-review-hardening.md §4 + Audit Trail R1/CR-P0-3, .tad/evidence/reviews/blake/lite-review-hardening/spec-compliance-reviewer.md

### Exit-0 ACs Are Unsatisfiable When the Verifier Has Legitimate BLOCK States — Use Two-Branch Delta Assertions - 2026-08-03
- **Context**: codex-wiring-stopbleed Gate 2. Handoff v2 paired "AC: `runtime-freshness-verify.sh` exit 0" with "§design: unverifiable rows keep `unknown_current_behavior` + genuine old `last_verified`". The verifier's own decision table (safety-surface unknown → BLOCK; high-volatility stale >30d → BLOCK) made the two mandates mathematically incompatible. Both independent Gate 2 reviewers collided on the same defect in the same round. The danger is directional: the implementer's nearest "exit" from the deadlock is falsifying provenance (backdating `last_verified`) — exactly what the design forbade.
- **Discovery**: When an AC asserts a gate-tool's success exit code, and the same contract legitimately mandates states that the tool's decision table maps to failure, the AC is unsatisfiable by construction — and unsatisfiable ACs don't just fail, they pressure the implementer toward the forbidden shortcut (edit dates/status/verifier to force green). The fix is a two-branch delta assertion: branch α (all governed rows reach a verified state via pre-authorized evidence categories → exit 0) OR branch β (residual BLOCK set ⊆ a pre-registered escalation list, zero BLOCKs from verified rows, count strictly below baseline, nonzero exit explicitly recorded as honest_partial for human disposition). The pre-registered list is a test contract, not a license to weaken the runtime verifier.
- **Action**: Before writing "tool X exits 0" as an AC, read tool X's decision table and ask: does this same contract mandate any state that table maps to failure? If yes, write the two-branch delta form with the escalation list pre-registered in the handoff/evidence, and explicitly forbid editing the verifier, dates, or status fields to reach green outside the two branches.
- **failure_mode**: Naive default: "the verifier passing" as the acceptance signal, assuming honesty mandates and green lights are independently satisfiable. Why wrong: fail-closed verifiers exist precisely to go red on honest uncertainty; demanding exit-0 while mandating honest uncertainty gives the implementer only dishonest paths to completion.
- **Grounded in**: .tad/archive/handoffs/HANDOFF-20260803-codex-wiring-stopbleed.md AC7 + Audit Trail R2, .tad/hooks/lib/runtime-freshness-verify.sh (SAFETY_SURFACES/staleness decision table), .tad/evidence/acceptance-tests/codex-wiring-stopbleed/ac7-branch-escalation.md

### State-Isolated Spikes Strip Credentials — Fixtures Prove Parser Behavior, Not Runtime Delivery - 2026-08-03
- **Context**: codex-knowledge-ingress. Spike C/D/E used an isolated scratch `CODEX_HOME` (correctly protecting the user's real trust/state sqlite, per the prior single's protocol) — but the scratch home carries no `auth.json`, so every probe died at HTTP 401 before an authenticated model turn. Hook envelope delivery, PreCompact fire, and child-process harness signals all went unmeasured; a parallel Claude-side probe separately exited at a $0.05 budget cap. The implementation still shipped safely because the contract had pre-specified conservative terminal states (fields stay empty, no wiring, ledger `verified_partial`, no doc-derived signal promotion) and the AC oracles (spike-set-equality) mechanically blocked substituting documentation for measurement.
- **Discovery**: (1) State isolation and authentication are a tradeoff, not a free win: the same isolation that protects user state removes the credentials the probe needs — plan the auth path (copy/mint credentials into the scratch home, or budget an authenticated turn) at spike-design time, or pre-declare which branch fires when auth is unreachable. (2) "Couldn't measure" (401, budget cap) is NOT "measured absent" — the honest-mapping branches a contract pre-authorizes for measured absence must not silently absorb unmeasured cases; the safe absorption is only legitimate when the terminal states happen to coincide with the most conservative branch, and the evidence must say "unmeasured", never "absent". (3) Fixture suites prove parser/consumer behavior under assumed envelope shapes; they prove nothing about runtime delivery — an AC set that is all-fixtures must label the delivery question open.
- **Action**: When designing spikes that need runtime state isolation: enumerate what the isolation strips (auth, trust, caches, budget) and either provision it deliberately or pre-register the couldn't-measure branch with its own terminal states (distinct from measured-absence). Keep an oracle that binds implementation inputs to measured evidence (set-equality) so unmeasured candidates cannot be promoted. Queue the authenticated re-probe as an explicit follow-up, not a silent loss.
- **failure_mode**: Naive default: treat probe failure as equivalent to "feature not supported" and take the honest-absence branch, or quietly fall back to documentation values so the feature can ship "complete". Why wrong: the first fabricates a measurement that never happened (the ledger then reports verified-absent on zero evidence); the second is exactly the doc-evidence-as-verified failure the ledger discipline exists to block.
- **Grounded in**: .tad/evidence/acceptance-tests/codex-knowledge-ingress/spike-{c,d,e}.md, .tad/evidence/journal/codex-knowledge-ingress-2026-08-03.md, .tad/runtime-compat/codex.md (context_compaction verified_partial), .tad/archive/handoffs/HANDOFF-20260803-codex-knowledge-ingress.md AC5/AC6

### Text-Anchor ACs Are Blind to Parser-Level Regressions — Structured-Header Files Need an Active-Parse Guard, and Published Commands Must Be Tested as Literal Text - 2026-08-03

- **Context**: model-vocabulary-universalization inserted an identical clause paragraph into 11 SKILL.md
  bodies. All grep-anchor ACs (`grep -cF '平台绑定交互决策'` ≥1) went green — but Layer 2 code review
  found the alex-side clause had landed INSIDE the YAML front matter (before the closing `---`), a real
  parser regression that would corrupt frontmatter-driven loading. Separately, the security reviewer
  found the AC scripts validated a happy-path WRAPPER reimplementation of the contract's published
  Codex capture commands, not the literal command text, and compared SAFETY blocks as a file-global
  bag instead of per-block.
- **Discovery**: (1) A substring-presence AC cannot distinguish "clause in the body" from "clause in
  the front matter" — the anchor matches either way, so structural validity is invisible to every
  text-level check. The fix class is an ACTIVE PARSE: run the format's real parser (Ruby
  `YAML.safe_load` on the frontmatter of every edited SKILL, both trees) as an AC guard. (2) When a
  contract publishes literal capture/probe commands, an AC that reimplements them "equivalently"
  proves the wrapper, not the contract — fail-soft and redaction must be probed against the PUBLISHED
  text (missing file, unset env, unmatched glob fixtures). (3) Per-block comparison needs ordinal
  ownership prefixes, or block reordering/relocation across sections is invisible to a sorted bag.
- **Action**: When an AC suite verifies text insertions into files with structured headers
  (frontmatter, TOML sections, shebang blocks), add an active-parse guard for the header format on
  every edited file in addition to text anchors. When the contract publishes literal commands, execute
  those literal commands against absence fixtures (file/key/env/glob missing) instead of a wrapper.
  Prefix extracted SAFETY blocks with section-ordinal ownership before sorted comparison.
- **Grounded in**: .tad/evidence/journal/2026-08-03/model-vocabulary-universalization.md (Layer 2
  reflexion + Final validator reflexion), .tad/evidence/reviews/blake/model-vocabulary-universalization/
  security-auditor.md, AC-06/AC-09 post-remediation scripts

### An AC Keyed on Commit Contents Is Coupled to `git add` Hygiene AND Freezes at That Commit - 2026-08-04

- **Context**: evidence-replayability-check. Gate 3 has a BLOCKING `Git_Commit_Verification`, so a
  handoff cannot end uncommitted; but the contract also needed to assert "exactly these 3 tracked
  files were modified". Using `git status --porcelain` for that is unsatisfiable by construction
  (after the mandatory commit the M set is empty), so the AC was rewritten to read the commit's own
  diff: `git show --name-status --format='' HEAD | awk '$1=="M"{print $2}'`.
- **Discovery**: Switching an AC's judgment source from working-tree state to COMMIT CONTENTS buys
  satisfiability but creates two new couplings that are invisible in the AC text. (1) **`git add -A`
  breaks it**: any file a concurrent process touched (here NEXT.md, being used as a queue bookmark in
  the other terminal) rides into the commit and inflates the M set. The commit must pin explicit
  paths. (2) **The AC freezes at that commit**: evidence generated AFTER the commit cannot be
  committed later, because a follow-up commit becomes the new HEAD with an empty-or-different M set,
  so recomputing the AC at Gate 4 FAILs. Post-commit evidence has to stay in the working tree as its
  carrier (`A` additions don't disturb an `M`-only predicate, but a second commit does).
- **Action**: When an AC's predicate reads `HEAD`, write both couplings into the contract: pin
  explicit paths in `git add` (never `-A`), and state that no further commit may be made after the
  evidence-generating run. Prefer an `M`-only predicate so later `A` files stay harmless. At Gate 4,
  recompute from `git show` rather than trusting the reported file list.
- **failure_mode**: Naive default: use `git status --porcelain` for a "which files changed" AC, or
  `git add -A` for convenience, or tidy up by committing the evidence afterward. Why wrong: the first
  is unsatisfiable once Gate 3 mandates a commit; the second silently absorbs concurrent edits from
  the other terminal; the third invalidates the very AC it was meant to document — and all three fail
  at Gate 4 recompute, after the implementer has already declared PASS.
- **Grounded in**: .tad/evidence/journal/evidence-replayability-check-2026-08-04.md (commit-blood-path),
  HANDOFF-20260804-evidence-replayability-check.md AC6, commit 31a96aa

### A Count-Based AC Constrains the Total, Not the Location — Pin the Siblings - 2026-08-04

- **Context**: Same single. `Critical Check (6 items):` already existed in the Gate 2 block of
  `gate/SKILL.md`, so a bare `grep -Fq` would PASS before any implementation (zero discriminative
  power). The AC was written as `grep -cF ... -eq 2` (1 pre-existing + 1 new).
- **Discovery**: `-c == N` fixes the total but says nothing about WHERE the new occurrence landed. A
  Gate 2 reviewer built the falsifying variant and ran it: put the new `(6 items):` in the Gate 4
  block (L727) and leave the Gate 3 block at `(5 items)` — total is still 2, the AC PASSes, the
  implementation is wrong. The cheap fix is not section-scoped extraction (fragile boundary logic)
  but **complement assertions on the sibling values**: `(5 items) == 0` (the old header must be gone)
  plus `(4 items) == 1` (an untouched block's invariant). Measured: BASE FAIL / HALF FAIL / WRONG FAIL
  / FULL PASS.
- **Action**: Whenever an AC counts occurrences of a string that also exists elsewhere in the file,
  add (a) a zero-assertion on the value being replaced and (b) a fixed-count assertion on at least one
  sibling that must NOT change. Then construct the "right count, wrong place" variant and prove the
  hardened AC kills it — a count AC that has not been shown to fail on a misplacement is unverified.
- **failure_mode**: Naive default: after discovering a bare `-Fq` is non-discriminative, upgrade it to
  `-c == N` and consider the problem solved. Why wrong: it closes the "did nothing" hole and leaves
  the "did it in the wrong block" hole wide open, and the second one is worse — it ships a broken edit
  under a green gate.
- **Grounded in**: .tad/evidence/journal/evidence-replayability-check-2026-08-04.md (ac-design),
  .tad/evidence/reviews/blake/evidence-replayability-check/code-reviewer.md, HANDOFF §9.1 AC2/AC3

### A Snapshot-Diff Scope Fence Is Valid Only Inside a Window Bounded on BOTH Sides by Framework Artifact Production - 2026-08-05
- **Context**: P1a of the lite-as-TAD-body Epic. The repo carries 3 pre-existing dirty tracked files and 850+ untracked files, so an absolute-list scope fence (`git status` must contain only authorized paths) false-FAILs by construction. The fence was therefore built as a snapshot diff: capture the tracked/untracked sets before work, `comm -13` after, and flag additions outside an allowlist. It fired three times, none of them on an actual violation.
- **Discovery**: A snapshot-diff fence measures *set additions between two instants*, so its correctness depends entirely on where those instants sit relative to the framework's own file production. Three distinct failures, all timing, none logic: (a) the before-snapshot was taken before the protocol's own init step wrote a state file, so a framework artifact looked like an unauthorized addition; (b) `git add` **migrates a path from the untracked set to the tracked set**, so an authorized deliverable staged per the contract's own commit instructions appears as a *new* tracked entry — an allowlist that isn't shared by both fences flags legitimate work; (c) the after-snapshot is taken before the protocol mandates writing its completion report and before a PostToolUse hook appends its decision log, so re-running the fence at gate time always reports artifacts the contract never authorized because they did not exist when it was written. The unifying root cause: **the allowlist and the snapshot instants were written against the repo's state at authoring time, not its state at AC-run time.**
- **Action**: (1) Take the before-snapshot *after* every framework step that precedes implementation. (2) Define the allowlist **once** and have every fence read it — separate copies diverge exactly at the tracked/untracked boundary that `git add` crosses. (3) Absorb known post-implementation framework paths (completion reports, hook-written logs) into the allowlist, or pin the fence's evaluation moment to the window "implementation artifacts complete, process paperwork not yet written" and treat later re-runs as smoke only. (4) State the residual honestly: `comm -13` reports additions only, so every file already in the before-set is exempt from further modification — close the tracked side with a content hash (cheap, few files) and record the untracked side as a named blind spot rather than implying coverage.
- **AMENDED 2026-08-05 (next single, tracked side)**: the window is bounded not only by *framework* artifacts but by **any actor other than this task**. The follow-up single's tracked fence flagged `.tad/research-notebooks/REGISTRY.yaml` — a pre-existing tracked file modified by the user in another terminal 40 minutes after the snapshot (`status: active → dormant`, one line). Nothing in the fence can distinguish that from a scope violation. So the fence's real precondition is "only this task touches the repo between the two snapshots", which is false whenever a second terminal, a background sync, or a hook is live. **Attribution is therefore a mandatory step, not an optional courtesy**: for every flagged path, check whether it appears in this task's commit, diff its content, and check its timestamp before judging. The correct disposition for an externally-caused hit is *documented attribution + substantive PASS*, never "re-take the baseline" (that hides the timing) and never "widen the allowlist" (that permanently blinds the fence).
- **failure_mode**: Naive default: snapshot at the top of the script, diff at the bottom, and treat any unexpected path as a scope violation. Why wrong: the framework itself creates files on both sides of the window, and other actors touch the repo during it, so the fence reports its own host protocol — or the user — as the intruder; an operator who sees the fence cry wolf three times will start waving through the one time it is right.
- **Grounded in**: .tad/evidence/journal/lite-pricing-gate-protocol-2026-08-05.md (findings 1 and 5), .tad/evidence/journal/pricing-gate-scan-fix-2026-08-05.md (finding 2, tracked-side external modification), .tad/evidence/reviews/2026-08-05-gate2-audit-lite-pricing-gate.md (R3 P0-1/P0-3), HANDOFF-20260805-lite-pricing-gate-protocol.md §5.0 + AC5/AC5b/AC6

### A Fence Needs a Positive Control Too, and the Probe Must Be Visible to the Specific Fence Being Tested - 2026-08-05
- **Context**: Same single. The scope fence was validated by planting a file in a forbidden directory and confirming it was caught. That negative control passed, and two P0s still shipped into the contract — both of the form "the fence blocks legitimate work."
- **Discovery**: Testing a gate only with a violation proves it *can* fail; it says nothing about whether it *passes the work it is supposed to pass*. Both defects were in that untested direction: the authorized ledger, once `git add`ed, and the protocol-mandated completion report both tripped the fence. Adding the positive control — create the sanctioned deliverables, stage them, confirm the fence stays silent — reproduces both in seconds. Second, complementary finding: a probe only exercises the fence whose **mechanism** can see it. A `touch`ed file is invisible to a `git diff --name-only` fence by construction, so a single probe validated only the untracked fence while appearing to validate "the fence"; testing the tracked fence requires `git add`ing a forbidden path.
- **Action**: For every gate, run both controls and record both outputs as evidence: negative (a violation must be caught) and positive (the sanctioned deliverable set, produced exactly as the contract instructs, must pass silently). When a check is implemented as multiple fences, construct one probe per fence matched to that fence's detection mechanism — and verify each probe actually reaches its target rather than assuming one probe covers all.
- **failure_mode**: Naive default: prove a scope fence works by planting one forbidden file and watching it get flagged. Why wrong: it validates only the reject path of one fence, leaving the accept path untested — so the fence ships blocking the very deliverables the contract requires, and the failure surfaces during implementation as an unexplainable FAIL on correct work.
- **Grounded in**: .tad/evidence/journal/lite-pricing-gate-protocol-2026-08-05.md (finding 2), .tad/evidence/reviews/2026-08-05-gate2-audit-lite-pricing-gate.md (lesson 2), HANDOFF-20260805-lite-pricing-gate-protocol.md §6.5b

### Discriminative Power Can Live in the SEMANTICS of the Hits, Not Their Count — a Count-Only Before/After Comparison Can Read as "No Change" While the Fix Worked - 2026-08-05
- **Context**: Phase 1a-fix of the lite-as-TAD-body Epic replaced a ledger-scanning command that had one false-positive class and four silent false-negative classes. The AC proving the fix worked ran the same synthetic probe through the old and the new command and compared results.
- **Discovery**: Both commands reported **8 hits**. A naive before/after check — `[ "$old_count" -ne "$new_count" ]` — would conclude the fix changed nothing and either fail the AC on a correct implementation or, worse, pass a no-op. The actual difference was entirely in **which rows and under which label**: the old command silently mis-classified a summary-column false positive and three malformed rows (full-width colon, missing colon, wrong case) as ordinary OVERDUE; the new one excluded the false positive and re-labelled the malformed rows as MALFORMED for human handling. Same cardinality, inverted meaning. The generalizable point: when a fix changes *classification* rather than *detection volume*, the discriminating signal is the labelled hit-set, and an AC that compares only counts is blind to exactly the property being fixed.
- **Action**: When proving a fix via old-vs-new comparison, compare the **full labelled output** (`[ "$out" = "$exp" ]` against a verbatim expected block, or a sorted set diff), never just `wc -l`. Build the probe so each scenario is identifiable in the output (a short stable key per row) and state the expected label per scenario. If the counts happen to match, say so explicitly in the evidence — it is the strongest demonstration that the AC measures semantics rather than volume.
- **failure_mode**: Naive default: prove a scan/filter fix by showing the hit count changed. Why wrong: reclassification fixes preserve cardinality, so the count is unchanged and the AC either false-FAILs correct work or certifies a no-op — and the defect it was meant to catch is precisely a mis-labelling.
- **Grounded in**: .tad/evidence/journal/pricing-gate-scan-fix-2026-08-05.md (finding 3), HANDOFF-20260805-pricing-gate-scan-fix.md AC4a (verbatim `exp` block, `=`-separated keys), .tad/evidence/acceptance-tests/pricing-gate-scan-fix/old-new-cmd-ac4a.txt

### Verification Strength Is Bounded by the Deliverable's Determinacy — Shrink the Scope Before Hardening the Checks - 2026-08-05
- **Context**: One Epic phase, four contract versions, seven independent reviewers. v1 (9 ACs, executor makes evidence judgements), v2 (5 ACs, judgement delegated to an adversarial sub-reviewer), v3 (5 ACs, judgements supplied as fixed data but 32 rows of free-form prose retained) — each was defeated by a reviewer constructing a **fully green forged delivery**, in 30, 15 and 20 minutes respectively. Forging got *cheaper* as the checks got more elaborate. v4 kept the same reviewers but cut the deliverable to 6 lines across 5 files; both reviewers then reported the delivery could not be forged.
- **Discovery**: The bound is not the cleverness of the ACs, it is the determinacy of the deliverable. When the deliverable is a **determinate text**, byte-exact comparison (section md5 + `git diff --numstat` shape) is exhaustive and leaves no forgery surface — one reviewer's phrasing: *the regex must actually be fixed correctly to pass*. When the deliverable contains **free-form content** (prose rationales, chosen search keywords, self-reported hit counts), no AC closes the gap; each added check merely relocates the bypass and enlarges the attack surface. v3's most instructive failure: the AC published the exact `git grep` query it would run, so the executor could pipe that query's own output back as the "explanation" — 132 canned lines, 30 seconds, zero files opened. Corollary observed across all four rounds: the defect count in the *verification apparatus* exceeded the defect count in the *deliverable* by more than an order of magnitude, and every apparatus generation introduced new defects while fixing old ones.
- **Action**: Before hardening an AC, ask whether the deliverable can be made determinate instead — split the free-form portion out of the executor's scope rather than trying to grep it. For the determinate remainder, prefer whole-artifact byte comparison (md5 of a bounded region + a change-shape assertion) over property checks; it has no entrance/exit asymmetry and nothing enumerable to target. When a Gate-2 round produces an all-green forgery, treat it as evidence about scope, not about the checks. Never let an AC publish the query whose answer it will accept.
- **failure_mode**: Naive default: when a reviewer forges a green delivery, add or sharpen ACs to close that specific bypass. Why wrong: the bypass was a symptom of free-form deliverable content, so the next round is forged through a different opening — usually faster, because the added machinery is itself new attack surface — and the team spends its budget building verification apparatus rather than shipping the change.
- **Grounded in**: HANDOFF-20260805-lite-inventory-pricing-audit.md §6 (v1/v2/v3 void record), Gate 2 v1–v4 reviewer reports (three forged deliveries with timings), EPIC-20260804-lite-as-tad-body.md P1b split rationale
- **AMENDED 2026-08-06 — the "never publish the answer" clause needs a cost qualifier**: this entry's Action ends with *"Never let an AC publish the query whose answer it will accept."* A later measurement contract deliberately did the opposite — it wrote the canary values, the expected missing-set size, the expected verdicts of three read-only scenarios, the exact row counts and block length **into the contract** — and that was the right call. The qualifier is **the cost of independent recomputation**. When the answer costs one `wc` or one `grep -c` to reproduce, publishing it saves nothing for a forger (who could run it in ten seconds anyway); what it saves is *disagreement between two honest executors* — the same contract had earlier left "200 characters" undefined between bytes and chars, and the two readings produced 100% opposite truncation results on all four target lines. **Boundary: publish only when the answer is cheap to recompute; when the answer is expensive, publishing it is simply handing over the points.** The cost is concentration — once the cheap answers are published, whatever section still requires real work becomes the sole attack surface, so that section needs its own per-row canary (see `An Aggregate-Only Check Locks the Exit, Not the Entrance`).

### A Positive Existence Anchor Only Protects the Region It Physically Sits In — and Is Inert Inside an Already-Frozen Region - 2026-08-05
- **Context**: A deletion single froze 27 untouched protocol sections by digest, leaving ~11 touched sections mechanically unguarded. To stop those from being gutted wholesale, the contract added "must-survive" anchors — `grep -cF <string> >= 1` for load-bearing prohibitions. An adversarial reviewer gutted one file's entire `## Forbidden` section anyway and passed all six ACs.
- **Discovery**: Two independent defects, both invisible on inspection. (1) **Placement**: the anchors for one skill all happened to sit *outside* the section they were meant to protect (a Stop clause, a different section, and the section *heading* itself) — so emptying the section's body satisfied every one of them. The sibling skill's anchors were inside the body, and its equivalent attack was caught. Same intent, asymmetric placement, opposite outcome. (2) **Redundancy**: one anchor sat inside a section already frozen byte-for-byte by the digest AC, giving it exactly zero discriminative power while looking like coverage. Together they mean an anchor list can be numerically impressive and structurally hollow. Related limit found the same round: a byte-count band (upper *and* lower bound) is defeated by padding — the reviewer gutted the sections then added 180 identical comment lines to land inside the band.
- **Action**: For every must-survive anchor, assert two things before shipping: the string physically resides **inside the unguarded region** it defends (verify with a line-number/section-membership check, not by reading), and that region is **not already covered** by a digest or frozen-set AC. Prefer several anchors per unguarded section drawn from different paragraphs over one anchor per section. Treat byte-count bands as a smell detector, never a content guarantee.
- **failure_mode**: Naive default: pick the most important-sounding prohibitions as anchors and count them ("16 anchors, all present"). Why wrong: placement, not importance, determines coverage — an anchor outside the region survives the region being emptied, and an anchor inside a frozen region was never at risk, so the count overstates protection while the actual attack surface stays open.
- **Grounded in**: HANDOFF-20260805-cut-routing-machinery.md §4 AC2 (v3 anchor set + placement comments), Gate 2 v2 reviewer reports (ALEXFB gutting demo, padding-to-band demo), §8 v2→v3 defect table items 3 and 7

### An Aggregate-Only Check Locks the Exit, Not the Entrance — Pin One Row's Ground Truth, Not Just the Sum - 2026-08-06

- **Context**: A measurement contract required a per-section size table for two protocol files, verified by four sum equalities (each file's per-row bytes/chars totals must equal `wc -c`/`wc -m` of the whole file), plus a manual-rerun AC ("the acceptor re-runs the command block above each table and gets the same values"). The same defect class survived two review rounds. **v1**: a single-row table `__PREAMBLE__ | 20846 | 11145` satisfies all four sums — and the manual-rerun defense *confirms it correct*, because the command supplied (`wc -c < file`) is entirely legitimate. **v2** added a row-count lock (13/21 rows) and a title-column `diff` against `grep -n '^## '`; the reviewer then put the whole-file value in `__PREAMBLE__` and filled the remaining 12 rows with `0` — row count passes, titles pass, sums pass, and pairing it with `for i in {1..12}; do echo 0; done` makes the manual rerun match too. **v3** pinned one row's measured ground truth (`__PREAMBLE__` = 209/119 and 195/135); the degenerate fill collapses on contact while honest work passes at zero extra cost.
- **Discovery**: Aggregate predicates — sums, set-equality, counts — constrain the **exit** of a row set, never its **entrance**. Any allocation of the correct total across a legal number of rows satisfies them, including concentrating all mass in one row and zeroing the rest. Crucially, "a human re-runs the command" is *not* an independent backstop here: the forger supplies the command, so they supply one that genuinely produces their table. The fix is not another aggregate — it is a single per-row ground truth, because every degenerate fill must pile the mass somewhere, and one canary at the structurally-obvious pile point (the first row) is enough. A cheap diagnostic for finding the hole: within one contract, compare how tightly each table is locked. Here AC3 locked its row set (`comm -3` against a fixed manifest), AC5 fixed four rows, AC6 fixed the count to `ls | wc -l` — only AC2 locked no rows. **That asymmetry was the location of the vulnerability, visible without any adversarial thinking.**
- **Action**: For any AC accepting a generated table or list, require at least two of: (1) exact row count, (2) an identity column diffed line-by-line against an independently recomputable source, (3) **a pinned ground-truth value for at least one row**. (1)+(2) without (3) still admits "all mass in one row, zeros elsewhere". Before shipping an AC set, rank the tables by lock strength and treat the weakest as the attack surface. Never count a manual-rerun clause as a lock on values the executor also supplies the command for.
- **failure_mode**: Naive default: treat "the rows sum to an independently measured total" as strong, on the intuition that only real data could add up. Why wrong: a sum is blind to composition — every distribution of the right total over a legal row count satisfies it, and the executor-supplied command makes human replay confirm the forgery rather than expose it. The check reads as arithmetic proof while constraining nothing about where the numbers came from.
- **Grounded in**: LITE-20260805-2259-p4-measurements.md §节切分口径 (canary table: alex 209/119, blake 195/135) + AC2 clauses ①②, Contract Review v1 P0-1

### Replacing a Path-Agnostic Prohibition with a Path-Specific Glob Silently Halves Its Coverage - 2026-08-06

- **Context**: A protocol clause forbade unbounded loading of "other SKILLs **and any references/**" — path-agnostic by construction. Rewriting the clause for auditability, the author replaced it with an emphatic `explicitly excluding .claude/skills/*/references/`, believing this strengthened it. A reviewer checked coverage not by judging whether the glob looked wide enough, but by taking a **fingerprint file of the protected content** (`design-protocol.md`) and scanning the whole repo: **11 copies**, spread across `.claude/skills/`, `.agents/skills/` (a byte-for-byte mirror, 298,111 chars each side), and eight worktree copies under both trees. The new glob covered roughly half of them.
- **Discovery**: `any references/` is a universal quantifier; every concrete path enumeration is a proper subset of it, so *specifying* it can only narrow. The narrowing is invisible on inspection because the new text is longer, bolder, and names a real threat — it reads as reinforcement. The sharpest detail: this happened inside a clause whose own stated purpose was preventing the heavyweight protocol body from being pulled back in, i.e. the author defeated the exact goal they were writing the sentence to serve. Generalizes to any repo with mirrors, worktrees, backups, or cross-platform duplicates: the author enumerates the one location they have in mind.
- **Action**: Before converting "any X" into `concrete/path/*/X`, locate X's actual distribution by scanning for a fingerprint of the protected *content*, never by reasoning about directory names. If ≥2 locations exist, either enumerate all of them or keep the path-agnostic wording. Treat "make this more precise" on a universally-quantified prohibition as a coverage-reducing change requiring the same scrutiny as a deletion. When reviewing such a clause, verify with a content fingerprint sweep — a glob that "looks right" is not evidence.
- **failure_mode**: Naive default: sharpen a vague "any X" into an explicit `path/*/X` so it can be mechanically audited. Why wrong: the vague form was universally quantified and the explicit form is a subset — mirrors, worktree copies and backup trees all escape, while the longer, emphatic new text reads to both author and reviewer as a strengthening rather than a contraction.
- **Grounded in**: HANDOFF-20260806-lite-full-parity.md §2 and §7 (v1→v2 item 2), Gate 2 code-reviewer 2026-08-06 (fingerprint sweep: 11 copies of design-protocol.md, `.claude/` vs `.agents/` 298,111 chars each)

### Before Trusting a Guard, Measure Whether Its Trigger Condition Can Even Occur — and Read the Cited Entry's Mitigation Clause - 2026-08-06

- **Context**: A mirror-sync contract forbade whole-tree rsync and installed an AC to catch it: "`git status --short .agents/` must be exactly 2 lines; more than 2 means other skills were touched." The justification cited a knowledge entry about a wholesale mirror leaking a gitignored private subtree into a public repo. A reviewer, instead of trying to bypass the AC, ran `diff -rq` on the two trees first: **across all 61 skills, only the 2 files this ticket intended to change differed** — every other skill was already byte-identical. So a whole-tree rsync produces exactly 2 changed files too. The guard's trigger condition was unreachable by construction. Worse, the leak it claimed to prevent was also invisible to it: the private directory it would create is itself gitignored at the destination, so it never appears in `git status` either. Zero discriminative power against both stated threats.
- **Discovery**: Two independent failures compounded. (1) **The guard was never measured against the current state** — its premise ("other skills would show up as changes") was an assumption about the repo, testable in one command, never tested. (2) **The cited knowledge entry was read to its headline, not its end** — the same entry recorded that the leak had already been mitigated twice (an ignore rule at the destination, and an `--exclude` flag in the parity tool). Building a guard on an obsolete threat model left the *actual* residual risk — private content materializing in the working tree, where `git status` cannot see it — with no guard at all. The same author had made the identical citation error earlier the same day on a different SAFETY entry, quoting its conclusion while missing its scope qualifier. The replacement guard came from measurement, not reasoning: `diff -rq` yields 3 lines before, must yield exactly 1 after, and 0 under whole-tree rsync — three states, mechanically separable.
- **Action**: For every guard, run its trigger condition against the current repo state *before* shipping it, and record the observed before/after values in the AC — a guard whose "failure" value cannot be produced is decoration. When citing a knowledge entry as justification, read it to the end and check for mitigation/amendment clauses; if the entry documents that the hazard was fixed, the guard must target the *residual* risk, not the historical one. Prefer guards whose pass/fail values you have actually observed (n→m) over guards derived from an argument about what "should" show up.
- **failure_mode**: Naive default: design a guard from a plausible causal story ("whole-tree copy → other files change → git status grows"), cite a knowledge entry whose headline matches the hazard, and ship. Why wrong: the causal story is a hypothesis about repo state that is one command away from being checked, and the cited entry may already record the hazard as closed — so the guard ends up defending a fixed problem in a way that cannot fire, while the live residual risk goes unguarded and the citation makes the whole thing look well-grounded.
- **Grounded in**: LITE-20260806-1000-agents-mirror-parity.md AC4/AC6 + §2 + Contract Review (P0-1, P1-2), Gate 2 code-reviewer 2026-08-06 (`diff -rq` premise test, 61-skill tree), release-sync.md entry's own mitigation clause + .gitignore:16 + .tad/hooks/lib/release-verify.sh:681

### A Guard Added to Repair a Guard Does Not Inherit the Original's Dry Run — Re-run the Same Check on the Fix - 2026-08-06

- **Context**: A review found an AC's存活-anchor set too sparse: 5 anchors clustered such that three whole regions of the target file had zero coverage, and one anchor was a *section heading* — the exact anti-pattern a knowledge entry in the same file already names. The author added 4 well-chosen body-text anchors that precisely covered the three gaps, and marked each new line with a `★ ` prefix inside the fenced block so a reader could see which were new. The next review round measured them: **all four returned 0**. `grep -F` on the literal line (including `★ `) matches nothing. Nine anchors, four dead — and the dead four were exactly the ones added to fix the coverage problem. The original five still worked.
- **Discovery**: The AC's own prose, one line above the block, read: *"anchors must not begin with `-` — `grep -F` treats it as an option, measured in dry run."* The author had recorded that **a character at the start of an anchor destroys the grep**, then added a new character at the start of four anchors. The mechanism is not forgetfulness about the rule; it is that **the dry-run step ran against the original artifact and was never re-run against the repair**. A fix arrives framed as "addressing a finding," which reads as downstream of verification rather than as new material requiring it — so the checklist that would have caught it (run every anchor, observe the count) is not re-applied. Compounding it: the annotation was *inside* the copy-paste region, so the marker meant for humans became payload for the machine.
- **Action**: Treat every artifact produced while fixing a finding as new material subject to the same dry run as the original — for anchors, re-run the full set and observe each count, never just the ones you did not touch. Keep provenance markers (which item is new, which changed) **outside** any block meant to be copied or executed verbatim; put them in surrounding prose that references items by position. When a review round adds N items to a verification set, the next round's first act should be measuring all N+M, not the delta.
- **failure_mode**: Naive default: after a reviewer identifies a defect, apply the fix and re-submit, treating the fix as already-validated because it came from the review. Why wrong: the fix is fresh, unmeasured material — and it lands in the region under the most scrutiny, so a defect there is disproportionately costly; annotating the fix "so reviewers can see what changed" is the specific way this backfires when the annotation sits inside a literal-match region.
- **Grounded in**: LITE-20260806-1200-full-standdown.md AC5 (v2 `★ ` prefix incident, Contract Review P1-A) + the `-` prefix warning one line above it in the same AC, Gate 2 code-reviewer 2026-08-06 incremental round (4/9 anchors measured 0)

#### Amendment — An Extracted Harness Cannot Prove Its Own Extraction Boundary - 2026-08-09

- **Context**: A contract repaired a version-routing guard with two core blocks. AC10 extracted only the `ACTION == "none"` block and behavior-tested it; AC2 checked that a `# ROOT FIX` comment appeared between two line anchors. Four independent review rounds then found two structurally valid counterexamples: moving the `PROBE_OK` decision just outside AC10's extraction range made every existing behavior check pass while restoring a silent downgrade path; deleting the ROOT FIX logic but retaining its comment made AC2 pass while removing the title-level fix entirely.
- **Discovery**: An extraction-based harness can test only what it extracts, so it cannot prove the premise that all relevant logic lies inside its extraction boundary. Likewise, a comment or marker at the correct location proves neither that executable logic exists below it nor that its condition has the intended polarity. These are self-reference failures: the verifier assumes the structural fact that it is supposed to establish.
- **Action**: Pair every extracted behavior harness with a whole-artifact structural invariant that proves all relevant reads/branches fall inside the extraction boundary. For every title-level decision block, add a behavior test that distinguishes at least the intended condition, its inverse, and the missing-logic case; never accept comment position as a proxy for executable behavior. Run at least one adversarial fixture outside the extraction boundary and one fixture with the marker retained but the body removed.
- **failure_mode**: Naive default: extract the expected block, get all behavioral cases green, and separately assert that a named comment is in the right interval. Why wrong: code moved outside the extraction range is invisible to the harness, while a surviving comment lets deleted or inverted core logic pass the positional check; both failures produce a fully green suite around an absent safety property.
- **Grounded in**: `.tad/archive/handoffs/LITE-20260807-1050-tadsh-version-authority.md` Contract Review P0-4/P0-5 + AC11/AC12; `.tad/evidence/acceptance-tests/tadsh-version-authority/ac-results.md`; `.tad/evidence/reviews/blake/tadsh-version-authority/code-reviewer.md`

### Count-Based Criteria for Partial Replacements Must Be Computed on the Residue, Not the Replacement - 2026-08-06

- **Context**: A release-prep contract specified several block replacements and pinned each file's `git diff --numstat` as a shape assertion. Three of those numbers were wrong, all in the same direction, all discovered by measurement rather than reasoning. **(1)** A README block was pinned at `23 12`, computed as "delete the 6-line old block, insert the 15-line new block" — but the new block *deliberately retained the old block's last four lines* (a reserved-channel table kept on purpose, itself the fix for an earlier finding). Any minimal-edit diff matches those four lines as context, so the real shape is `18 7`. **(2)** A parallel guide block shared one blank line between old and new; pinned `9 7`, actual `8 6`. **(3)** A "this string must not appear" anchor was pinned at `== 0` while the same contract, two pages earlier, mandated preserving one historical occurrence of that exact string — so the correct value was `== 1`. All three were re-derived correctly only after a reviewer ran the actual diff.
- **Discovery**: The shared error is computing a count from **the replacement operation** ("I remove N lines and add M") instead of from **the resulting file** ("after this edit, K of these lines remain"). The two diverge exactly when the edit is partial — and partial is the normal case, because *the act of fixing a previous finding is what leaves old content in place*. Both README cases trace directly to earlier fixes: the retained table was the remedy for a contradiction, the retained heading was an explicit "do not touch history" clause. So the more carefully a contract handles prior findings, the more its count-based criteria drift from the replacement arithmetic. A wrong pinned value is not merely a false FAIL: an honest executor doing correct work hits it and must choose between stopping and *padding the file to reach the number* — the same padding attack recorded under `A Positive Existence Anchor…`. **A miscomputed constant actively recruits forgery.**
- **Action**: Derive every count-based criterion (numstat, occurrence counts, line totals) by **constructing the post-edit artifact and measuring it**, never by summing the edit operations. Pair each diff-based number with an implementation-independent cross-check (`wc -l`, total occurrence count) and declare the latter primary — diff algorithms legitimately vary by ±1–2 on shared lines, so the contract must say which wins. For "must not appear" anchors, first grep the current file and subtract whatever the contract itself instructs you to preserve.
- **failure_mode**: Naive default: compute the pinned shape from the edit you are describing — old block is 6 lines, new block is 15, therefore `+15 −6`. Why wrong: minimal-edit diffs match common lines as context, and partial replacements are exactly where fixes-for-prior-findings live, so the arithmetic and the artifact disagree precisely in the cases you were most careful about — and the executor who did it right is the one who gets failed.
- **Grounded in**: LITE-20260806-1600-release-doc-sync.md AC8 (v3 values `23 12`→`18 7`, `9 7`→`8 6`; banner anchor `== 0`→`== 1`) + Contract Review P0-6/P0-7, Gate 4 recompute 2026-08-06 (tad-help pinned `7 9`, actual `7 10`, `wc -l` 232→229 self-consistent — the same error recurring one round after it was named) and v2 NEW-P1-C, .tad/evidence/audits/P4-measurements.md A-section delivered tables

### A Fixture Matrix Whose Rows Carry Their Own `expected_result` Is Self-Certifying — a Schema + Digest Gate Proves Integrity, Not Correctness - 2026-08-10

- **Context**: Phase 3b built a 30-case authority fixture matrix (`authority-fixtures.jsonl`) where every row declares both the scenario and the outcome it expects, e.g. `{"id":"unlisted-consequence", ..., "expected_result":"BOUNDARY_CHANGE before mutation","human_prompt":true,"runtime_prompt_reason":"consequence_change"}`. AC7's verifier checked (a) the JSON schema, (b) row cardinality = 30, and (c) a SHA over the file. All three passed. Gate 4 independent code review then reproduced a false PASS: change a row's `expected_result` from `BOUNDARY_CHANGE` to `ALLOW`, recompute the digest, and every check still passes — the gate had been validating that nobody *edited* the file, while the thing under test was whether the *outcomes were right*.
- **Discovery**: When the fixture supplies both the input and the expected output, the verifier has no independent reference to compare against, so schema validity and digest integrity are the only properties it can actually assert — and neither is the property the AC claims. The digest makes this worse than it looks: it reads like a tamper-proof seal, so a reviewer scanning the gate sees "integrity verified" and stops. The failure is structurally identical to marking your own exam and stapling a hash of the answer sheet to it. The repair is not a stronger digest but a **second, independently authored copy of the expected outcomes** — here a 30-row literal heredoc table inside the verifier, byte-compared against the normalized fixture. That table must be derived from the *spec* (the design contract's outcome list), not transcribed from the fixture file, or the second copy is just the first copy again.
- **Action**: For any fixture/golden-file gate, ask "if I flipped one expected value and re-sealed the file, would this gate fail?" If no, the gate tests integrity only. Add an expectation source that does not come from the artifact under test — a literal table in the verifier, a separately-maintained golden file, or a re-derivation from the spec — and compare byte-for-byte. Then prove the repair with your own mutation probes rather than the implementer's: at minimum flip an outcome value, inject an unknown key, misplace a legal-but-conditional optional key, and invert a boolean; all four must be rejected. Keep the closed-world schema (exact key-set equality, typed fields, optional keys legal only on named rows) — it is what makes the byte-compare meaningful.
- **failure_mode**: Naive default: guard a fixture matrix with a strict schema check plus a content digest and call the fixture set verified. Why wrong: both properties are about the file's *form and integrity*, and a self-declaring fixture's *semantics* are unconstrained by either — so an internally consistent but wrong expectation passes every check, and the digest's tamper-proof appearance actively discourages the reviewer from looking for the missing oracle.
- **Grounded in**: `HANDOFF-20260810-lite-authority-model-v2.md` §7 AC7 + §6 fixture matrix; `.tad/evidence/acceptance-tests/lite-authority-model-v2/verify-authority-model-v2.sh` (`write_expected_outcome_oracle` heredoc L124–L156, `normalize_fixture_semantics`, `fixture_semantics_valid`); Gate 4 rerun code review that reproduced the digest false-PASS (`.tad/evidence/reviews/alex/lite-authority-model-v2/gate4-rerun-code-review.md`); Alex Gate 4 repair-2 acceptance — four independent Alex-authored probes (outcome flip / unknown key / misplaced `control` / `human_prompt` inversion) all rejected 4/4

### A Digest-Based Scope Fence Is Unsatisfiable in a Repo Whose Own Hooks Write During Execution — Exclude the Self-Written Telemetry Paths in the Contract, Not in the Fix - 2026-08-10

- **Context**: A contract's scope fence was designed in two halves: `comm -13` against a pre-implementation `git status --porcelain -uall` snapshot to catch newly-created out-of-scope paths, plus a per-path SHA-256 digest comparison to catch *modifications* to the ~870 paths already dirty before the work started (the second half existed precisely because `comm -13` reports additions only, so an already-dirty path could be edited invisibly). The digest half shipped with a separate bug that made it dead code. When that bug was fixed, the reviewer noted the half would then **false-FAIL on correct work**: `.tad/evidence/traces/*.jsonl` had accumulated seven `evidence_created` rows, appended by the framework's own hooks as a side effect of the implementation creating its evidence files.
- **Discovery**: In a repo where the agent framework writes telemetry, journals, or state as a side effect of the very actions under verification, any fence that asserts "nothing outside the file list changed" is **unsatisfiable as written** — the act of producing evidence mutates paths the contract never listed. Two failure shapes follow, and the second is worse: the contract is unsatisfiable, and an implementation bug can *mask* that by making the assertion vacuous, so the design defect ships behind a green run and only surfaces when someone repairs the bug. The exclusion is a **contract-design obligation**, not something to patch into the verifier during implementation: the contract author is the one who knows which paths are framework-written versus agent-written, and burying the carve-out in `verify.sh` converts a stated scope boundary into an undocumented one.
- **Action**: When writing a scope fence for a repo with active hooks, enumerate the self-written paths (telemetry/traces, journals, session state, lock files, index caches) in the contract's own exclusion list before the fence is authored, and state why each is excluded. Then prove the fence is not vacuous with a mutation probe on a path that *is* in scope — a fence that has never failed on a deliberately corrupted input has not been shown to work. Prefer a digest snapshot taken in the same pre-edit action that records the base SHA, so both halves share one provably-before-any-edit baseline.
- **failure_mode**: Naive default: assert "every dirty path outside the file list has an unchanged digest" and treat a green result as proof of containment. Why wrong: the framework appends to its own trace/journal files while the work runs, so the honest answer is FAIL on correct work; the contract is impossible to satisfy, and if the verifier happens to be buggy the impossibility stays hidden behind a PASS that proves nothing.
- **Grounded in**: `.tad/active/handoffs/LITE-20260810-1820-layer1-ac-driven-compat.md` AC14 (both halves + the `scope-baseline.txt` design); `.tad/evidence/reviews/blake/layer1-ac-driven-compat/code-reviewer.md` P0-1 item 3 (traces baseline `1500f1e9`→`b25661c6`, 7 self-written rows); the same single's Reflexion 修复 3, where appending the raw journal itself tripped the fence and had to be deferred until after archive

### A New Guard's Criteria Must Be (1) Read Verbatim From the Implementation, (2) Dry-Run Against Every Shape the Real Data Takes, and (3) Composed Only of Inputs the Executor Can Obtain Independently — Missing Any One Makes the Guard the Next Round's Defect - 2026-08-11

- **Context**: A diagnostic contract (v2.41.0 install smoke test) went through 6 revisions and 3 independent incremental reviews. Every round after the first introduced its new defect **inside the fix for the previous round's defect**, and three of those were the same shape: a freshly-authored guard whose *criteria* had never been validated against the thing it guards. Instance 1 — a scope-fence carve-out whitelisted `dormancy|dormant|last_queried|status`, assembled from what the field names *sounded like* they'd mean; reading `lib/notebook-lifecycle.sh` showed the hook's only write is `yq -i '(… | .status) = env(STATUS)'` with values `{active, dormant}`, and `last_queried` is **read-only** — i.e. the whitelist admitted precisely the field a non-hook writer would touch, opening the guard on the door it existed to lock. Instance 2 — the corrected line-level regex `^[<>][[:space:]]*status:[[:space:]]*(active|dormant)[[:space:]]*$` was dry-run against a hand-made 2-space, comment-free fixture and passed both controls; the real `REGISTRY.yaml` carries `    status: dormant # active / dormant / archived` at one entry, which the anchored regex matches **0** times, so that entry flipping would have false-FAILed. Instance 3 — an assertion "changed-line count == 2 × number of status flips" was unsatisfiable in principle: the flip count is decided at runtime by `now - last_queried > threshold`, so obtaining it means reimplementing the logic under test.
- **Discovery**: These are three failures of one thing — the guard's *judgement criteria*, as distinct from its mechanism. A guard has a mechanism (digest diff, regex, count) and a criterion (which changes are legitimate). Review attention naturally lands on the mechanism, because that is what the code shows; the criterion is a claim about the guarded system's behaviour, and a claim is exactly what a reader cannot see. Each criterion therefore needs its own evidence, of three separable kinds: **provenance** — every allowed value/field/path traced to the line of implementation that produces it, never to the plausibility of its name; **shape coverage** — the dry run performed against the *live* artifact's full distribution of forms, since a synthetic fixture inherits the author's assumptions about formatting and a guard that only meets tidy data is untested against the data it will meet; **input obtainability** — every quantity the criterion consumes derivable by the executor without reimplementing the system under test, or the assertion is either silently skipped or filled with a guessed number that becomes false evidence. The structural reason this recurs is that a repair is written under the belief that the analysis is already done — the previous round *just* established what the defect was — so the repair inherits the confidence of the diagnosis without inheriting its verification.
- **Action**: Before a newly-written or newly-widened guard is accepted, produce three artifacts alongside it. (a) For each admitted value, field, or path, cite the implementation line that writes it; delete any entry with no citation — a field name that merely sounds related is a defect, not a criterion. (b) Run the guard's positive and negative controls against the **real** artifact, having first enumerated that artifact's shape distribution (`grep -o '<field>:.*' … | sort | uniq -c` for text; the equivalent inventory for structured data) — and when shapes vary in ways a text matcher cannot absorb (inline comments, quoting, indentation), move the judgement to a structural projection (`yq -o=props`, canonical serialisation) rather than hardening the regex. (c) For every number the criterion compares against, name where the executor gets it; if the answer is "recompute what we're testing", cut the assertion and replace it with a reporting obligation that routes the judgement to a human. Apply the same three to the *repair* of a guard, not only to its first draft.
- **failure_mode**: Naive default: having identified a defect precisely, write the fix and dry-run the fix's mechanism on a small hand-made fixture. Why wrong: the mechanism is not what is unverified — the criteria are. The whitelist admits a field nobody checked is written, the matcher meets a real-data shape the fixture never contained, and one input turns out to be underivable — so the guard ships green, and its next reviewer finds a defect that did not exist before the repair.
- **Grounded in**: `.tad/archive/handoffs/LITE-20260811-1951-install-smoke-v2410.md` §Contract Review (增量复核 2 P1-R1, 增量复核 3 唯一放行条件) + L2.25 rows rev4-a/rev4-b/rev5-a/rev5-c; `.tad/hooks/lib/notebook-lifecycle.sh` (`recompute_notebook_dormancy` — sole write, `{active,dormant}` domain, read-only `last_queried`); `.tad/research-notebooks/REGISTRY.yaml:127` (the trailing-comment `status` line, measured 0 matches against the anchored regex)

### Two Individually-Correct Fixes Can Compose Into a New Defect — Dry-Run the Revised Whole, Not the Changed Line - 2026-08-11

- **Context**: Same 6-revision contract. Review round 1 raised two independent items: P0-3 (a clone step was ordered after the upgrade it was supposed to precede, so the clone started from an already-upgraded tree) and P1-7 (the preflight did not create every working directory it referenced, so a `tar -x -C "$WORK/REF"` would fail). Revision 2 adopted both, each correctly on its own terms: preflight became `mkdir -p "$WORK/A" "$WORK/B" "$WORK/U" "$WORK/U2" "$WORK/REF"`, and the clone became `cp -R "$WORK/U"` → `"$WORK/U2"`. BSD `cp -R src dst` copies *into* `dst` when `dst` already exists as a directory, so together they produced `U2/U/…` instead of populating `U2` — reproduced on the host: `U2/marker.txt` absent, `U2/U/marker.txt` present. The downstream consequence was worse than the original bug: with no `.tad/` at `U2`'s root, the curl variant would run a **fresh install** rather than an upgrade; a fresh install does not print `Nothing to do`, which was the exact string the round-1 fix had added as a hard ban — so the new failure walked straight through the guard built for the old one, and the version assertion passed anyway.
- **Discovery**: A review returns findings one at a time, and each fix is validated against the finding that motivated it. Nothing in that loop examines the **interaction surface** between fixes landing in the same revision, yet fixes cluster by construction — they touch the same objects, because that is where the defect was. The composition hazard concentrates wherever a fix changes a *precondition* another fix's command depends on (here: whether a directory pre-exists, which silently reverses `cp -R`'s meaning). Worse, a targeted guard added in round N is calibrated to round N's failure signature; a round N+1 failure reaching the same endpoint by a different route is invisible to it. That is why the residual check must be an assertion about the **intended end state** (`test -d "$WORK/U2/.tad"`, and a dedicated `test ! -e "$WORK/U2/U"` probe for the nesting shape itself), not another string ban on the symptom last seen.
- **Action**: When one revision lands two or more fixes, list the objects each touches and dry-run every pair that shares an object — in a throwaway directory, with the actual commands in the actual order. For any command whose semantics depend on whether its target pre-exists (`cp -R`, `mv`, `ln -s`, `tar -x`), state that precondition explicitly in the contract and assert it at runtime. When a guard exists for a known symptom, add a positive assertion on the intended end state next to it, so a differently-routed failure cannot pass by simply not producing the banned string. Prefer copy forms that are precondition-independent (`cp -R "$src/." "$dst/"`) over ones that switch behaviour silently.
- **failure_mode**: Naive default: adopt each review finding's fix, verify each against its own finding, and ship the revision. Why wrong: the fixes share objects precisely because they address the same region, and one fix can invert another's semantics through a precondition neither finding mentioned — reproducing the original failure by a new route that the original fix's symptom-shaped guard cannot see.
- **Grounded in**: `.tad/archive/handoffs/LITE-20260811-1951-install-smoke-v2410.md` §Contract Review (增量复核 1 P0-N1) + AC12b-pre 的三条断言 + L2.25 row rev3-a (host reproduction of the `cp -R` nesting); `tad.sh:1584-1590` (the `Nothing to do` early-exit the symptom-ban targeted)

### To Test Whether a Migrated Protocol Landed as a Usable Procedure, Hand a Fresh Reviewer That Artifact and Nothing Else — With a Contract-Fixed Prompt and a Generalization Probe - 2026-08-12

- **Context**: Phase 4 of the full-retirement Epic migrated a dependency protocol out of a large agent body into a standalone capability skill, and dogfooded it with a real upgrade in the same单. The first contract review found the fatal shape: every acceptance criterion spelled its commands out verbatim in the contract, so the executor could complete the entire dogfood by reading the contract — the new skill could remain a file nobody ever opened and all criteria would still pass. The remedy added was a replay: spawn a reviewer with a fresh context, give it **only** the new artifact, and ask it to reconstruct the procedure. On the live run it fired — one question's answer was traced to explanatory prose rather than to a step you could follow, judged FAIL, and only passed after the formula and its threshold values were inlined into the operational step.
- **Discovery**: A migration produces two things that look identical from outside and are not: the information having been *transcribed*, and the artifact having become *usable on its own*. Ordinary criteria — file exists, section present, string matches, byte-parity with a mirror — cannot separate them, because a verbatim copy satisfies every one. Only forcing reconstruction from the artifact alone can, and three details decide whether the replay measures anything. **The prompt must be fixed by the contract, not written by the executor**: an author who knows the questions can encode the answers into them and the reviewer will still be "answering only from the artifact". **At least one question must be a generalization** — one whose answer is not sitting anywhere in the source material, so a copy-plus-FAQ appendix fails it while a real procedure does not; retrieval questions alone are all satisfiable by an appended cheat-sheet. **The reviewer must classify where each answer came from** — operational step versus explanatory aside — because information can be present and still not be followable, which is exactly the distinction the whole exercise exists to draw, and in practice this is the criterion that fires. The replay also has to run *before* any irreversible step the migration is supposed to justify, or it reports on a cost already spent.
- **Action**: When a protocol moves into a new artifact, add one acceptance step: a fresh-context reviewer receives that artifact and nothing else, asked a prompt written into the contract verbatim (assert the executor's copy diffs empty against it). Include retrieval questions, at least one generalization probe with its grading standard fixed in the contract, and a requirement to cite each answer's section and label it operational-or-explanatory; require the load-bearing answers to land in operational sections. Order this step before the migration's irreversible actions, and bind failure to invalidate the coverage claim, not merely to log a note. A different model or harness for the reviewer strengthens the independence.
- **failure_mode**: Naive default: verify a migration with structural criteria — the file exists, the required sections and constraints are present, the mirror matches byte-for-byte — and treat all-green as evidence the capability transferred. Why wrong: a verbatim copy of the source passes every one of those, so the artifact can be one nobody ever opens while the work is completed from the contract that describes it; nothing in the criteria distinguishes transcription from a procedure, and the migration ships as a document plus an unrelated side effect.
- **Grounded in**: `.tad/archive/handoffs/LITE-20260811-2254-dependency-ops-skill.md` AC17 (contract-fixed prompt Q1–Q6, Q5 generalization probe with grading standard, clause (iii) provenance classification, `skill-load-bearing-gate` ordered before `upgrade-gh`) + its Contract Review 首轮 P0-3 and 增量复核 1 的 P1-NEW-3/4/5; `.tad/evidence/acceptance-tests/dependency-ops-skill/AC17.txt` (Q3 首轮出处=说明性文字 → FAIL → 内联操作指令 → 转正；reviewer 为 opencode/deepseek-v4-flash，跨 harness)

### Repairing a Loud Failure Must Not Replace It With a Silent Success - 2026-08-12

- **Context**: Same contract. A review found that the very first command of the run wrote a file into a directory that did not exist yet — a bare redirect, `exit 1`, the whole date-derivation chain dead on line one. The repair added `mkdir -p`. The next review round pointed out what that repair had cost: the contract elsewhere states that the shell tool's working directory is not guaranteed, and had absolutized a script path for exactly that reason — but the new chain used relative paths throughout. With `mkdir -p` in front, a wrong working directory no longer errors; the directory is created wherever the process happens to be, the file lands there, `exit 0`, and every later assertion reads that same misplaced file in perfect self-consistency until someone notices the real evidence directory is empty. Reproduced on the host before the fix was accepted.
- **Discovery**: The bug was found *because* the failure was loud — a bare redirect into a missing directory is impossible to miss. The repair removed that loudness, and it removed it unconditionally: `mkdir -p` cannot distinguish "the directory legitimately doesn't exist yet" from "you are standing in the wrong place", and it silences both. So the repair for one failure mode deleted the only signal that would have surfaced its neighbour. This is a general hazard of the idempotence and tolerance switches — `mkdir -p`, `|| true`, `2>/dev/null`, `rm -f`, `cp -f`, `touch` — every one is added precisely to stop a command from complaining, and what it stops complaining about is broader than the case that motivated it. The fix is not to avoid them but to pair each with an assertion of the precondition it just stopped enforcing: `mkdir -p` with a check that the root is the intended one, `|| true` with a check on the state the command was supposed to produce, `2>/dev/null` with a check that the output is non-empty.
- **Action**: Whenever a repair makes a command stop failing, ask what that command's failure was previously proving, and restore that proof explicitly. Concretely: before creating paths, resolve and assert the base (`ROOT="$(git rev-parse --show-toplevel)"` plus an equality test) rather than trusting the ambient directory; after any suppressed-error command, assert the state it should have produced. Treat "the command no longer errors" as a change in observability, not only in behaviour, and state in the contract which precondition the suppression gave up.
- **failure_mode**: Naive default: a command failed because a precondition was missing, so add the flag that makes it tolerate the missing precondition — `mkdir -p`, `|| true`, `rm -f` — and confirm the fix by seeing exit 0. Why wrong: the flag tolerates a whole class of missing preconditions, not just the intended one, so a genuinely wrong state now produces the same exit 0 as a correct one; the run proceeds self-consistently against the wrong target and the defect surfaces only at the end, or not at all.
- **Grounded in**: `.tad/archive/handoffs/LITE-20260811-2254-dependency-ops-skill.md` 实现约束第一条（`git rev-parse --show-toplevel` + 等值断言 + `mkdir -p` + `test -s` 四段链）+ Contract Review 增量复核 1 的 P0-NEW-4 与增量复核 2 的 P1-R3-1（错误 cwd 下 `exit 0` 的实测复现；修订后错误 cwd `exit 128` / 正确 cwd `exit 0`）

### "Is This Right?" and "What Can't This Hold?" Are Different Questions — a Correctness Review Cannot See a Structure's Ceiling - 2026-08-12

- **Context**: An Epic to separate process discipline from process weight needed a foundation artifact: a table of every discipline with cost, evidence, and a keep/retire/dial judgment. Its predecessor Epic had already failed at exactly this layer — a 19-row capability table that structurally could not represent discipline, so the disciplines that mattered were never rows at all and one was retired as "fixed startup overhead", costing 28 days of unnoticed security advisories. Across the redesign the same shape defect surfaced three more times (a mechanism table that could not hold the mechanisms' *distribution* in time; a "what did lite lose from full" frame that could not hold "is what lite added worth keeping"), and every one was caught by the human, never by the author or by review. So the contract added one acceptance step whose entire content was a differently-framed question: a fresh-context reviewer, given the finished artifact, asked **"what can't this table's shape hold?"** — explicitly forbidden from answering whether the table was correct. Two Gate-2 experts had already reviewed the design and passed it; the artifact then passed all sixteen mechanical checks.
- **Discovery**: The blindspot reviewer returned ten classes of missing information and named the structural cause in one line: the table had a column for cost and none for benefit, a column for the verdict and none for the reasoning, and rows with no way to express relations between rows — so all three were being shoved into one optional free-text column that consequently held three unrelated semantics. The artifact was a valid inventory and could not support the decision it had been built for. What makes this reusable is not the finding but the asymmetry that produced it: a correctness review takes the structure as given and checks the contents against it, so a container's ceiling is invisible from inside — every cell can be right while the schema cannot express the question. The two framings are not degrees of rigor but different objects of attention, and no amount of adversarial pressure on "is this right" converges on "what can this not say". The reviewer must be denied the correctness question outright; left available, it is the easier one and it is where the effort goes.
- **Action**: For any artifact whose shape is itself a design decision — a table, schema, taxonomy, inventory, checklist — add one acceptance step that hands the finished artifact to a fresh context and asks only what its shape cannot hold, with the correctness question explicitly forbidden in the prompt and the prompt text fixed by the contract so the executor cannot soften it. Run it before anything irreversible depends on the artifact. Treat its output as design input, not as defects to fix: the honest disposition is often "this is a valid X but cannot answer Y", which reshapes the next step rather than patching this one. A different model or harness sharpens it further, since shape blindness tends to be shared by whoever built the frame.
- **failure_mode**: Naive default: review the artifact hard for correctness — are the entries right, are the criteria sound, do the checks run — and treat passing as evidence the artifact is fit for its purpose. Why wrong: correctness review operates inside the given structure and therefore cannot report what the structure excludes; the artifact ships with every cell verified and still unable to express the decision it exists to support, and the gap surfaces only when someone tries to use it.
- **Grounded in**: `.tad/active/handoffs/HANDOFF-20260812-discipline-inventory.md` AC7（题面由契约钉死，含"装不下"、禁"对不对"，排在不可逆动作之前）；`.tad/evidence/designs/discipline-inventory/shape-blindspot-review.md`（十类缺口 + "有成本无收益、有判定无论证、有行无行间关系"的根因诊断，reviewer 为 opencode/deepseek-v4-pro，跨 harness）；`EPIC-20260809-full-capability-extraction-retirement.md`「Epic 终止记录」（同一形状缺陷的前三次，全部由人发现）

### "Run X" Is Not a Criterion Until X's Failure Signal Is Defined — a Print-Only Verifier Gives Every AC a Green With No Red - 2026-08-12
- **Context**: Phase 1b 契约把 6 条 AC 的判据写成「跑 `verify.py <sub>`」。该脚本既有的 9 个子命令**全部 print 完就 return**，全文唯一一处 `SystemExit(2)` 只在整列缺失时触发——实测九条全部 `exit=0`，包括 `carriers` 打印 `bad=1` 时。新子命令若照抄这个房规，报 `mismatch=15` 也是 `exit 0`。
- **Discovery**: 这**不是"永真"，是根本没有"红"这个状态**。永真的 AC 至少还有一个可以变红的理论路径；一个只 print 不 exit 的验证器，让「通过/不通过」这个二元判断**在机器层面不存在**，只剩执行者口头声称"哪个数字算红"。审查逐条实跑后的结论：12 条验证命令里 **11 条最终没有红态**。更隐蔽的是，它会连带毁掉建在其上的**负控**——负控的判据是"注入后变红"，而红不存在，于是负控退化成自述。**承重防线和它要防的东西一起塌**。
- **Action**: 任何以"跑某脚本"为判据的 AC，契约必须**先立退出码契约**：零违规 → 末行 `RESULT=PASS` + exit 0；有违规 → 逐条打印 + 末行 `RESULT=FAIL` + `SystemExit(1)`；标的物缺失 → `ERROR` + exit 2（不得回落成 PASS）。并把「变红 = `exit≠0` 且末行 FAIL」写成契约里的**唯一定义**。写 AC 前**读一遍被调用脚本的退出码行为**——一分钟的事。
- **failure_mode**: Naive default: 把「跑 verify.py severity」当成一条判据写进 AC，默认脚本会用退出码表达成败。Why wrong: 该脚本的既有风格是 print-only，"失败"没有机器可读的信号，于是这条 AC 以及所有以它为基础的负控**永远只有绿**——而全绿会被读成"验证充分"。
- **Grounded in**: HANDOFF-20260812-discipline-inventory-columns.md §8.0 退出码契约 + §12 rev2→rev3 修复 #1，Gate 2 code-reviewer 实测九子命令 exit=0

### A Fix Is a Defect Source — Budget a Gate for Fix-Induced Defects, Not Only for Re-Checking the Original - 2026-08-12
- **Context**: 同一 session 内三次观察到同一形状：Phase 4 三轮审查 9 个 P0 中 **5 个是修复自己造的**；Phase 1b rev2→rev3 修 AC9 时把扫描对象挪到完成报告，**造出"文件不存在也返回 0 = 通过"**；rev3→rev4 时为覆盖新列要求独立复核成本量级，**同时又禁止复核者读该列所在目录**——自造互斥，Blake 怎么做都错。
- **Discovery**: 修复引入缺陷的比率高到不能当噪声处理。机制是**修复只在局部正确**：AC9 的新扫描对象是对的、`test -s` 守卫是对的，但两者组合后守卫恰好缺席；覆盖要求是对的、隔离要求是对的，但两条约束的作用域重叠处没人检查。**审查发现的是原始缺陷，门禁必须发现修复缺陷**——它们不是同一批。本单实测：闭集门禁抓到 4 个（`$TMP` 未定义 / V7 静默通过 / `nine-col` 对照物未定 / 编造的行数），但那两个修复自造的互斥是 reviewer 抓的，门禁没抓到，因为门禁只查了"原 P0 修没修"。
- **Action**: 修复门禁的判据集里必须**独立列一条**：「本轮每个修复，是否与其他条款产生新的作用域重叠或互斥？」逐条对新增文本做一次交叉检查，而不只是核对原 P0 是否闭合。⚠️ 这条不能靠"再审一轮"解决——重审会一直找新问题不会停（用户 2026-08-12 裁定）；要的是**闭集判据**，查完就完。
- **failure_mode**: Naive default: 修完 P0 后只验证"原来那条问题没了"。Why wrong: 修复文本本身是新写的、未经审查的内容，其缺陷密度与初稿相当；只回归原缺陷会让修复引入的缺陷 100% 逃逸。
- **Grounded in**: COMPLETION-20260812-discipline-inventory-columns，HANDOFF-20260812-discipline-inventory-columns.md §12 两轮修复清单

### A Grep-for-Forbidden-Word Checker Collides With the Instruction That Must Name That Word - 2026-08-12
- **Context**: AC8 要求形状审查的 prompt 段内 `对不对` / `是否正确` 出现 **0** 次（防止把"这个形状装不下什么"跑偏成正确性审查）。而契约 §7 钉死的题面本身写着「**禁止回答"填得对不对"**」——**下禁令必须点名禁词**，于是忠实照抄题面必然让 AC8 变红。同一形状还有一例：AC6 要求证伪条件"无空格"，而要求逐字照抄的 4 句源句本就含 ASCII 空格——**两条约束互斥，两边都过不去**。
- **Discovery**: 当检查器用"字符串不出现"表达"不许做某事"时，它无法区分**做那件事**与**禁止做那件事**——两者都包含该字符串。这类互斥在契约里不显眼，因为两条条款通常分处不同小节（一条在题面、一条在 AC 表），写的时候不会同屏。
- **Action**: 用「不许出现字符串 S」当判据前，先自问：**本契约里有没有任何一处必须点名 S？**（禁令、撤回说明、修复记录都会点名。）若有，改判据形态：查**提问模式**而非裸词（如 `是否.*正确\?`），或把禁令改写成不含 S 的等价表述（如「禁止做正确性审查」），并在契约里注明为何这样绕。
- **failure_mode**: Naive default: 想禁止某种回答，就写"该词出现次数必须为 0"。Why wrong: 表达该禁令的文本自己会命中该词，检查器把守卫误判成违规——照字面执行则恒红、绕过则规则失效。
- **Grounded in**: HANDOFF-20260812-discipline-inventory-columns.md §7 题面 / AC8 / AC6，Blake Gate 3 §13 裁定请求 1

### A Replay Test Reproduces the *Moment*, Not the *Utterance* — a Fixture Built From the Correction Tests a Different Scenario Than the Accident - 2026-08-12
- **Context**: Phase 2 造了一道「Epic Objective 闸」防 2026-08-12 的真实事故（把用户的假设当批准，推进 6 个 phase）。回放 fixture 的设计是：把用户那句原话 + **一份已存在的 Epic 摘要**（"模拟上下文动量"）喂给 fresh subagent。结果 subagent 判**不触发**，Blake 据此报告"闸覆盖不到事故"。
- **Discovery**: fixture 错了，闸没错。核对时间线：Epic **创建于 08-09**（那句话正是它的"被误读的前提（根因）"），Objective 于 **08-12 被重述**——**两个时刻都在闸的触发条件内**。而 fixture 给的"已存在的 Epic"把场景设成了**单中途延续**，那是另一个已登记的缺口。更普遍地：事故留下的**文字记录**通常是**纠正时刻**的话（"我只是做了一个假设"是用户回头指出误读时说的），而干预要拦的是**更早那个决策时刻**。**照抄记录里的语句造 fixture，测的是纠正场景，不是事故场景。**
- **Action**: 造回放 fixture 前先画时间线，明确回答三件事：(a) 干预要拦的是**哪一个时刻**？(b) 该时刻的**上下文状态**是什么（有无 Epic / 有无契约 / 累积了多少推进）？(c) 手上这句话是**该时刻说的**，还是**事后纠正时说的**？三者任一错位，回放的结论就不可归因。⚠️ 回放判"不触发"时，**先怀疑 fixture，再怀疑机制**。
- **failure_mode**: Naive default: 从事故记录里摘出用户原话，配上"当时的上下文"，喂给 agent 看干预会不会触发。Why wrong: 记录里的原话多半是**纠正时刻**的，而配的上下文又常按"事故已经发生"来构造——两者合起来模拟的是事故**之后**的场景，于是必然得出"干预不触发"，把正确的机制误判成无效。
- **Grounded in**: HANDOFF-20260812-lite-goal-gate.md §6.2 fixture 设计缺陷，EPIC-20260809-full-capability-extraction-retirement.md「二、被误读的前提（根因）」时间线

### A Negative Control Keyed on a Marker the Design Itself Invented Is Trivially True - 2026-08-12
- **Context**: rev1 的负控 A：把用户原话喂给**未修改**的规则文本，要求 agent **不得**产出 `## 目标选择` 段。该标题与 `[无工作项]` 标记都是本次设计**新造的**，实测在 T=0 文本中**各 0 次命中**。
- **Discovery**: 负控通过率≈1，**与"改动是否真的改变了行为"完全无关**——任何没读过新规则的 agent 都不可能自发吐出一个刚被发明的标题。而它声称排除的混淆恰恰是"agent 本来就会这么做"：实测后台探针把同一句话喂给未修改规则的 fresh agent，**两次都主动列出了目标选项并拒绝代选**。**要排除的东西被判据放过了。** 一般形式：**当负控的判据是"是否出现处理组特有的形式标记"时，它测的是格式新颖度，不是行为差异。**
- **Action**: 负控的判据必须**格式无关**——用语义指标（互斥读法的**个数**、是否出现"不产生工作项"这类**内容**、是否把某目标当既定前提直接进方案），并由**不知条件归属的第三方**盲判。另加一个 **placebo 条件**（等长但无关的新增文本）控制"文本变长"本身。若买不起这套设计（agent 调用成本），**明写"因果未证"，不要用一个恒真的负控假装证过**。
- **failure_mode**: Naive default: 负控 = "旧版本不应产出新版本要求的那个东西"。Why wrong: "那个东西"通常是新版本发明的格式，旧版本不产出它是**定义使然**而非行为差异，于是负控恒过，实验对因果零证明力，却读起来像做过对照。
- **Grounded in**: HANDOFF-20260812-lite-goal-gate.md rev1 §6.2 / rev2 §6.2 撤回段，Gate 2 code-reviewer P0-3

### A Verification Anchor That Stores the Document and a Document That Stores the Anchor Is a Fixed Point — Exclude the Self-Referential Line - 2026-08-12
- **Context**: 契约 §0.1 要写入 T=0 的 commit hash；而 T=0 的 commit 又必须**包含该契约**（否则契约不在 git 里、AC 的"契约未被改"没有外部锚）。回填 hash 后，`cmp <(git show $T0:契约) 契约` 立即失败。再提交一次、再回填，hash 又变——无穷回归。
- **Discovery**: 这是标准不动点，不是失误。**逃逸方式只有一个：把自指的那一行排除在比对之外**，其余全部锁死。代价是那一行失去保护——可接受，因为它的内容（一个 hash）本身可被 `rev-parse --verify` 独立校验。
- **Action**: 任何"文档里存锚、锚里存文档"的结构，比对时 `grep -v '^<自指前缀>'` 排除该行，并在契约里**写明为什么排除**（否则下一个人会以为是漏了）。同时对该行的**值**加独立校验（如 hash 必须是合法 commit）。
- **failure_mode**: Naive default: 把 hash 回填进文档后，继续用"文档与锚版本逐字相同"当判据。Why wrong: 回填这个动作本身制造了差异，判据恒红；而"再提交一次"会换新 hash，形成无穷回归。
- **Grounded in**: HANDOFF-20260812-lite-goal-gate.md §0.1 / AC10 / V8，commit a94f7a3 → 51ceeda

### 逐字钉死表的每个 NEW 值必须先过一遍禁词表 - 2026-08-14

- **Context**: 清理类改动常同时配两样东西：(a) 一张逐字替换表「这一行必须变成 X」，
  (b) 一条穷举扫描「全仓不得再出现词组 W」。二者分别由不同的 AC 承载。
- **Discovery**: 路由脱钩单里，替换表第 13 条的 NEW 值是
  `echo "  /alex, /blake, /gate     - Default channel"`，而扫描的禁词表含 `default channel`
  （大小写不敏感）——**契约要求写入的字符串，正是契约要求消灭的字符串**。两条 AC 在任何实现下
  都不可能同时绿。同一形状在同一份契约里出现了第二次：另一条 AC 要求某文件「除豁免区间外逐字不变」，
  而该文件有一行被替换表钉死必改、且不在豁免区间内。**两次都由三个并行专家审查漏掉，
  在实现期才暴露**——因为专家各审一个维度，而这个缺陷恰好活在**两个维度的接缝上**：
  单看替换表合理，单看禁词表也合理。
- **Action**: 契约定稿前跑一条机械预检（几秒钟）：**把替换表的全部 NEW 值拼成一个文本，
  用穷举扫描的禁词表去 grep，命中数必须为 0**。同理，把"必改行"的清单与"必须逐字不变"的
  区间定义做一次交集，必须为空。这两条都可脚本化，且**必须由 Alex 在交单前跑**，
  不能留给实现方——留给实现方时，他面对的是一条无解的 AC，最省事的出路是改 AC。
- **failure_mode**: 朴素默认：分别检查"替换表对不对"和"禁词表全不全"，两边都没问题就交单。
  为什么错：缺陷不在任一张表内部，在两张表的交集上；而并行的多维度审查天然按维度切分，
  接缝正是无人负责的地带——维度越分得干净，接缝越没人看。
- **Grounded in**: `.tad/archive/handoffs/HANDOFF-20260814-routing-decouple.pins.tsv` pin 13；
  `.tad/evidence/acceptance-tests/routing-decouple/amendment-record.md` §矛盾 + 补记 1；
  `COMPLETION-20260814-routing-decouple.md` Gate 4 §「三专家未捕获」

### 每收窄一次过滤器，都要验"该留的还在"——否则会恰好滤掉目标 - 2026-08-14

- **Context**: 一条 AC 需要"该协议块内的独立强制子项"清单作为期望集。首版正则不加过滤，
  产出 1144 条（24 个块，均 48 个），逐字列举不可满足，必须收窄。
- **Discovery**: 三轮收窄，**中间一轮恰好把要守的那个目标滤掉了**。(1) 按宽标记过滤 → 151 条；
  (2) 按窄标记过滤 → 62 条，**但区间是按"下一个匹配行"算的，把父子当成了兄弟**：
  `      knowledge_assessment:`(6 空格) 的区间被截在 `        blocking: true`(8 空格) 之前，
  于是**有意义的键名被丢弃、无意义的 `blocking: true` 反被保留**——而 `knowledge_assessment`
  正是审查员点名"会被静默吞掉"的那条纪律，AC 却恰好不再守它。(3) 改缩进感知 → 98 条，
  但 `blocking` 这类键**自己满足自己**（键行即标记行）；要求标记出现在键行**之后** → 84 条。
  **每一轮都是跑出来发现的，不是读出来的。**
- **Action**: 过滤器每收窄一次，**必须用一个已知必须保留的样本做正控**（"该留的还在吗"），
  不能只看总数从大变小就认为收窄成功。数量下降是收窄生效的证据，**不是收窄正确的证据**。
  正控样本优先取**审查意见里点名的那个具体案例**——它是外部给的，不是自己挑的。
  另：任何"区间/作用域"计算，先问它是否需要**层级感知**；扁平的"到下一个匹配"在任何
  嵌套结构（YAML、markdown 标题、代码块）上都会把父子当兄弟。
- **failure_mode**: 朴素默认：过滤条件太松就加条件，看总数从 1144 降到 62 就认为对了。
  为什么错：收窄同时在删噪音和删信号，而**总数只反映删了多少、不反映删了什么**；
  被删掉的恰好是目标时，AC 依然全绿、依然"有判别力"的样子，只是不再守它本来要守的东西。
- **Grounded in**: `HANDOFF-20260815-discipline-enumeration.step0.sh`（缩进感知区间 + 键行后置要求）；
  `.tad/evidence/acceptance-tests/discipline-enumeration/subkeys.tsv`（84 条/13 块，含两条 `knowledge_assessment`）

### 契约里的"冻结命令"必须从契约文件里抽出来跑——重打一遍等于没验 - 2026-08-14
- **Context**: 一张 express 单（移 33 行、不改一个字）出了 5 个 rev。其中 rev2 与 rev5 是**同一个错**：
  Alex 在 shell 里敲一条命令、看输出对了，然后把**另一条**写进契约。
  rev2 是拿 `printf` 造的合成 YAML 片段当证据，断言"插入会破坏 YAML"——而真文件因含
  `*product:`（命令语法被当 YAML alias）**本来就不是合法 YAML**，该论证的事实不存在。
  rev5 是测试时用了 `else {if(...) m=...; if(...) n=...}`，写进契约时只改了 `END` 段、
  主体留成 `else if(...) m=...` → `n` 从未赋值 → `SPAN` 恒等于 `MAXLINE` →
  **正确完成移动后 AC1 仍然红**，且 T1 恰好也红（因 `m>981` 本就成立）**掩盖了方向负控**。
- **Discovery**: 这两次都不是"没验证"，是**验证了一个替身**。合成片段、重打的命令、
  自己另写的判定脚本——都属于替身。替身跑绿只证明替身对，不证明交付物对。
  抓到 rev5 的是 Blake 在 Step 0 老实跑契约让他跑的东西，比任何一轮专家审查都便宜。
- **Action**: (a) 凡契约里出现"冻结命令/判定器"，验证时**必须从契约文件里 grep 出该字符串再 eval**，
  禁止重打；(b) 结构性断言必须跑在**真文件**上，不能用手造的最小片段代替；
  (c) 完成度类 AC 的方向负控要**同时验 T1 必红与"模拟完成后必绿"**——只验前者时，
  一个恒真的失败条件会被"本来就该红"掩盖。
- **failure_mode**: 朴素默认：命令在终端跑对了就复制进文档，或者凭记忆重写一遍。
  为什么错：文档里那条才是 Blake 要执行的对象，它和你跑过的那条之间没有任何机制保证一致；
  而 AC 越"看起来只是抄一下"，越没人会去验它。
- **Grounded in**: `HANDOFF-20260814-obligations-above-cap.md` §8 版本记录；
  Blake Step 0 退回记录（`MINLINE=` 空 / `SPAN=1789`）；第三轮审查报告 P0-1

### 验收键选长了假阴、选短了假阳——键必须同时过"零旁路"和"抗改写"两道 - 2026-08-14
- **Context**: 一条行为回读 AC，判"agent 是否读到了某个块"。键取自块内原文
  `rm -rf/DROP/删桶/打印凭据/移除鉴权/转公开/改支付`，选了 `删桶`/`鉴权`/`转公开`/`改支付`。
  4 个真实 agent 实测：**读到块的两个 agent 复述时自然展开**成「删存储桶」「把私有资源转为公开」
  「改支付相关逻辑」→ `grep -Fq 删桶` / `转公开` **MISS**，判分 2/4 与 3/4，按 AND 语义**双双 FAIL**。
  信号是满格的（未读到的两个 0/4、读到的两个复述了全部七类），**坏的是尺子**。
- **Discovery**: 反方向同样致命：键取太短会命中块外内容（`凭据` 在 12 个常驻文件里块外有 1 次命中）。
  正解是**最短的区分性名词**——去掉动词前缀的 `桶`/`鉴权`/`公开`/`支付`：
  块外仍全为 0（鉴别力不丢），而对四种真实改写全部命中（假阴消失）。
- **Action**: 任何"必含键"型判定，定键时必须机械过两道：
  (1) **零旁路**——该键在候选来源集合里除目标块外命中数为 0；
  (2) **抗改写**——把键对一组**真实 agent 作答**（不是自己想的同义词）`grep -Fq`，全部命中。
  两道都过才能写进契约。**只做第一道会假阴，只做第二道会假阳。**
- **failure_mode**: 朴素默认：从原文里挑一个看起来最有代表性的短语当键。
  为什么错：`grep -Fq` 是精确子串，而 agent 复述时几乎必然改写；
  于是"干对了活被自己的 AC 判死"，接着执行方就得在"按规矩退回"和"找理由说这次算过"之间选——
  后者正是纪律失效的入口。
- **Grounded in**: 第三轮审查报告 P0-1（4 份原始作答 T1 0/4 vs postmove 3/4、2/4）；
  `HANDOFF-20260814-obligations-above-cap.md` §6 Step 0 第 5 步（零旁路 + 抗改写双证明）
