# Progressive Disclosure: Extract Only Constraint-Token-FREE Blocks

**Date:** 2026-05-31
**Linked to:** L1 "Judgment-Only Skill Files: Constraint Rules Are NOT Mechanical"

---

### Progressive Disclosure: Extract Only Constraint-Token-FREE Blocks, Descending Order, Live Boundary Re-Derivation - 2026-05-31
- **Context**: P3 of the tad-lean-trustworthy Epic moved 9 mutually-exclusive intent/command-gated protocol blocks out of alex/SKILL.md (6441→5825 lines, ~10%) into on-demand `references/*.md` stubs. The SAFETY constraint: the always-loaded `MUST NOT|VIOLATION|MANDATORY|forbidden_implementations|NOT_via_alex_auto` count MUST stay 131. The conflict that shaped scope: the obvious large blocks to extract (research_plan/express/experiment) ALL carry constraint tokens, so moving them would drop the count → the user chose OPTION A (token-free blocks only) rather than reframe the SAFETY AC.
- **Discovery**: (1) **Pre-measure per-block constraint-token counts to pick the extraction set** — the safety of "byte-identity trivially holds" rests entirely on every moved block being 0-token; verify each block's count is 0 BEFORE slicing, not after. (2) **Extract in DESCENDING line order** so earlier edits don't shift later block boundaries — but ALSO re-derive each block's boundary LIVE from the file (top-level YAML key at col 0 → line before next col-0 key) immediately before each slice, making it robust to any drift rather than trusting the handoff's measured ranges. (3) **The col-0 top-level key LIST must be byte-identical before/after** — because each block is replaced by a STUB that keeps the same key, the key list is the cheapest structural invariant proving "9 keys remained, nothing else moved": `diff <(HEAD col-0 keys) <(after col-0 keys)` = empty. (4) **wc-l accounting must reconcile every line**: 9 stub swaps gave 5822, the +3-line router NOTE gave 5825 — track stub-line-additions AND wiring edits separately so the final count is explainable, not just "under the cap". (5) **The byte-identity check diffs `git show HEAD:block` vs `tail -n +3 reference`** (skipping the 2 header lines = comment + blank), keeping the provenance header OUTSIDE the identity contract.
- **Action**: For progressive-disclosure extraction guarded by a constraint-token SAFETY count: (a) measure per-candidate token counts and extract ONLY 0-token blocks; (b) descending order + live boundary re-derivation per block; (c) prove structure via col-0 key-list diff (must be empty) + per-block HEAD-vs-reference diff (must be empty); (d) reconcile wc-l line-by-line including wiring edits; (e) keep the reference provenance header outside the byte-identity check.
- **Grounded in**: COMPLETION-20260531-tad-lean-trustworthy-phase3.md, .claude/skills/alex/references/ (9 files), HANDOFF-20260531-tad-lean-trustworthy-phase3.md §6/§9/§10
