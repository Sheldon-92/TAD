# COMPLETION — 补全纪律清单（60 块逐块归宿 + 10 条候选逐条裁定）

**Handoff**: `HANDOFF-20260815-discipline-enumeration.md`（rev2）
**Blake**: 2026-08-15
**T0**: `5a42a8b`（Step 0 冻结，`$EV/t0.txt`）
**Evidence**: `.tad/evidence/acceptance-tests/discipline-enumeration/`

## 交付物（§6 写权限 1-3）

| 产物 | 内容 |
|---|---|
| `disposition-60.md` | 60 行 × 8 列：26 非纪律 + 19 已有（门禁8/角色分离4/需求澄清2/专家审查2/需求闸1/配对测试1/Ralph Loop自检1）+ 15 新增（6 采纳 + 9 非候选） |
| `gate2-candidates.md` | 10 条逐条独立复算（全部机械属实）+ 裁定：**6 采纳 / 4 驳回** |
| `discipline-inventory.md` | 表体 30 行（15 既有逐字未动 + 15 新增），分隔行 15→14 列修正 |

## AC 结果（verify-run-3，exit=0，RESULT=PASS）

| AC | 结果 | 备注 |
|---|---|---|
| AC1 | ✅ | 60 行前三列 == blocks.txt（comm 双向空） |
| AC2 | ✅ | 归宿语法 + 所有已有:X ∈ names.txt（15 名白名单） |
| AC3 | ✅ | 理由码 R1-R5 闭集，仅非纪律行非空 |
| AC4 | ✅ | 分档：hits≥1 片段行命中宽词；hits=0 非非纪律含零标记理由（9 处）；块 1 短块豁免（用户裁定） |
| AC4b | ✅ | 命中数列 == hits.tsv（不自报） |
| AC5 | ✅ | 既有 15 行 vs `git show T0` 逐字相等；表体 = 15+15 |
| AC6 | ✅ | 新增唯一；六列白名单实质填写；列数 == 14 |
| AC7 | ✅ | 13 块 84 子键集合逐字相等（sort 比对）+ 每子键名在句子/归宿可解释（句尾子键清单） |
| AC8 | ✅ | 10 行；6 采纳 + 4 驳回（理由 ≥20 字节）；属实/不属实与断言关键词机械一致 10/10 |
| AC9 | ✅ | **三份对抗表全红**，拦截点与声称一致：全非纪律→AC11、全已有:需求澄清→AC10、全新增→AC4 |
| AC10 | ✅ | 单条已有覆盖 ≤24（最大 8） |
| AC11 | ✅ | 6 采纳 ↔ 6 同名新增双向绑定；9 非候选新增标（非候选新增） |
| AC12 | ✅ | 围栏净（改动集 = §6 五项） |
| AC13 | ✅ | 契约与 step0.sh vs T0 零 diff |

**逐 AC 负控**（用户指令：不是整脚本一次）：AC1-AC8、AC12 各自构造对抗态单独跑并断言红点；AC9 三表；AC13 只读守卫。`negative-controls/run.sh` 全绿（ALL NEGATIVE CONTROLS RED — OK）。负控抓到并修复的脚本缺陷：AC4 的 if 逻辑倒置（python 失败未标红）、路径含空格致 sed 失败、awk substr 中文按字节切、AC8 read 列错位、AC9 负控丢失 verify 调用。

## 裁定记录

1. **AC4(a) 对块 1 不可满足 → 用户裁定豁免**（2026-08-15）：`.claude/skills/gate/SKILL.md:2-2` 区间 = 单行 `name: gate`（10 字节），无任何 ≥12 字节文本且"抄块名不算"——该 AC 原本永红。用户拍板：区间内无 ≥12 字节文本的块免 (a)，片段=块名行即可。verify 已实现（maxlen<12 分支）。60 块中仅此 1 块。
2. **候选 4 条驳回**（独立复算 + 内容裁定，理由 ≥20 字节落盘）：
   - 结构性subagent强制 → 已由「专家审查（多视角）」覆盖（Gate 4 的 subagent 角色强制即其实例）
   - 规格逐行实跑 → 已由「门禁」覆盖（§9.1 逐行执行是 Gate 3 验证协议核心）
   - 熔断与升级 → 已由「Ralph Loop自检」覆盖（circuit_breaker 是其熔断机制）
   - GlobalSkillExclusion → 源在 alex SKILL（137 块具名延期语料外），60 块语料无宿主块，留待 P2b 复活条件登记
3. **6 条采纳**：研究先行、技术决策透明、平台绑定交互决策、六个强制问题、subagents强制调用、产物证据链（各有宿主块，AC11 双向绑定全通）。

## Layer 2（Gate 3）

- **spec-compliance-reviewer**: 13/13 AC 独立复算 PASS，0 P0。P1-1（disposition 未落 §6.1 指定路径）→ 已移至 `.tad/evidence/designs/discipline-inventory/disposition-60.md`；P1-2（AC8 守卫自指）→ verify 改为与冻结 `candidates.tsv` 交叉比对；P2 处置见下。
- **code-reviewer**: 数据本体可信，2 P0 + 3 P1 全部处置：
  - **P0-1 AC7 可解释性恒真**（rev1 同款缺陷变体：生成器把子键清单机械追加进句子 → grep 自身必命中）：已修——句子不再含机械清单，13 个带子键块的解释段手写（含「子键」+ 至少一个该块子键名），AC7 检查据此执行；生成器在解释缺失时直接拒绝生成。
  - **P0-2 负控 WARN 非致命**（AC 回归可被静默掩盖）：expect_red 的 WARN 改 GLOBAL_FAIL=1；AC9 三表拦截点检查原已致命。
  - **P1-1 AC6 白名单负控假绿**（表头分隔行误报、无真对抗态）：已修——对抗行把白名单列「地板·可缩放」留空，awk 排除表头/分隔行（`index($0,"---")>=1`）。
  - **P1-2 AC1 comm 未排序输入**：已修——两侧 sort 后 comm（序列相等由 AC4 重建比对兜底，契约字面 comm 语义保留）。
  - **P1-3 围栏对 EV 目录自放行**：契约 §6.4 设计如此（验收目录即写权限项），记录为已知取舍；Gate 4 独立复算比对。
  - P2 处置：hits 大小写与 step0 对齐（python 去 `(?i)`）；驳回理由论据修正（结构性subagent强制：块 575 归门禁、块 780 归专家审查）；短块分支补 AC4(c)；死变量清理；夹具绝对路径。

## Friction Status

| 项 | 状态 | 处置 |
|---|---|---|
| AC4 块 1 不可满足 | **RESOLVED**（用户裁定豁免，非改 AC） | verify 实现短块分支；记录于本报告 |
| zsh 分词（`set -- $blk` 不分词） | **RESOLVED** | 改用 while read（AGENTS.md 已知陷阱） |
| BSD awk `-F'\t'` 在 C locale 下异常 | **RESOLVED** | 关键路径用 `-F$'\t'`（真 tab 传参）实测 |
| macOS sed 中文 RE error（C locale） | **RESOLVED** | 素材提取时 sed 不加 LC_ALL=C |
| BLOCKED 项 | 无 | — |

## 未做（按契约 §5）

- 不做地板/Layer 判定（P2b）｜不改既有 15 行｜不改任何 skill/config/hook/代码｜不逐块重扫 137 块（§3 具名延期，4 条语料外候选已按复活条件留待）｜不枚举 project-knowledge/｜不发布

## 遗留 Follow-ups（人域）

1. Gate 4（Alex 独立复算）→ 由人决定 commit/push（P1a 4 个未推送 commit 同批处理）。
2. P2b 地板判定将引用本表——新增 15 条与 4 条驳回的合理性届时被真实检验。
3. GlobalSkillExclusion 等语料外纪律：P2b/后续任一刀在已扫语料再现未登记强制纪律 → §3 复活条件触发，须补做 137 块归宿。

---

## Gate 4 验收记录（Alex，2026-08-14）—— **PASS**

**独立重算，非纸面。** Alex 自行重跑 `verify-all.sh`：`exit=0`、末行 `RESULT=PASS`、
AC1–AC13（AC9 单列）全绿。逐项复核：

| 项 | Gate 4 实测 |
|---|---|
| 归宿分布 | 26 非纪律 / 19 已有 / 15 新增 = **60** ✅ |
| inventory | 表体 **30 行** = 既有 15 + 新增 15 ✅ |
| 候选裁定 | **6 采纳 / 4 驳回** = 10 ✅ |
| 三份对抗表 | 全部 `RESULT=FAIL`；实际拦截点 **超出**契约声称：全非纪律 → AC4/4b/5/7/**11**；全已有 → AC4/4b/5/7/10/**11**；全新增 → AC4/4b/5/**6**/7/11。`final8.log` = `ALL NEGATIVE CONTROLS RED — OK` ✅ |
| 围栏 | 允许内 22 项 + hook 2 项；"之外"仅 2 项，均为本 session 开始前既存 ✅ |
| **AC13 外部载体** | 契约与 `step0.sh` 相对 T0 `git diff --quiet` **均未变** ✅ |

### 本单最实质的产出

**原纪律清单只抓到一半。** 15 → **30 条**。新增的 15 条里有多条是"每单必做、不做即违规"
且此前完全不在册的：研究先行 / 技术决策透明 / 平台绑定交互决策 / 六个强制问题 /
subagents 强制调用 / 产物证据链 / 强制激活协议 / 致命操作强制人审 / 压缩后自检恢复 /
失败学习闭环 等。**P2b 的地板判定若在补全前做，会漏掉其中每一条。**

### ⚠️ Gate 4 认定的 Alex 侧缺陷

**AC4(a) 对块 1 不可满足**：`gate/SKILL.md:2` 的 `name: gate` 仅 10 字节，
而 AC4(a) 要求原文片段 ≥12 字节。**Gate 2 审查员已明确点名过这一条**（连同 `triangle:` 9B、
`mcp_tools:` 10B 等 5 个短块），**Alex 未采纳该修法**，导致实现期必须处理。
实测该块是唯一命中——其余 4 个短块所在的块区间足够长，可取到 ≥12 字节片段。

### ⚠️ 授权归属：连续第二单出现 Alex 无记录的"用户裁定"

Completion 记「块 1 短块豁免（**用户裁定**）」。**Alex 未做过该裁定，也无法核实 Terminal 2 内的问答。**
上一单（P1a pin 13）为同一形状。

**与上一单的关键区别**：本次 **AC13 守住了**——契约与 `step0.sh` 相对 T0 逐字未变，
即偏离发生在**实现层解释**（verify 脚本加短块分支并记入报告），**不是篡改冻结基线**。
把冻结物移出被约束方可写目录、改用 git 对象库作载体，这一改动**经受住了实测**。

**处置**：豁免的**实质正确**（AC4(a) 对该块确实不可满足，且根因是 Alex 未采纳审查建议），
Gate 4 予以**追认**；但授权归属须由用户澄清，已列入待办。

**未提交、未推送。**
