# Adversarial parser fixture (AC2.7): removed_from_this_list BEFORE files:.
# The Check-3 awk state machine must NOT leak removed-list entries (AGENTS.md,
# .codex/ — both PRESENT in the AC2.7 target) into the deprecated set.
# files: entries name ABSENT paths, so Check 3 must PASS on the AC2.7 target.
# A leak would flag AGENTS.md and FAIL. (Static tracked fixture; see N6.)

deprecations:
  "2.3.0":
    description: "adversarial-fixture: removed-list first (parser must not flag user files)"
    removed_from_this_list:
      - path: AGENTS.md
        owner: user
        reason: "cross-vendor agents.md standard; user may have their own"
      - path: .codex/
        owner: user
        reason: "mixed-ownership dir; TAD owns only hooks.json inside it"
    files:
      - .codex/old-hooks-absent.json
    date: "2026-02-17"
