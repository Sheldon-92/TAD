# Gate ROI Report

Generated: 2026-07-02 | Window: 2026-07-01 to 2026-07-02 (1 days)

## 1. Gates Run

Total gate_result events: 4

| Gate | pass | partial | fail |
|------|------|---------|------|
| Gate 3 | 4 | 0 | 0 |

```
复算命令: grep '"type":"gate_result"' .tad/evidence/traces/*.jsonl | jq -r '[(.context|capture("Gate (?<g>[0-9]+)") // {g:"?"}).g, .outcome] | @tsv' | sort | uniq -c
```

## 2. Caught Pre-Ship

### 2a. Review Findings (P0/P1)

Numbered findings (P0-N/P1-N, deduplicated): **5**
Files with unnumbered P0/P1 mentions: **3** (per-file-per-level count — structural lower bound)

### 2b. Gate 4 Late Interceptions

Handoffs with non-empty gate4_delta: **3**

> Note: gate4_delta entries are Gate 4 **interceptions** (gaps Alex caught at acceptance), not escapes. Structural lower bound — unnumbered review findings are counted per-file, compressing the true finding count.

```
复算命令: for f in .tad/archive/handoffs/HANDOFF-*.md; do awk '/^gate4_delta:/{g=1;next} g && /^  -/{found=1} g && /^[^ ]/{exit} END{if(found)print FILENAME}' "$f"; done | wc -l
```

## 3. Escaped Post-Ship

escape rate = 0 / 3 = 0.0%

> **Lower bound**: escape count only captures handoffs explicitly prefixed `bugfix-`/`fix-`. Silent fixes (folded into feature handoffs or direct commits) are invisible to this metric. The true escape rate is at least this high, likely higher.

```
复算命令: ls .tad/archive/handoffs/HANDOFF-*.md | grep -cE '(bugfix|fix)-'
```

## 4. Judge Score Trend

insufficient data (n=1) — accumulating

```
复算命令: find .tad/evidence/acceptance-tests -name trajectory-judge.json -exec jq -r '[.D1.score,.D2.score,.D3.score,.D4.score,.D5.score] | @csv' {} \;
```

## 5. Per-Gate Attribution

| Gate | pass | partial | fail | total |
|------|------|---------|------|-------|
| Gate 3 | 4 | 0 | 0 | 4 |

```
复算命令: grep '"type":"gate_result"' .tad/evidence/traces/*.jsonl | jq -r '[(.context|capture("Gate (?<g>[0-9]+)") // {g:"?"}).g, .outcome] | @tsv' | awk -F'\t' '{print $1, $2}' | sort | uniq -c
```

