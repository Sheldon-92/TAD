# Fixture t3-heavy（AC7 判别力 · 命中 3 → 重档 · 顺位 1+2）

> 一段带合法 `## 档位` 段的契约文本。改动集为普通文档调整，不涉及
> 目录结构或外部命令，失败在脚本输出中直接可见。

## 档位
新建判断: 3（① 报告按周粒度还是按日粒度汇总；② 阈值取 30% 还是 50%；③ 例外清单包含还是排除边界值。投影不算）
产物是否成为判据: 是（生成的清单会被后续的单/agent 当成依据或基线）
失败是否可见: 否（填错一个格子、闸不触发都不会报错）
→ 档位: 重，加派 2
