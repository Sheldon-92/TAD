# Fixture t1-criterion（AC7 判别力 · 命中 1 → 中档 · 顺位 2）

> 一段带合法 `## 档位` 段的契约文本。改动集为普通文档调整，不涉及
> 目录结构或外部命令，失败在脚本输出中直接可见。

## 档位
新建判断: 0（无——全部按既有规则机械执行，逐条机械复算。投影不算）
产物是否成为判据: 是（生成的清单会被后续的单/agent 当成依据或基线）
失败是否可见: 是（任何一步做错都会在脚本输出中报错）
→ 档位: 中，加派 1
