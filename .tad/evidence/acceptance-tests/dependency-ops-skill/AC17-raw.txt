## AC17 判分原始证据（reviewer 首轮回答摘要）
Q1: 完整命令序列（perl alarm + NO_AUTO_UPDATE + 绝对 root）→ scan-results.yaml + yq -r 读取
Q2: current_version/version_pinned_at/last_checked + Edit + yq -i 规范化理由 + 邻居不变
Q3: 首轮来源=定位与口径（说明性）→ 修复后来源=deps show 第3步（操作指令）
Q4: registry:null 三个跳过（notebooklm-cli/rsync/claude-code-cli）+ npm/pypi 才有 advisory
Q5: npm 会被扫到 + 唯一拿得到 security advisories 的一类
Q6: 完整动作序列（三字段 + Edit + 基线快照 + diff 6 行变化断言）
### 增量复核（Q3 fix 后）
Q3 现可完全从操作指令小节得出（29-31 行内联公式+tier+N>0 条件），自足无跨节依赖

## 判分保真说明（P1-2 处置）
reviewer 首轮回答未逐字落盘（落盘为执行者转述的每问摘要）。判分依据：
- reviewer 输出中的关键内容（Q1 完整命令、Q2 三字段+Edit+yq 原地写、Q5 npm 会被扫到+
  唯一拿得到 advisories、Q6 基线+diff 6 行）与契约判分基准逐项对应，判分过程真实。
- 首轮 Q3 出处=说明性文字 → FAIL → SKILL.md fix → 同 reviewer 增量复核（resume 同一
  spawn）确认 Q3 转正——修复轨迹由同一 reviewer 自证，非执行者自判。
P1-2 属证据保真问题：原始回答全文未保留。后续单建议 reviewer 回答直接写文件。
