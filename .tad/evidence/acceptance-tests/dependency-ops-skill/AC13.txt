## AC13 limitation resolution 分支：本轮不可达（显式声明）
gh 的 known_limitations 实测为 []（REGISTRY.yaml L69）→ deps_update 第 5 步的
limitation-resolution 分支在本轮 dogfood 上结构性不可达。

为何不用其它依赖替代：
- yq：DEP-003（yq -i 规范化）与 limitation 无关，且本轮上游无变更（4.53.3 == 4.53.3），
  强行触发需要修改 known_limitations —— 合成 fixture，验的是假数据。
- rsync：DEP-004（版本钉死）本轮上游无变更，同左。
- notebooklm-cli / claude-code-cli：registry: null 结构性跳过，无 changelog 可依据。

依据（ac-verification.md）：2026-08-11「守卫判据须在真实数据的全部形态上空跑」；
2026-08-06「Before Trusting a Guard, Measure Whether Its Trigger Condition Can Even Occur」。
诚实声明优于假绿：用合成 fixture 掩盖不可达分支 = 验假数据。

RESULT: PASS（显式声明，见 findings.md 缺口小节）
