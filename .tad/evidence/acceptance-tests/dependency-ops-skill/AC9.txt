## AC9 relevance 判定（完整 changelog，11294 字符）
输入: gh api repos/cli/cli/releases/latest | jq -r '.body'（全量，非 scan-results 截断 18%）

**判定: HIGH**

理由（逐字引用 REGISTRY 中 gh 的 capabilities_used 之一）:
"GitHub API access (repos, issues, PRs, releases)" —— 本单 changelog 中 4 个安全漏洞
修复全部落在该能力路径上：GHSA-3m3g-3wcr-px46（terminal escape sequence injection，
影响 gh gist view / gh api / gh pr diff / gh release download 等命令）、
GHSA-4fjg-2h4q-fwg3（URL path metacharacters 可改请求路径）、
GHSA-cg6r-mpgc-h9mm（gh auth status 可能以明文打印部分 token）、
GHSA-mm27-mwq9-fr5g（attestation verify 的 signer 匹配可被 lookalike 仓库绕过）。
这些命令（gh api / gh release）正是 *publish / *sync-add 流程的日常调用面。

breaking change 检查: 唯一 "removal" 命中是内部 CODEOWNERS 清理（PR #13886，
删除不存在的 pkg/cmd/release/attestation/ 目录的规则），非用户可见不兼容变更。
判定: 无 breaking change → 不触发停机，继续执行 upgrade-gh。

RESULT: PASS（HIGH relevance，可升级）
