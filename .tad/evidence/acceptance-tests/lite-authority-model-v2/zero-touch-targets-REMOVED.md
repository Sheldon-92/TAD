# zero-touch 目标清单已移除（2026-08-15）

原有四个文件 `{repair2-,}zero-touch-{pre,post}-targets.txt`（合计 **78 MB / 520,524 行**）
记录了 AC 验证时对下游项目做的 zero-touch 前后比对。

**移除原因**：它们逐行记录了每个下游项目**每一个文件的内容哈希**，
以及未提交文件名、个人网站文稿名、客户项目名——
等于把多个私有仓库的完整文件清单发布到了公开仓库。

**这不影响当时的验收结论**：AC 的判定是「pre 与 post 逐行相同」，
该结论已记录在同目录的 COMPLETION 与 Gate 报告里，可复算方式是重跑生成命令。

⚠️ 该内容仍存在于本仓库的历史提交中（它们在移除前已被推送）。
本次处理的范围是**当前版本不再包含**。
