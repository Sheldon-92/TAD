# Design Output — WITH Skill (example-skill enabled)

We produced a design for the example bounded task applying the skill's discriminative markers.

Applied:
- EXAMPLE_RULE_ALPHA — the skill's named alpha rule applied to the decision tree.
- EXAMPLE_THRESHOLD_42 — enforced 42-unit threshold for the critical gate.
- EXAMPLE_EXIT_99 — terminal token verified at completion.

The output also preserves standard quality language but the three markers are the discriminative evidence.

