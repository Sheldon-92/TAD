# Gate 4：express 单 obligations-above-cap（Alex 独立复算，2026-08-14）

T1 = `837d9a3`。**不采信 COMPLETION 的任何数字**，全部 Alex 自跑。

## 1. 11 条 AC 独立复算

| AC | Alex 实跑 |
|---|---|
| AC1（**从契约文件抽命令跑，不重打**） | `MAXLINE=230 MINLINE=202 SPAN=28` `exit=0` |
| AC2 | P7 的 AC1 仍 29/29 命中 |
| AC3（`git show ${T1}:` 现算） | 行集 `diff` **0 行** |
| AC4 | T1=98,752 / now=98,752 / 镜像=98,752 —— **delta 0** |
| AC5 | `verify.sh all` 七项全 ok，`RESULT=PASS` |
| AC5.5(a) | 八份冻结基线 **零越界** |
| AC5.5(b) | `fence-baseline` **恰好只多 §6 那两行**，无删除 |
| AC6 / AC6.5 | parity 零输出；标题@L200、前置围栏 2、**parity=0**、总数 10→10 |
| AC7 | `measure.sh` 退出码 0；`TOTAL_STATIC` 250,924 两侧恒等 |
| AC8 | Blake 3 份 + **Alex 自派 1 份**，四键（桶/鉴权/公开/支付）**全部四中四** |

## 2. AC8 的独立复验（Gate 4 不接受纸面验收）

Alex 另派 1 个零上下文 agent，限定：对 `alex/SKILL.md` **只许一次 Read、不带 offset/limit、
禁止 Bash/Grep/Glob 旁路**，并要求回报 harness 的截断原文。

**它的自述**：
> 调用了 **1 次 Read，不带 offset/limit**。截断提示原文
> `showing lines 1-981 of 1790 total (38747 tokens, cap 25000)`。
> 未翻页、未 Grep。**没有违反读取限制。**

**它的作答**逐字引用了 L205 / L210 / L214 / L217 / L218 / L219 / L220 / L226 / L227 / **L230**
——**全部落在移动后的祈使句块（L200-231）内**，四键全中。

⚠️ **对照**：移动前（Gate 4 首轮 + 审查员）共 4 次同类测试，**七类致命操作一条都没被提到**，
四键 **0/4**。移动后 4 次（Blake 3 + Alex 1）**全部 4/4**。**信号从零到满，且方向单一。**

## 3. 一条如实记录的偏差

Blake 的 COMPLETION 报截断线为 **898 行**，Alex 自派 agent 实测为 **981 行**（原文如上）。
两者都远大于块的落点（L200-231），**结论不受影响**；差异可能来自 harness 的 token 估算随会话浮动。
**未采信任一方的数字作为契约值**——AC1 用的是绝对行号 ≤981 这个保守阈。

## 4. 判定：**PASS**

11 条 AC 全部由 Alex 独立复算通过；AC8 另有 Alex 自派的独立行为验证。
移动逐字无损（字节/行集/parity/围栏结构四重恒等），P7 的全部护栏未受影响。

## 5. 连带：P7 的 Gate 4 **由 FAIL 改判 PASS**

P7 首轮 Gate 4 的**唯一** FAIL 理由是「承重（29 条义务祈使句）落在单次 Read 够不到的位置」。
该阻塞项已由本单消除并经独立行为验证。P7 的其余结论不变：
13 条 AC 独立复算全绿、5 份负控 Alex 亲自重跑全红、外置内容 0 约束行、删除内容 0 约束词、
**没有任何一条纪律变暗**。

⚠️ P7 的四笔账仍然成立、转后续单：净省打折 25%（`knowledge-bootstrap` 每次激活都读）·
P7 的 AC8 前三题与 AC1 同义反复 · P7 rubric 的键太短会子串误判 · express 审查下限两个口径。
