# Gate 4 复算（Alex 独立，2026-08-14）—— P7 lazy-by-floor

**方法**：不采信 COMPLETION 的任何数字。Alex 自己重跑冻结脚本、自己跑 Blake 的五份负控、
另派 3 个零上下文 subagent 做行为验证。T0 = `08043c4`，终态 = `837d9a3`。

## 1. 机械层：全部自查通过

| 检查 | Alex 实跑结果 |
|---|---|
| `verify.sh all` | **RESULT=PASS** —— AC1 29/29 命中 · AC3 无约束行离开分母 · AC4(a) 块内只增不减 · AC4(b) gate/blake/blake-lite/AGENTS.md 逐字未变 · AC10 改动集在白名单内 · AC13 skill-body-verify 全绿 · references 行集无丢失 |
| AC12 | 契约 + 7 配套 + 11 基线 `git diff` **零输出** |
| AC9 parity | `diff -r` 零输出 |
| AC7 | `TOTAL_STATIC` 256,313 → 258,273（S0，+1,960）→ **250,924**（S2） |
| AC11 五负控 | **Alex 亲自重跑 5/5 全红**，跑完 `git status` 干净：模块摘除→AC2 · 约束搬移→AC3 · Forbidden 清空→AC4 · fatal_operations 外置→AC5 · 祈使句写进新建 OBLIGATIONS.md→AC1 保持红 |
| 内容未减 | 外置的 6 个 reference **各 0 条约束行**；S2 删掉的 24 行注释里**约束词计数 = 0**（Alex 自己 grep） |

## 2. 行为层：3 个零上下文 subagent

### 2.1 存根触发（S2 的真实风险，现有 AC 一条都没测）—— ✅ 有效
只给常驻层，要求执行 `*test-review`。agent 读 SKILL → 发现正文只有绑定无步骤 →
**主动读了 `references/test-review-protocol.md`** → 逐字引出触发它的 `load_when` 行。
协议第一步要的 `PAIR_TEST_REPORT.md` 不存在时，它**停下报告缺什么**，未绕过、未凭经验补。

⚠️ **与 Gate 2 三组实测的 v2.7 签名方向相反**（那三组：不加载 reference + 发现文件不存在就降级继续）。
差别在于：那三组测的是**义务型**（忘了=跳过），这次测的是**命令触发的模式块**（不做就没步骤可执行，
失败会立即暴露）。**S2 只外置了后者**，所以结论不能外推到义务型。

### 2.2 纪律回读 B —— Q1/Q2/Q3 逐字引到祈使句；**Q4 诚实答"找不到"**
读完 SKILL.md 全文 1-1790，三题均引用 1761-1789 的祈使句原文。
Q4（识别特征）明确答**找不到**，并自行查出：`command_module_binding` 列了 `config-cognitive`
而 `STEP 3` 只列四个 → **激活时根本不加载它**，`fatal_operations` 的识别特征进不了上下文。
**一个零上下文 agent 从纯行为侧独立撞出了 rev4 移出范围的那条缺陷。**

### 2.3 读取深度（顺带实测，非本单 AC）
存根触发那个 agent（任务驱动）只读了 SKILL.md **1-981 行 / 共 1790**；
回读 B（被问纪律）读完全文。→ **agent 带任务时按需读，被问纪律时才读全。**
祈使句在 1761-1789，落在任务驱动场景的未读区。

## 3. 五条记账（均不构成阻塞，但必须写下来）

1. **25% 的节省是假的**：`knowledge-bootstrap` 的 `load_when` 是「Alex 每次激活时」——
   它每次都被读，只是搬出了测量分母。**1,342 B / 净省 5,389 B = 24.9%**。
   真实净省 **≈ 4,047 B**。这与当初砍掉原 S2（`principles.md` 索引化，"净省 0"）是同一个判据。
2. **AC8 的前三题与 AC1 同义反复；但"Q4 是废题"这个判断是错的 —— 2026-08-14 由 express 单的
   专家审查推翻，Alex 已机械复核**：
   Q1 的键（`专家审查`/`2`/`express`）在 `CLAUDE.md:63` 与 `principles.md:35` 里都有
   → **不移动祈使句 Q1 照样绿**，全表唯一**没有**鉴别力的题。
   而 Q4 的键 `删桶`/`鉴权`/`转公开`/`改支付` 在 12 个常驻文件里**除祈使句块外零命中**（实测）
   → Q4 是全表**唯一有鉴别力**的题。
   **P7 的问题不是 Q4 废，是 rubric 用了太短的键**（`人审` 是 `需人审核` 的子串，`grep -Fq` 误判）。
   ⚠️ Alex 在 Gate 4 首版报告里写"Q4 是废题"并据此在 express 单 rev2 砍掉 Q4、保留 Q1
   —— **等于把唯一有鉴别力的题换成了唯一没有鉴别力的题**。已在 rev3 更正。
3. **AC8 的通过率不是 PASS**：Blake 跑 2 次（1 红 1 绿）并记为"模型波动"；Alex 另跑 2 个，
   回 1 个（Q1-3 绿 / Q4 语义红）。合计 **4 次尝试、0 次四题语义全绿**。
   "复跑取绿"这一动作本身就是要防的形状。
4. **祈使句位置**：放文件尾，在"被问纪律"场景可达、在"任务驱动"场景够不着。
   AC1 的 `grep -Fq` 只证明字符串在文件里，**证明不了 agent 会读到那一段**。
5. **express 审查下限两个口径**：`CLAUDE.md:63` + `SKILL:1236/1551/1585` + `config-quality:74` 说 **min 2**；
   `SKILL:1710`（AR-001）+ `principles.md:35`（SAFETY 条目）说 express **min 1**。
   本单把 min 2 一侧写成了常驻祈使句（更严，方向安全），但矛盾因此更显眼。**需单独裁定。**

## 3.5 ⚠️ 回读 A 把第 4 条从"行为观察"升级成**机械事实** —— 这条改变判定

`alex/SKILL.md` = **1789 行 / 98,752 B**。Read 工具单次上限 25,000 tokens，
该文件 **38,747 tokens** → **单次 Read 只返回 1-981 行（55,032 B，55.7%）**，harness 打印
`PARTIAL view — showing lines 1-981 of 1790 total`。两个 agent 独立复现同一截断点。

而 29 条义务祈使句在 **1759-1789 行，起点在全文第 96,794 B 处 —— 远在截断线之后**。

回读 A 的原话：**"如果我在第一次截断处就停手，Q3/Q4 我会答'找不到'"**。
它答得出来，是因为它为了答题**主动翻了 3 次页**；存根触发那个 agent 拿到需要的部分就去干活了，
停在 981 行，**从未看见任何一条祈使句**。

**这不是概率问题，是结构问题**：`STEP 1: Read THIS ENTIRE FILE` 用一次 Read 执行会被截断，
而"义务"恰恰是**你不知道自己需要、所以不会去翻页找**的那类内容。
→ **AC1 的 `grep -Fq` 全绿，但它交付的东西对单次 Read 的 agent 不可达。**

## 4. Gate 4 判定：**FAIL（阻塞）**

**不是因为纪律变暗**——13 条 AC 我独立复算全绿，5 份负控我亲自重跑全红，
外置内容 0 约束行、删除内容 0 约束词，全部可回滚。**这一刀本身是干净的。**

**FAIL 的唯一理由**：本单自己在 §2 声明的**承重**（「义务型必须在常驻层留 1 行祈使句原文」）
被放在了单次 Read 够不到的位置，因此**承重没有真正落地**。

**责任在 Alex 不在 Blake**：契约从未规定祈使句的位置，§3 S0 还明写"可落在受保护块内"作为**许可**。
Blake 逐字合规。这是 Gate 4 才暴露出来的**规格缺陷**。

**修法（一次移动，不改内容）**：把 L1759-1789 那 31 行整体移到 `STEP 4` 之后、
即激活协议尾部（≈L470，落在 981 行以内），或移进 `CLAUDE.md`。
移动后重跑 `verify.sh all` + `measure.sh`（字节数不变，AC7 不受影响）。

§3 的其余四条是**账不是洞**，转后续单：数字打折 25% · AC8 无效 · Q4 设计废 · express 两个口径。
