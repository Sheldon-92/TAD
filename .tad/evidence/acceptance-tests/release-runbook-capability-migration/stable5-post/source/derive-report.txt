=== RELEASE SYNC SET (derived from /Users/sheldonzhao/01-on progress programs/TAD/.tad/ — bias-to-sync, REPORTED each run) ===

--- A. ZERO-TOUCH (preserve target's own — NEVER sync) ---
  active
  archive
  decisions
  dependencies
  evidence
  github-registry
  memory
  pair-testing
  project-knowledge
  research-notebooks
  skill-library
  skillify-candidates
  (+ top-level file: sync-registry.yaml)

--- B. FRAMEWORK-SYNC (derived = live dirs MINUS deny-list — the auto-included set) ---
  agents
  capability-packs
  codex
  context
  cross-model
  data
  eval
  gates
  guides
  hooks
  logs
  migrations
  ralph-config
  references
  runtime-compat
  schemas
  scripts
  skills
  sub-agents
  tasks
  templates
  tests
  workflows

--- C. TRANSIENT / MAIN-ONLY (do NOT sync) ---
  checklists
  domains
  reports
  spike-v3
  working

--- Special-case notes ---
  * capability-packs → sync/diff ONLY capability-packs/pack-registry.yaml (registry index), never the pack tree.
  * per-release one-shot scripts → write to .tad/evidence/releases/ (zero-touch ⇒ not in the synced set).
