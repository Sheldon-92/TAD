# Layer 2 Review — code-reviewer (pricing-gate-scan-fix)

**Date**: 2026-08-05 | **Verdict**: ⚠️ **CONDITIONAL**（0 P0 / 2 P1 / 3 P2，全部执行实证）
**Model 自报**: deepseek-v4-flash

## 审查总结

核心交付物验证通过：落地命令与 §2.3/§4 规格整行逐字节一致（AC1 全项）；改动 1-6 按规格落地未溢出节外（AC2/AC3）；台账 0 数据行 + 前言同步（AC6）；parity PASS（AC9）。§2.3 声称的 13 项行为 + AC4a 的 12 项期望全部重跑验证逐字节吻合；新旧命令命中集不同，判别力成立。僵旗与假阳性两个立项缺陷均已消除。

## Findings

### P1-1 — AC7 围栏 1 条命中，DoD「全 PASS」未严格达成（执行实证）
`.tad/research-notebooks/REGISTRY.yaml`（active → dormant，mtime 12:19）不在 pre-impl 快照，实现窗口内变脏，不在授权集；Blake 自跑与 reviewer 重跑同样命中——非本次运行引入。归属判定：apply-changes.py 目标硬编码 3 文件无触碰能力；语义与本单无关。**结论：非本单实现引入，围栏按设计 fail-loud 工作**。处置义务：Completion 显式记录命中与处置（Git checkout 还原或人工确认为用户侧操作），Gate 4 前闭环。不得以「全 PASS」模糊带过。

### P1-2 — 补零但非法日期静默漏：违反自称的 fail-safe 性质（执行实证）
探针 P9：`PROVISIONAL: review-by 2027-13-99`（非法月份，补零）→ **零输出，永久静默**；P8：`2024-13-99` → 误标 OVERDUE。根因：主正则 `[0-9]{4}-[0-9]{2}-[0-9]{2}` 只验形状不验月份/日期范围，非法日期按词法进 `d < t` 比较。正是 §2.3「宁可吵，不可静默漏」要防的类别。AC1 冻结命令 + §1.3 禁新增约束 → 修法（月份 `(0[1-9]|1[0-2])`、日 `(0[1-9]|[12][0-9]|3[01])`）只能进后续单/P1b；**本单义务 = Completion 记录残差**。

### P2-1 — 前置过滤 token 碰撞假阳性（执行实证）
探针 P14：摘要列含英文词 `provisional`（小写）的合法 HAS-CARRIER 行 → 报 MALFORMED。前置过滤自身 `[Pp]…[Ll]` 大小写不敏感 + 冒号可选，对摘要列英文词碰撞——把自己防掉的类别用另一 token 请回。噪音非静默，概率低（需英文词进中文摘要列）。建议后续单锚定「行尾状态列」（如 `PROVISIONAL[：:]?[[:space:]]*$` 前置于解析）。

### P2-2 — 台账前言缺反静默续期规则，修复只落地一半（阅读推断）
台账前言已同步「状态列可就地转移为终态…」，但未含「禁止静默续期」与「不要重复 PROVISIONAL 字样」。按本单自己的理由（Gate2 P1-6：处置者当场看台账），只看台账的处置者拿不到反续期禁令。建议补一句「不得就地改写 review-by 期限」。

### P2-3 — AC4a 覆盖声称行为完整，但两个执行实证残差恰在覆盖之外（阅读推断）
AC4a 12 探针逐条对应 §2.3 全部声称行为且吻合；但 P1-2/P2-1 恰在 AC 覆盖之外。建议 AC 注释显式枚举「补零非法日期」边界，钉死残差为已知而非未知。

## 约定级条款语义检查

- §2.1 仅限终态：改动 4 文本枚举 4 终态 + 显式排除 N/A 与再发 PROVISIONAL——严格表达无歧义 ✓
- 改动 5 静默续期禁令：与「仅限终态」组合后无歧义 ✓
- §2.2 副作用：文本显式承认 + DoD 强制 Completion 承认——被承认 ✓
- 台账前言：与 skill 一致但缺反续期规则 → P2-2

## AC 设计检查

AC4a 判别力执行实证成立（新旧命中集不同：旧含 A5/A6 假阳性漏 A8；新 A5 静默、A8 显式报）。AC4b/AC5 判别力成立（旧命令对 zombie 探针 n=1≠0；positive 探针 n=3≠1）。AC 幂等性 OK（probe 为静态夹具或 cat>/cp 覆盖重建）。AC2 STRIP-FAIL 走 stderr + ok 计数自证「AC 真跑了」✓。

## Verdict: CONDITIONAL

无 P0；命令与全部声称行为验证正确，无需返工。两个 P1 为 Gate 4 前的处置义务，须进 Completion：(a) AC7 REGISTRY.yaml 命中与处置归因；(b) P1-2 补零非法日期静默残差记录并转 P1b/后续单。P2 三条建议性。

## 执行证据（摘录）

```
1. git diff 4b29dc26 -- 3 目标文件 → 改动 1-6 与 §4 规格逐字节对应
2. bash post-impl-check.sh → AC1-AC6 完毕 / AC7 → REGISTRY.yaml（唯一命中）/ AC8 0 命中 / AC9 parity PASS
3. /tmp/probe-edge.md 16 条边界探针（printf 精确控制空白）：
   OVERDUE: P1(无尾管道) P4(尾管道+TAB) P8(2024-13-99 误报) P11(跨年 2025-12-31) P12(2026-01-01) P16(CRLF)
   MALFORMED: P2(双管道) P3(中文句号) P7(2024-1-1) P10(大小写混合) P14(摘要英文 provisional) P15(多列)
   零输出: P5 P6 P9(2027-13-99 静默漏) P13
4. 跨年/当天边界（awk BEGIN 直测）："2025-12-31"<"2026-01-01"=YES；到期当天不报（语义正确）
5. AC4a 新命令输出与 exp 逐字节相同；BASE 旧命令命中 {A1,A2,A5,A6,B1,B2,B3,A7}
```
