# 纪律溯源（形态 B —— 逐条溯源）

> 每条纪律一段：防住过什么（带可验证载体）、删掉会怎样、想删之前必须先找到什么反例。
> 载体格式（AC2）：`<相对仓库根路径>#L<行号>` + 该行 ≥12 字符逐字片段。
> 实例类型：缺席致害（没有它出了事）/ 在场生效（有它抓到了）/ 合成（对抗测试产生，弱）。

## D01 需求澄清

- 判定：`1-留`｜来源：`both`｜频率：`每单`
- 地板·可缩放：`地板`（理由：防"做错东西"，其代价不随任务后果缩放；若找到"做错东西代价显著低于澄清成本"的轻任务，此判定证伪）
- 防住过什么：本 session 把用户的假设当成批准，6 个 phase、一次对外发布、一次系统升级建在未确认的前提上。
- 删掉会怎样：需求误解无人拦截，直接建错东西——上次删错（启动扫描）已证明"动手之前"的纪律一旦删掉代价不随后果缩放。
- 删之前先找什么反例：找一个"任务后果极轻、做错也无害"的任务，证明动手之前澄清对其也无必要。

### 实例

- 实例1（类型=`缺席致害`，严重度=`高`）：本 session 假设被当成批准，6 个 phase 建在未确认的前提上。
  - 载体：`.tad/active/epics/EPIC-20260812-discipline-weight-separation.md#L74`「假设被当成批准，6 个 phase」

## D02 需求闸

- 判定：`1-留`｜来源：`full`｜频率：`每单`
- 地板·可缩放：`地板`（理由：防"需求理解错了没人发现"，代价不随后果缩放；若轻任务需求错也无害，证伪）
- 防住过什么：lite 无需求闸，L0/L1 全是 Alex 自评无人复核——需求理解错（假设当批准）时无人发现。
- 删掉会怎样：需求理解错无人复核，与 D01 同根的事故重演且无人察觉。
- 删之前先找什么反例：证明"自评即可充分捕获需求误解"（需要一次"自评抓到了需求误解"的在场实例）。

### 实例

- 实例1（类型=`缺席致害`，严重度=`高`）：lite 无需求闸，需求理解错无人复核（与 D01 同根）。
  - 载体：`.tad/active/epics/EPIC-20260812-discipline-weight-separation.md#L76`「L0/L1 全是 Alex 自评」

## D03 重量裁定

- 判定：`1-留`｜来源：`both`｜频率：`每单`
- 地板·可缩放：`可缩放`（理由：重量裁定本身就是"重量随任务缩放"的定义——旋钮对象；若重量无法用离散档位表达而是连续谱，此判定证伪）
- 防住过什么：lite §2.5 主动删除重量裁定后，本 Epic 5 单的代价差异显示重量不再随任务匹配——不可撤销的对外发布 8 版 7 轮，可回滚的本机升级 4 版 3 轮，都走了同样的重量。
- 删掉会怎样：流程重量与任务后果脱钩，轻任务背重流程（或反之）。
- 删之前先找什么反例：证明"统一重量"对同类任务代价差小于一个量级（SC2 的证伪）。

### 实例

- 实例1（类型=`缺席致害`，严重度=`中`）：lite 删除重量裁定后，发布与升级代价不随后果缩放。
  - 载体：`.tad/active/epics/EPIC-20260812-discipline-weight-separation.md#L75`「本 Epic 5 单：不可撤销的对外发布」

## D04 专家审查（多视角）

- 判定：`1-留`｜来源：`both`｜频率：`每单`
- 地板·可缩放：`可缩放`（理由：视角数可缩放 min 2/1/0；若找到"视角数减少却不漏检"的反例，证伪）
- 防住过什么：① 一次 15 分钟 express 小改被专家审查抓出 4 个 P0（在场生效，证明小改不等于免审）。② Phase 4 三轮 9 个 P0 中 5 个是修复自己造的（在场生效，证明串行多轮不如并行多视角）。
- 删掉会怎样：单视角盲区无独立视角覆盖，同模型同盲区的缺陷直接穿透。
- 删之前先找什么反例：证明单一执行者自审能捕获"协议契约缺陷"这类跨节盲区（历史上 2026-07-30 AC principal 缺陷已证伪此假设）。

### 实例

- 实例1（类型=`在场生效`，严重度=`高`）：专家审查在 15 分钟 express 小改上抓到 4 个 P0（`expert review found 4 P0 on a 15-min express edit`）。
  - 载体：`.tad/project-knowledge/principles.md#L33`「Express Handoff is NOT Review-Exemption」
- 实例2（类型=`在场生效`，严重度=`高`）：三轮审查 9 个 P0 中 5 个是修复自己造的，证明串行多轮换视角数不划算。
  - 载体：`.tad/active/epics/EPIC-20260812-discipline-weight-separation.md#L77`「Phase 4：三轮 9 个 P0 中」

## D05 门禁

- 判定：`1-留`｜来源：`both`｜频率：`每单`
- 地板·可缩放：`地板`（理由：判据闭集、成本恒低；若某档位跳过门禁仍被其它纪律拦住同一失败类，证伪）
- 防住过什么：契约过了三轮对抗审查仍被 L0.5 机械准入拦下，三轮一个都没提——机械判据抓住人审查漏掉的形式缺陷。
- 删掉会怎样：已知失败类漏检，且无机械判据兜底。
- 删之前先找什么反例：证明"人审查 + 其它纪律"能等价替代机械门禁的闭集判据。

### 实例

- 实例1（类型=`在场生效`，严重度=`高`）：契约过了三轮对抗审查仍被 L0.5 机械准入拦下。
  - 载体：`.tad/active/epics/EPIC-20260812-discipline-weight-separation.md#L78`「仍被 L0.5 机械准入拦下」

## D06 启动扫描

- 判定：`1-留`｜来源：`full`｜频率：`每单`
- 地板·可缩放：`地板`（理由：成本近零 ~5 秒；若扫描成本随依赖数增长到不可忽略，证伪）
- 防住过什么：`urgent_security` 检测停跑 28 天，期间 `gh` 的 4 个安全漏洞（含明文打印部分认证 token）无人发现。
- 删掉会怎样：该看的东西没人看——上次退休它已造成 28 天安全盲区。
- 删之前先找什么反例：证明"依赖演进/安全公告"有等价载体在别处持续运行（`grep -rl 'scan-results\|last_scan' .tad/hooks/*.sh` 已证伪）。

### 实例

- 实例1（类型=`缺席致害`，严重度=`高`）：`urgent_security` 检测停跑 28 天，漏掉 `gh` 4 个安全漏洞。
  - 载体：`.tad/active/epics/EPIC-20260809-full-capability-extraction-retirement.md#L47`「`urgent_security` 检测停跑 28 天」

## D07 知识评估

- 判定：`3-挂起`｜来源：`both`｜频率：`每单`
- 地板·可缩放：`待判`（理由：无实例无法定位它防的阶段；需先找到"教训流失"实例）
- 触发条件：蒸馏循环停跑导致教训流失且无记录。
- 防住过什么：无实例——本 session 蒸馏正常发生，从未有"教训流失"被观测到。
- 删掉会怎样：教训流失不可见（它防的失败没有显式记录）。
- 删之前先找什么反例：找到"有可复用教训但未蒸馏、事后造成重复踩坑"的实例，否则维持挂起。

### 实例

（无实例——三关键词 × 四语料检索全空，见 search-log.md D07）

## D08 跨模型审查

- 判定：`1-留`｜来源：`full`｜频率：`罕见`
- 地板·可缩放：`待判`（理由：实例来自一次性对抗审查、非制度化协议；需先制度化并测成本随任务缩放行为再判）
- 防住过什么：同模型审查有系统性盲区——Claude 同模型循环对一类具体错误（错类名、废弃 API、错指标类型、错常量）整体橡皮图章，Codex 跨模型审查全部浮出，量化约 44 处同模型循环漏掉的抓包。⚠️ 但该实例是**一次性跨模型对抗审查**，非制度化协议——本判定纠正了 Epic 草稿「弱实例/偶然非制度」的低估，但「留」=制度化，非已制度化。
- 删掉会怎样：同模型同盲区无交叉视角（该盲区已实测存在，约 44 处）。
- 删之前先找什么反例：证明"同模型自审 + 专家审查"能捕获跨模型才可见的事实/API 正确性错误（pack-evaluation 已证伪）。

### 实例

- 实例1（类型=`在场生效`，严重度=`高`）：Codex 跨模型审查浮出约 44 处同模型循环漏掉的错误（错类名/废弃 API/错指标类型）。
  - 载体：`.tad/project-knowledge/patterns/pack-evaluation.md#L20`「now quantified at ~44 catches the same-model loop missed」

（旁注：Epic 草稿引「AC17 碰巧用了别的模型」为弱实例——本检索找到更强的 pack-evaluation 载体，纠正了草稿的低估。）

## D09 配对测试

- 判定：`3-挂起`｜来源：`full`｜频率：`罕见`
- 地板·可缩放：`待判`（理由：无实例；需非自指任务出现真机测试需求再判）
- 触发条件：真机/真外部系统任务出现。
- 防住过什么：无实例——TAD 全为框架内部工作，无真机/真设备任务。
- 删掉会怎样：未知（防什么未知）。
- 删之前先找什么反例：先出现一个"需要真机验证"的非自指任务，观察配对测试是否必要。

### 实例

（无实例——三关键词 × 四语料检索全空，见 search-log.md D09）

## D10 角色分离

- 判定：`1-留`｜来源：`both`｜频率：`每单`
- 地板·可缩放：`地板`（理由：角色混淆代价（越权执行）不随任务缩放；若找到"轻任务越权无害"的反例，证伪）
- 防住过什么：violations.log 两条缺席致害记录——2026-06-10 Alex 越权直执行跨项目破坏性清理，2026-08-02 Alex 直接写实现代码。
- 删掉会怎样：设计与执行混同，越权执行无人拦截（历史上已两次）。
- 删之前先找什么反例：证明"同一 agent 设计与实现"不会造成越权或盲区（violations.log 已两次证伪）。

### 实例

- 实例1（类型=`缺席致害`，严重度=`高`）：2026-08-02 Alex 直接写实现代码。
  - 载体：`.tad/logs/violations.log#L2`「Wrote implementation directly」
- 实例2（类型=`缺席致害`，严重度=`高`）：2026-06-10 Alex 越权直执行跨项目破坏性清理。
  - 载体：`.tad/logs/violations.log#L1`「cross-project destructive cleanup directly」

## D11 Execution Mandate

- 判定：`1-留`｜来源：`lite`｜频率：`每单`
- 地板·可缩放：`待判`（理由：价值取决于"一次性精确授权 vs 逐命令审批"哪个更防越权，需非自指实例对比——Epic 旧标签"保留"是对 Alex 自设计机制的偏好，AC13 质询后不默认沿用）
- 防住过什么：3b/3c/P4 实测 mandate 内零可避免运行时询问。
- 删掉会怎样：回到逐命令审批（full 模式）或橡皮图章式审批。
- 删之前先找什么反例：找到"精确 mandate 漏掉越权"的非自指实例，或证明逐命令审批更优。

### 实例

- 实例1（类型=`在场生效`，严重度=`中`）：3b/3c/P4 实测 mandate 内零可避免运行时询问。
  - 载体：`.tad/active/epics/EPIC-20260812-discipline-weight-separation.md#L83`「mandate 内零可避免运行时询问」

## D12 约束准入

- 判定：`4-威慑免死`｜来源：`lite`｜频率：`偶发`
- 地板·可缩放：`待判`（理由：威慑型无实例；需观察约束增长速率才能判地板/可缩放）
- 触发条件（防的不可逆后果）：约束无限膨胀致流程重量失控、无法推广（累积性、难以回缩）。
- 防住过什么：威慑型——靠"存在本身"让约束不膨胀，从没抓到过（膨胀没发生），也没有缺席致害记录（它一直没缺席）。按三类必然落第 3 类写不出一次性触发条件而退场——这是第 4 类的结构补丁。
- 删掉会怎样：约束无定价门槛，无限膨胀到流程重得只有作者本人受得了。
- 删之前先找什么反例：这是威慑型，不能按"有无实例"删——须人工复核：约束增长速度是否真的趋零。

### 实例

（无实例——威慑型，靠存在本身让坏事不发生；见 §4.4 第 4 类定义）

## D13 AC可执行性检查

- 判定：`1-留`｜来源：`both`｜频率：`每单`
- 地板·可缩放：`地板`（理由：设计期一步、成本恒定；若 AC 可执行性可用事后审查替代且不漏，证伪）
- 防住过什么：v1 的 9 条 AC 对敷衍产物 9/9 全绿（实跑证明）——AC 无机械验证命令时，敷衍产物也能全绿。
- 删掉会怎样：不可验证的 AC 进入契约，实现者可敷衍通过。
- 删之前先找什么反例：证明"事后审查"能等价捕获"AC 不可验证"（v1 的 P0-4 已证伪）。

### 实例

- 实例1（类型=`缺席致害`，严重度=`中`）：9 条 AC 对敷衍产物 9/9 全绿（AC 无机械验证命令）。
  - 载体：`.tad/active/handoffs/HANDOFF-20260812-discipline-inventory.md#L515`「9 条 AC 对敷衍产物 9/9 全绿」

## D14 Friction反跳过

- 判定：`1-留`｜来源：`both`｜频率：`偶发`
- 地板·可缩放：`地板`（理由：内置禁令成本近零；若"缺工具就跳过"在无此禁令时也不发生，证伪）
- 防住过什么：设计期专家审查被砍掉后 L0.5 自审替代，AC principal 契约缺陷穿透自审存活至最后一道 gate（缺席致害）。
- 删掉会怎样：缺工具/权限/auth 成为跳过必修步骤的理由，且"自审替代"被合理化。
- 删之前先找什么反例：证明"自审等价于独立审查"（2026-07-30 已证伪）。

### 实例

- 实例1（类型=`缺席致害`，严重度=`中`）：自审替代独立审查，AC principal 缺陷穿透存活至最后一道 gate。
  - 载体：`.tad/evidence/research/2026-07-30-lite-vs-full-quality-comparison.md#L16`「L0.5 自审未抓到，存活至最后一道 gate」

## D15 Ralph Loop自检

- 判定：`1-留`｜来源：`both`｜频率：`每单`
- 地板·可缩放：`可缩放`（理由：验证强度可缩放 build/test/lint 可增减；若机器自检无法随任务分层，证伪）
- 防住过什么：TypeScript 类型检查失败（缺类型声明）是 Blake Layer 1 失败的反复根因——Layer 1 自检拦截实现错误。
- 删掉会怎样：实现错误无机器自检拦截，全推到人工审查。
- 删之前先找什么反例：证明"专家审查"能等价替代机器自检（成本上已不划算）。

### 实例

- 实例1（类型=`在场生效`，严重度=`中`）：TypeScript 类型检查失败是 Blake Layer 1 失败的反复根因。
  - 载体：`.tad/project-knowledge/patterns/ac-verification.md#L24`「TypeScript type-checking failures (missing type declarations)」
