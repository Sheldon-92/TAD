# 防线关系独立复核——分歧裁定

> Step 3 产物。§6.3：一致采纳，不一致两版并列 + 最终取哪个 + 一句理由，只做一轮。

## 复核信息

- 复核者自报 model：deepseek/deepseek-v4-pro
- subagent_type：`general`（opencode 通用 subagent，fresh context；复核者自报 `main-assistant`）——**非 fork**，未继承 Blake 上下文，未以 Alex/Blake 身份启动。
- FILES-READ（复核者实际读过的文件）：仅 `/var/folders/.../tad-p1b/formB.md` 与 `/var/folders/.../tad-p1b/costs.md`，不含 `discipline-inventory`、不含 `defense-graph-blake`（隔离有效）。
- 顺序证据：Blake 边集封存 md5 `77af46bab8617efb98de1cc2a587f11b`，spawn 前已落盘 `graph-blake-pre-review.md5`。

## 成本量级 diff（复核者 COST-TIER vs Alex 裁定值）

| 行 | 复核者 | Alex 裁定值 | 裁定 |
|---|---|---|---|
| D05 门禁 | 机械近零 | 机械一次 | **采用 Alex**：门禁核验闭集判据需读契约/判据表（读写产物），§4.2「需读写产物 → 机械一次」。复核者只按秒数判断，未计「读写产物」维度 |
| D09 配对测试 | 人一次 | 人多轮 | **采用 Alex**：配对测试本质持续往返（blake-lite user-gated AC 单步协议），「1次」数是场次不是轮次 |
| D13 AC可执行性 | 机械近零 | 机械一次 | **采用 Alex**：AC 空跑需实际执行验证命令（读 AC 命令、执行、读写产物），落「需读写产物」一侧；且与 D05 同判据保持一致性 |
| 其余 12 行 | 与 Alex 值一致 | — | 采纳 |

> 复核者与 Alex 值一致 12/15，分歧 3 行全部由 Alex 裁定值胜出（复核者按「秒数」单一轴判断，漏了「读写产物」与「场次 vs 轮次」两个维度）。

## 防线关系 diff（复核者 GRAPH vs Blake 边集）

<!-- BLAKE-EDGES -->
Blake 边集（defense-graph-blake.md，封存 md5 77af46bab8617efb98de1cc2a587f11b）：
- 同根：D01↔D02
- 互补：D04↔D05、D04↔D08、D04↔D14、D04↔D15、D03↔D12、D10↔D11
- 独立：D06、D07、D09、D13

<!-- REVIEWER-RAW-EDGES -->
复核者边集（defense-graph-independent.md）：
- 同根：D01↔D02
- 互补：D04↔D05、D04↔D08、D04↔D13、D04↔D14、D04↔D15、D05↔D13、D10↔D11、D03↔D12
- 独立：D06、D07、D09

<!-- 分歧裁定 -->
分歧仅两处，均为 D13 是否连边：

| 边 | Blake | 复核者 | 裁定 |
|---|---|---|---|
| D04↔D13 | 独立 | 互补 | **采纳复核者（互补）**：D13「删之前反例」=「证明事后审查能等价捕获 AC 不可验证」，而 D13「防住过什么」=「v1 的 9 条 AC 对敷衍产物 9/9 全绿」证明事后审查（专家审查 D04）抓不到 AC 不可验证 → 不能替代 → 互补 |
| D05↔D13 | 独立 | 互补 | **采纳复核者（互补）**：D13（AC 可验证）与 D05（门禁闭集判据）同属机械验证防线，各挡一类失败（AC 不可验证 vs 形式缺陷），删一条验证链缺一环 |

最终采用边集（1 同根 + 8 互补，独立 3 行）：
- 同根：D01↔D02
- 互补：D04↔D05、D04↔D08、D04↔D13、D04↔D14、D04↔D15、D03↔D12、D10↔D11、D05↔D13
- 独立：D06（安全/依赖演进盲区）、D07（教训流失）、D09（真机/真设备验证）

<!-- REVIEWER-PROMPT -->
以下为 spawn 时给复核者的 prompt 全文（含 §4.5 四类定义 + §4.2 六值域与四条消歧规则，只给两个 TMP 路径，禁止读 `.tad/evidence/designs/`）：

```
你是一名独立的复核者，任务是对一份 15 行纪律清单做两类独立判断。你的上下文是全新的，只允许读取下面给出的两个文件，禁止读取任何其他文件（尤其禁止读取 `.tad/evidence/designs/` 目录下任何文件，禁止读取 CLAUDE.md、SKILL 文件、仓库其他路径）。这是隔离要求：你的答案必须完全由这两个输入文件 + 我给出的定义推导，不得参考任何第三方结论。

允许读取的两个文件（唯一输入）：
1. /var/folders/ht/dds194f911zbp3nk0vl3ssr00000gn/T/opencode/tad-p1b/formB.md —— 15 条纪律逐条溯源（每条 `## D01`…`## D15`，段内 `防住过什么`/`删掉会怎样`/`删之前先找什么反例` + `### 实例` 含 `严重度=X`）。
2. /var/folders/ht/dds194f911zbp3nk0vl3ssr00000gn/T/opencode/tad-p1b/costs.md —— 15 行 ×（行号 | 纪律 | 成本）三列表。

任务一：防线关系边集（输出到 GRAPH 节）。关系四类定义（唯一权威）：同根=两行防同一个失败的两个环节（删任一条另一条仍在但覆盖不全）；互补=两行各挡不同失败类（删任一条另一条完全挡不住）；冗余=两行挡的失败类相同且覆盖相当（删任一条另一条基本挡得住）；独立=无上述任一关系。失败类从「删掉会怎样」提取。互补与冗余不许混填。判冗余前检查三行有无反证词（漏掉/穿透/不等价/已证伪/量化…处/挡不住），有则禁止判冗余。填独立须写明该行失败类。

任务二：15 行成本量级（输出到 COST-TIER 节）。取值域（有序）：零 < 机械近零 < 机械一次 < agent一次 < 人一次 < 人多轮。判据：零=无运行时动作；机械近零=确定性程序单次<10秒无LLM；机械一次=确定性程序单次≥10秒或需读写产物无LLM；agent一次=1–2次LLM/subagent调用无人参与；人一次=需人1次输入或裁定；人多轮=需人≥2轮往返。四条消歧规则：①复合取max（人>agent>机械>零）②多次同类不升档③档位相关按full填注lite④「谁执行」不在成本列时按实际执行主体判定。agent一次特指LLM/subagent调用，纯shell执行/文件写入/确定性程序都归机械档。

强制输出四锚：FILES-READ（实际读过的文件绝对路径）/ 套不上（可答无）/ GRAPH / COST-TIER。最后给 REVIEWER-MODEL 与 SUBAGENT_TYPE。只判断不写代码不改文件。
```

> 注：prompt 中未出现任何紧邻的 `D<nn>→D<nn>` / `D<nn>->D<nn>` 边（AC7 机械正则 `D[0-9]{2}\s*(→|->)\s*D[0-9]{2}` 命中数 = 0，见 V9）。
