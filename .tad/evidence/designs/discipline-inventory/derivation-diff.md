# derivation diff（Blake 推导 vs Alex 对照表）

> §5 交付顺序第 4–5 步。derivation-t1.md 的 md5 已封存（`cbc8e61c358627c5d271f07e5e9aa830`），报人类后转交对照表，此后 derivation-t1.md 不得变更（AC15）。

## 1. 总览

| 列 | 性质 | diff 结果 |
|---|---|---|
| 判据 | 投影 | 完全一致（C1=12 / C3=2 / C4=1 / C2=0） |
| 严重度 | 投影 | 完全一致（高=7 / 中=5 / 未知=3 / 低=0） |
| 证伪条件 | 投影 | 完全一致（10 行有证伪句 / 5 行待判：D07 D08 D09 D11 D12） |
| 成本量级 | 新判断 | 12 行一致，2 行不一致（D12 D13），1 行 Alex 改判（D09） |

三个投影列零分歧，唯一的新判断列有分歧——与 §1.4 的分类预测一致。

## 2. 成本量级分歧逐条

### D09 配对测试：Blake `人一次` → Alex `人多轮`（Alex 改判）

- 我推：字面「1次」→ 人一次，标注歧义。
- Alex 裁定：`1次人机配对` 的「1次」数的是**场次不是轮次**；配对测试按定义就是持续往返 → `人多轮`。
- 结论：**Blake 推错**（把「1次」误读为轮次）。采用 Alex 值。

### D12 约束准入：Blake `agent一次` → Alex `机械一次`

- 我推：约束准入由 agent 执行（往台账追加行、填三项）→ agent一次，标注歧义。
- Alex 裁定：台账追加是文件写入，不是 LLM/subagent 调用。判据必须落在「成本本身是不是一次 LLM 推理」上。
- 结论：**Blake 推错**（读规则分歧：把「agent 发起」当成了 agent一次）。采用 Alex 值。

### D13 AC可执行性检查：Blake `agent一次` → Alex `机械一次`

- 我推：空跑 = AC dry run，agent 逐条确认 → agent一次。
- Alex 裁定：空跑是 shell 执行，不是 LLM 调用。与 D12 同一规则问题。
- 结论：**Blake 推错**（同 D12）。采用 Alex 值。

### D05 门禁：双方 `机械一次`（一致，我标了歧义）

- 一致。Alex 确认：清单核验要读产物，落在「≥10 秒或需读写产物」一侧，不是机械近零。歧义消解，采用 `机械一次`。

## 3. 裁定后的最终分布（AC16a 会验）

- 零=2（D10 D14）· 机械近零=1（D06）· 机械一次=4（D05 D12 D13 D15）· agent一次=3（D04 D07 D08）· 人一次=3（D02 D03 D11）· 人多轮=2（D01 D09）
- 6 桶 ≥ 4 ✅；D10/D14 同为 零、其余无 零 ✅；D01 逐字 `人多轮（lite: 人一次）` ✅

## 4. 遗留信号（Alex 提示，Step 2 留意）

Alex 指出：我标了 D12 歧义却没标 D13，而两者是同一规则问题——同一条判据在两行上一个触发警觉、一个没有，这类不对称本身是信号。Step 2 填防线关系时留意同类不对称。
