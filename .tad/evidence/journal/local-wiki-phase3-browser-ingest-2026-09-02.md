# Local Wiki Phase 3 execution journal

- Browser capture patches that temporarily replace global APIs need an ownership token and an
  idempotent cleanup path: a timeout/error must restore only wrappers installed by this request.
- A hyphenated CLI filename remains a good user-facing command name, but tests must load it by
  file location rather than normal Python module import.
- The non-Git extension boundary is recoverable only when the original bytes and both unified
  patch directions are captured before independent review.
