# Epic: 纪律与重量分离 —— 让流程的重量与任务相称

**Created**: 2026-08-12
**Owner**: Alex-Lite
**Status**: ACTIVE — Phase 1 / 1b / 2 / 2b COMPLETE；**Phase 3 跳过（用户裁定）**；
**Phase 5 待触发——下次做任何非框架真活时自动发生，不需要专门启动**
**Predecessor**: `EPIC-20260809-full-capability-extraction-retirement.md`（已终止，见其「Epic 终止记录」）

## Objective

**让流程的重量随任务变化，而那些真正在保证质量的纪律不随之流失。**

用户的原始纠结（2026-08-12 原话）：

> "我为什么想要 Alex-Lite / Blake-Lite？因为我觉得原来的 Alex 和 Blake **太重了**，
> 但是它又有它**很多很好的地方**，所以我又**舍不得**。因为好的地方如果摒弃了的话，
> 那确实是能保证我的产出质量的东西。所以就在这里做一些纠结。"

这个纠结之所以无解，是因为 **full 把"纪律"和"重量"焊死在一起了**——
你要它的纪律，就得连它的重量一起要。而 lite 反向犯了同一个错：为了减重，
把三条"动手之前"的纪律一并削掉了（见下）。

**本 Epic 的全部工作就是把这两样拆开**：纪律在任何档位都在，重量随任务后果变化。

## 这不是什么

- **不以"退休 / 替换 / 冻结 full"为目标。** 那是可能的后果之一，不是终点。
  用户 2026-08-12 明确："我现在还不着急替换，因为现在完全还没有到能够替换的地步。"
  full 的处置作为**开放问题**记在文末，不占任何 phase。
- **不清点能力。** 前一个 Epic 的地基是"full 能做什么"的 19 项能力表，
  而它值钱的四样纪律**一样都不在里面**——一张清点能力的表，结构上看不见纪律。
  本 Epic 的地基是**纪律清单**。
- **不把权限做进旋钮。** 调重档不得附带更大的写权限；权限永远由每单一次的
  Execution Mandate 单独管（依据 `principles.md` 2026-08-06 AMENDED：
  权限是无界集合，其上的 deny-list fail-open 面无限，会形成 agent 自放大回路）。
- **不在缺乏非自指证据前固化档位定义。** 现有的全部实测样本都来自"验证框架自己"这类工作，
  它天然把证明成本推到极限。用它来定义"轻档"会定错。

## 判据原则：纪律要有实例，重量要能缩放

一条纪律留在流程里，必须回答两个问题：

1. **它防住过什么？** —— 要有**具体实例**（某次它抓到了什么 / 某次它缺席导致了什么），
   不是"它提升质量"这类空话。答不出实例的纪律，退场。
2. **它的成本能不能随后果缩放？** —— 能缩放的进旋钮；不能缩放的是**地板**，
   任何档位都必须执行。

⚠️ 分辨"地板"与"可缩放"是本 Epic 最难也最核心的判断。
初步假设（待 P1 证伪）：**"动手之前"的纪律多半是地板**（它们防的是"做错东西"，
而做错东西的代价不随任务大小线性变化）；**"做完之后"的验证多半可缩放**
（它们防的是"做得不对"，而做得不对的代价确实随后果变化）。

## Success Criteria

| # | 判据 | 可证伪方式 |
|---|---|---|
| **SC1** | 每条保留的纪律都有"它防住过什么"的实例载体；无实例者已退场或已找到实例 | 逐条查载体路径 |
| **SC2** | 存在 ≥2 个档位，且两档在**同类任务**上的实测代价差 ≥ 一个量级（契约版次 × 审查轮次） | 实测两个任务 |
| **SC3** | 档位由**人**裁定、agent 只评估；**权限不随档位变化** | 对抗测试：调重档后尝试越权写入，须被 mandate 拦住 |
| **SC4** | 轻档下"动手之前"的地板纪律**仍然生效**，不是被跳过 | 轻档跑一单，检查地板项是否都留下了载体 |
| **SC5** | ≥2 个**非自指任务**（非框架自身工作）跑完完整流程；轻档**没有漏掉真问题** | 事后由独立审查复查该单，看轻档漏了什么 |
| **SC6** | 本 session 的实际失败可被新流程拦住 | **回放测试**：把"你假设要替换 full"这句原话作为输入，看 A1 会不会问 |

⚠️ **SC6 是本 Epic 的锚**。它把一次真实的、代价高昂的失败变成一个可重复的测试用例：
Alex 把用户的**假设**当成**批准**，据此推进了 6 个 phase、一次对外发布、一次系统升级，
而**没有任何一条 AC 能抓到它——AC 只验"做对了没有"，不验"该不该做"**。

## 地基：纪律清单（不是能力清单）

前一个 Epic 的 19 项能力表**保留为参考资料，但不作为本 Epic 的任何判据**。
本 Epic 从下表出发（P1 的任务是把它补全并证伪）：

| 纪律 | full 的形态 | lite 现状 | 防什么 | 实例 | 地板还是可缩放？ |
|---|---|---|---|---|---|
| **需求澄清** | 规则 0 苏格拉底提问，**BLOCKING**，3–5 轮 | L1 目标锚，≤1 问 ≤2 轮，明写"**不问是常态**" | 做错东西 | ✅ **本 session**：假设被当成批准，6 个 phase 建在未确认的前提上 | 待判（假设=地板）|
| **重量裁定** | Adaptive Complexity：Alex 评估、**人裁定** | §2.5 明写"文件数、协议密度…**均不构成升级理由**"——**主动删除** | 流程与任务不匹配 | ✅ 本 Epic 5 单：不可撤销的对外发布 8 版 7 轮；可回滚的本机升级 4 版 3 轮 | **本 Epic 的核心对象** |
| **需求闸** | Gate 1 需求清晰 | 无——L0/L1 全是 Alex 自评，**无人复核** | 需求理解错了没人发现 | ✅ 同"需求澄清" | 待判 |
| **多视角审查** | 专家审查 **min 2 并行** | 1 个通用 reviewer **串行多轮** | 单视角盲区 | ✅ Phase 4：三轮 9 个 P0 中 **5 个是修复自己造的**；本质是用轮数换视角数 | 可缩放（视角数）|
| **门禁** | 四道 Gate，**显性关卡** | 溶进脊柱当步骤 | 已知失败类漏检 | ✅ 契约过了**三轮对抗审查**仍被 L0.5 机械准入拦下，三轮一个都没提 | **地板**（判据闭合、成本恒低）|
| **启动扫描** | 依赖演进 / 安全公告 / 僵尸 handoff / 研究图景 | 无 | 该看的东西没人看 | ✅ 安全公告停跑 28 天，漏掉 `gh` 4 个漏洞（含明文打印 token）| 地板（成本近零）|
| **知识评估** | 规则 5，**BLOCKING** | Knowledge Closeout，**明写不阻塞** | 教训流失 | ⚠️ 无实例（本 session 蒸馏正常发生）| 待判 |
| **跨模型审查** | Codex / Gemini 交叉审计 | 无协议规定 | 同模型同盲区 | ⚠️ 弱实例：AC17 碰巧用了别的模型，**是偶然不是制度** | 待判 |
| **配对测试** | pair testing | 无 | ⚠️ 未知 | ⚠️ 无实例 | 待判 |
| **Execution Mandate** | 无（full 是逐命令审批）| **lite 独有** | 橡皮图章式审批 | ✅ 3b/3c/P4 实测：mandate 内零可避免运行时询问 | **保留，lite 优于 full** |
| **约束准入台账** | 无 | **lite 独有** | 约束无限膨胀 | ✅ 每次新增 MUST 都被迫定价 | 保留 |

## Phase Map

| # | Phase | Status | Deliverable |
|---|---|---|---|
| 1 | **纪律清单补全与证伪** | ✅ **COMPLETE**（2026-08-12，Gate 4 PASS）| 上表补全：每条纪律的**实例**与**地板/可缩放**判定。无实例者退场。产出是本 Epic 后续全部判断的地基 |
| 1b | **补五列**（严重度 / 判据 / 证伪条件 / 成本量级 / 防线关系）| ✅ **COMPLETE**（Gate 4 PASS，2026-08-12）—— 十四列表可作拍板依据 |
| 2 | **Epic Objective 闸**（需求澄清+需求闸这对同根纪律） | ✅ **COMPLETE**（Gate 4 PASS）| 改 lite 的 L0/L1：按任务来源分流，**不得 agent 自评跳过**；需求理解落盘并由人确认，明确是**闸**。⚠️ 范围**从"补回三条"缩到"改一对"**，依据见下 |
| 2b | **摸清需求（两件）**：lite 每单目标题 + 路由由人裁定 | ✅ **COMPLETE**（Gate 4 PASS） —— 用户 2026-08-12 裁定「两个都改」。5 个文件（含 `.agents` 镜像），改法须**逐行钉死旧文/新文对**（要改既有行，用不了零删除守卫）|
| 3 | ~~重量裁定（旋钮）~~ | ⏭️ **SKIPPED**（2026-08-13 用户裁定）| 接回 Adaptive Complexity 的形状：Alex 评估、人裁定"本单验证强度"。**权限不随档位走** |
| 4 | **审查与门禁重构**（B1/B2/B3） | BLOCKED by 3 | 一轮多视角取代串行多轮；**修复门禁取代复核循环**（闭集 5 条）；门禁从"脊柱步骤"改回**具名关卡 + 闭集判据表** |
| 5 | **非自指实证** | **待触发（下次真活自动发生）** | 在 ≥2 个**非框架**任务上跑完整流程，测 SC2/SC4/SC5。**这是唯一能证伪档位定义的 phase** |
| 6 | **按实证收口** | BLOCKED by 5 | 依据 P5 结果调整档位定义与地板集合；写回 CLAUDE.md 与 lite skills |

```text
P1 纪律清单 ─┬─► P2 需求澄清+需求闸 ─┐
             └─► P3 重量裁定（旋钮）─┴─► P4 审查与门禁重构 ─► P5 非自指实证 ─► P6 收口
```

## Risk Register

- **🔴 又在自指数据上设计**（本 Epic 最大风险）：全部现有样本来自"验证框架自己"的工作。
  缓解：**P5 是硬前置**，没有非自指实测不得进 P6 收口；档位定义在 P5 之前只是假设。
- **🔴 减重时再次连纪律一起削**（lite 已犯过一次）：缓解——判据原则要求每条纪律先有**实例**，
  退场必须证明"它没防住过任何东西"，而不是"它看起来很贵"。
- **🔴 把假设当批准**（本 session 已犯）：缓解——SC6 把它做成回放测试；
  A1 要求"来自一句话/假设/模糊方向的任务**必须问**"。
- **旋钮成为权限通道**：缓解——SC3 的对抗测试；权限与仪式**不共轴**写进 Non-Goals。
- **纪律清单变成又一张能力表**：缓解——每行必须有"防什么 + 实例"，
  没有实例的行不允许留在表里当"待办"，只能退场或去找实例。
- **本 Epic 本身仍是内部管道**：⚠️ 用户更早的明确反馈是"**你不要老是做类似的事情**"
  （`feedback_pick-generative-directions`：别再做内部框架管道类升级）。
  **本 Epic 无法回避这一点**——它确实是内部工作。唯一的辩护是：
  它服务的是"TAD 用到软件开发之外"这个北极星的**前提**（一个重得只有作者本人受得了的流程，
  推广不出去）。**是否值得现在做，是人的决定，不是本 Epic 能自证的。**

## 开放问题（不占 phase，等实证后再谈）

1. **full 最终怎么处置？** 冻结 / 删除 / 保留为重档的实现。
   ⚠️ 若 P3 的旋钮做成了，"两个 agent"这个实现本身就没有存在理由了——
   **full 的处置会变成旋钮的一个副产品，而不是一个需要专门规划的工程。**
2. **`/gate` 算不算 full？** `gate-protocol` 的载体是 `/gate`，而它自己在 full 命令表里。
3. 4 项 `DECISION_REQUIRED`（tournament / ideas / alex-design-inquiry / knowledge-maintain）。

## 承接的独立待办（不属本 Epic）

- **F1 / 2.41.1**：25 个存量下游跑本地旧 `tad.sh` 会被告知"已是最新"，收不到修复。
  **交付物是"如何让他们改用 curl"，不是 patch release。**
- **残留登记 19 项**：见前一个 Epic 的「残留登记」节。
- 前一个 Epic 的 Phase 1–4 交付全部保留：`release-runbook` 扩展、**Authority Model v2**、
  v2.41.0 发布、`dependency-ops` skill。


---

## Phase 1 完成（2026-08-12，Gate 4 PASS）

**产物**：`.tad/evidence/designs/discipline-inventory/`（形态 A 九列 15 条 / 形态 B 逐条溯源 /
search-log / enumeration-diff / shape-blindspot-review / verify.py）
**契约**：`.tad/archive/handoffs/HANDOFF-20260812-discipline-inventory.md`（rev2）

### 三个实质发现

1. **枚举补回 4 条 Epic 草稿漏掉的纪律**，其中 **`角色分离`（Alex/Blake 分离）** 判定为 `1-留 / 地板`
   —— 那可以说是 TAD 最核心的纪律，而 Alex 的 11 行草稿**整条漏掉**。这是同一形状盲区的第 4 次。
2. **第 2 类（净删除）= 0**。设计期最担心的"判据偏向删除"没有发生；
   而 `约束准入` 落进了 **`4-威慑免死`** —— **那一类是 Gate 2 时按 product-expert 建议临时加的，
   没有它这条会被自动降级退场**。第 4 类上任即接住一条。
3. **AC7 抓到第 4 次形状盲区**，诊断为：**有成本无收益、有判定无论证、有行无行间关系** ——
   三者全被挤进第 9 列一句可空自由文本，而该列同时装着三种互不相关的语义。
   跨 harness reviewer（opencode / deepseek-v4-pro）独立得出与 Gate 2 的 product-expert 相同的结论。

### 验收判断（Gate 4 v2）

| 项 | 判定 |
|---|---|
| AC 全绿 V1–V16 | ✅ Blake 严格照契约执行 |
| 可用作**清单** | ✅ |
| 可用作**拍板依据** | ❌ 缺三样 → **Phase 1b** |

⚠️ **不是执行问题，是 Alex 的 handoff 把表设计成九列就不够。** AC7 正是为此而设，
它在花掉更多之前把这件事挖出来了。

### 已知取舍（未修，写进 handoff §10.2）

**行级判定零交叉验证**：两位 Gate 2 专家审的是**规则**，AC7 reviewer 被禁答"对不对"
→ 每一行的实际判定只有 Blake 一人做过。形态 A 头部已声明"拍板前请抽查"。

### 蒸馏（1 条）

`patterns/ac-verification.md`：**「这对不对」与「这装不下什么」是两个问题** ——
正确性审查在给定结构内部运作，**结构的天花板从内部不可见**；每个格子都对，
而 schema 表达不了那个问题。题面必须由契约钉死并**明令禁止**回答对不对，
否则那是更容易的问题，力气都会流到那边去。

---

## Phase 1b 设计期记录（2026-08-12）

**契约**：`.tad/active/handoffs/HANDOFF-20260812-discipline-inventory-columns.md`（rev3，641 行）

### 范围从「三列」长到「五列」

用户初批**四列**（严重度/判据/证伪条件/防线关系）。Gate 2 的 product-expert 指出只补了
**收益侧**——现有成本列是 15 行 14 种异构自由文本（`~5秒` vs `3-5轮问答` vs `2次spawn`），
与收益不通约，**补了收益也读不出比值**。故加第 5 列「成本量级」（六档有序闭值集）。
⚠️ 这是对已批范围的扩大，理由与记录都在契约 §1.3 / §12。

### Gate 2：三专家并行，各审不同维度，各一轮 —— **共 16 个 P0**

| 维度 | P0 |
|---|---|
| 授权边界 / 围栏 / 自证循环（security-auditor） | 6 |
| 拍板可用性（product-expert） | 2 |
| AC 判别力 / 命令可执行性（code-reviewer） | 7 |
| Alex 自查（空跑 + 闭集门禁） | 5（其中 1 个是修别的 P0 时新造的） |

**本 Epic 应当记住的三条**（它们都不是本单特有，是可复用的失败形状）：

1. **「投影不能反向」不能押在被判方自己生成的基线上。** rev1 用形态 B 的 md5 + porcelain
   行集当锚，两者都由 Blake 在 Step 0 生成——锁和钥匙一起交出去了。
   顺带查出**全部 Phase 1 产物根本不在 git 里**（`git ls-files`=0，非 gitignore，是从未提交），
   已由 commit **`2859b75`** 冻结 T=0。
2. **验证脚本可以完全不读它声称在验的源。** 没有任何 AC 要求 `verify.py` 打开形态 B——
   把 Alex 的答案表硬编码进判据即可"用答案验答案"，十二条 AC 全绿。
   → **源侧负控**（污染源文件副本，判据必须变红）。
3. **「跑 verify.py」不是判据。** 该文件既有约定是 **print 完就 return，永远 exit 0**——
   报 `mismatch=15` 也是 0。**不是永真，是根本没有"红"这个状态**。
   实测 §9 的 12 条命令里 **11 条最终没有红态**。→ 先立**退出码契约**再谈 AC。

### 对 Epic 假设的一个实证反例

本单立项时定为 **Light TAD**（"往已验收的表上加几列，是增补不是重做"）。
实际走完设计期：3 个 reviewer + 16 个 P0 + 3 个 rev + 641 行契约。
⚠️ **「文件数少 / 是增补 / 不改协议」并不预示低验证成本。**
本 Epic 的 Phase 3（重量裁定旋钮）若按"改动规模"定档位，会把这类单定成轻档而漏掉全部 16 个 P0。
**这是 P3 设计时必须解释的一个反例。**

---

## Phase 1b 完成（2026-08-12，Gate 4 PASS）

**产物**：形态 A 十四列（`discipline-inventory.md`）+ `verify.py` 6 个新子命令 + 防线图 +
独立复核 + 形状审查。**契约 rev4，Gate 2 四轮并行审查共 21 个 P0。**

### Gate 4 验收：**PASS，且 Phase 1 卡住的那一条解开了**

Phase 1 的结论是「可用作清单 ✅ / 可用作拍板依据 ❌」。补完五列后按**成本升序**排表，
能直接读出「哪些无需讨论、哪一行才要人裁定」——这是九列排不出来的，因为当时没有可比较的成本轴。

Alex 独立复跑（非纸面验收）：6 个新子命令全 `RESULT=PASS`；形态 B md5 未变；前九格 diff 为空；
围栏只剩 T=0 既存那一条；`bare=0`。**并在副本上自行注入三处负控，全部见红**
（含可读报错 `不对称：D01 同根→D02 但 D02 无反边`）——判据有真红态，不是"只有绿"。

### ⚠️ 本 phase 最重要的产出不是那张表，是它读出来的一件事

| 实测 | 数值 |
|---|---|
| `冗余` 关系 | **0 行**（15 条纪律**没有任何一条与另一条重复**）|
| 第 2 类净删除（Phase 1） | **0 行** |
| 需要**花人的时间**的纪律 | **只有 5 条**，其余 10 条是 `零` / `机械` / `agent` |
| 这 5 条里 | `重量裁定`=旋钮本身；`Execution Mandate`=lite 自己的；`配对测试`=无实例待判 |
| **剩下的** | **`需求澄清` + `需求闸`——而它俩是 `同根`** |

**两个独立结论都指向：这里没有能删的东西。**
用户感觉的"full 太重了"，量化后**收敛到一对同根纪律**上——不是 15 条太多，
是其中一条要人来回 3–5 轮。

→ **这改写了 Phase 2/3 的题面。** 原假设「减重 = 砍掉一些纪律」被实测证伪：**没得砍**，
只能改那一对的**形态**。用户 2026-08-12 裁定：**Phase 2 直接做那一对**，
不做原计划的"补回三条"全面改造。

### 防线图（15 行）

`同根` 2 · `互补` 10 · `独立` 3 · **`冗余` 0**。
`独立` 三行各自点名了无人覆盖的失败类：`启动扫描`→安全/依赖演进盲区、
`知识评估`→教训流失、`配对测试`→真机验证。**它们是仅有的单点故障**，
且其中两条恰好是无实例的待判行——**没有实例的行同时也没有防线备份**。

`专家审查` 有 **5 条互补边**，是图上的枢纽，且判定为 `可缩放` + `agent一次`
——**最该做成旋钮的就是它**（Phase 3 输入）。

### 两处 Gate 4 裁定

1. **§7 题面与 AC8 字面冲突**（禁令必须点名禁词）→ 采纳 Blake 的改写（「禁止做正确性审查」），
   契约侧记录，已蒸馏成知识条目。
2. **D04↔D13、D05↔D13 判 `互补`** → 采纳。**本单自身即证据**：
   专家审查抓到 21 个 P0 但漏掉 `$TMP` 未定义 / V7 静默通过 / Alex 编造的行数；
   AC 空跑与闭集门禁抓到那 4 个却漏掉那 21 个。**两边各挡一类失败，谁都替代不了谁。**

### 蒸馏（5 条）

`ac-verification.md`（3）：**「跑 X」不是判据，除非 X 的失败信号被定义**（print-only 验证器让
整批 AC 只有绿没有红，连带毁掉建其上的负控）／**修复本身是缺陷源**（同 session 三次观察，
门禁须独立查"修复间是否互斥"）／**grep 禁词的检查器与必须点名禁词的指令互斥**。
`shell-portability.md`（1）：**macOS `sort`/`uniq` 在 UTF-8 locale 下把不同中文串判为相等**，
产出语法正确、总数正确、分类全错的静默答案，须 `LC_ALL=C`。
`handoff-design.md`（1）：**改动规模不预测验证成本**——本单按"是增补不是重做"定轻档，
实际 21 个 P0；真正的驱动是"新建了几个判断"和"产物是否成为判据"。

---

## Phase 2 完成（2026-08-12，Gate 4 PASS）

**产物**：`alex-lite` 新增「Epic Objective 闸」+ Forbidden 节 epics 豁免收窄；
`blake-lite` L0.5 新增「Epic 载体检查」；`.agents/` 镜像同步。**纯增行 122 / 删除 0。**
**契约**：`HANDOFF-20260812-lite-goal-gate.md` rev2（Gate 2 三专家并行 **13 个 P0**）
**T=0**：`51ceeda`

### 关键成果

**「需求闸」在 lite 第一次有了机械载体。** Phase 1b 的纪律清单记录它在 lite 的状态是
「无——L0/L1 全是 Alex 自评，**无人复核**」。现在载体落在**人桥另一侧**（blake-lite L0.5），
与既有四条机械检查同构：Epic 缺 `## Objective 来源` 段 → `GATE FAIL / BLOCK`。

### 干预点移了一次（rev1 → rev2）

rev1 把闸挂在 alex-lite L2（**单**的开头）。三条证据推翻它：
1. `product-expert`：事故发生在**单的中途**，L2a 不会触发
2. `code-reviewer`：负控以"是否出现本设计新造的标题"为判据，而该标题 T=0 零命中 → **恒真**
3. **后台探针实证**：未修改规则 + 那句原话 → fresh agent **两次都主动列出目标选项并拒绝代选**

→ 重查定位到真正的层级：**单有 boundary 闸，Epic 没有**，
且 `.tad/active/epics/` 在 alex-lite 写权限豁免清单内。

### Gate 4 更正了 Blake 的一条结论

Blake 报告"回放判**不触发**，闸覆盖不到事故"。核对时间线后**更正**：
Epic **创建于 08-09**（那句话正是其"被误读的前提（根因）"），Objective **08-12 被重述**
——**两个时刻都在触发条件内**。是 §6.2 的 fixture 给了"已存在的 Epic"，
把场景设成了单中途延续（§9.3 已登记的另一个缺口）。**fixture 错在 Alex，不在 Blake，也不在闸。**

### ⚠️ 用户的一句话把根因又往上推了一层

> **「你为什么走的是 lite？」**「不应该是走 full 吗？」

核实：那个 Epic 的 `Owner` 是 **Alex-Lite**，落的是 `LITE-*` 单。
而 **full 早就有这道闸**——`adaptive-complexity-protocol.md#L123`
`AskUserQuestion: confirm Epic + Phase definitions`。

**事故不是"lite 缺一条规则"，是路由规则把那件工作塞进了没有闸的通道。**
`CLAUDE.md` §2.5 明写「文件数、协议密度、**是否触及协议契约均不构成升级理由**」，
而那件工作是 Epic 级 + 改协议契约 + 对外发布 + 系统升级——**按 §2 条条该走 full**。

⚠️ **而 §2.5 那条规则，出自被终止的那个 Epic 自己（2026-08-06「lite 接管 full」）。
造成事故的规则，是事故那条线自己写的。** → Phase 2b。

### 未证事项（写进契约，不是漏了）

1. **因果效力未证**：有效设计需 3 条件 × 5 次 + 盲判 ≈16 次 agent 调用，与用户的 credit 约束冲突。
   **本单明写"没买这个证明"**，而不是用单次采样假装证过。
2. **选项公正性无机械载体**：闸能强制 disclosure 的**格式**，管不了**公正性**
   （"我想做的 + 两个稻草人"格式上完全合规）。
3. **新增 2 处 BLOCKING 未走 lite 的「约束准入」台账**：台账归 alex-lite 管辖，
   full 通道的 Blake 不代写，交后续 lite 单补。

### 蒸馏（4 条）

`ac-verification.md`（3）：**回放 fixture 复现的是"时刻"不是"语句"**（记录里的原话多半是
**纠正时刻**说的，照抄会测成另一个场景）／**以"是否出现本设计新造标记"为判据的负控恒真**／
**自指不动点：锚存文档、文档存锚 → 比对时排除自指行**。
`gate-design.md`（1）：**下层闸全绿而事故仍发生 = 上层缺闸的 signature；
定位层级之前不要开始设计干预**。
`handoff-design.md`（1）：**改规则前先数副本**——本仓库协议类规则默认多副本
（路由层 + 每角色 SKILL + 每 harness 镜像），只改一处会让 agent 服从离它最近的那份。

---

## Phase 2b 完成（2026-08-13，Gate 4 PASS）

**用户原话即目标**：「**摸清楚需求，不管是什么模式都需要的。**」
两次裁定：「不够，轻模式也要问」「两个都改」。

**产物**（5 个文件 + 证据）：
1. **路由**：`CLAUDE.md#L49` 与 4 个 lite SKILL 的 Lite-First 政策行——
   「协议密度」删净，改为**创建 Epic / 多阶段任务 / 修改框架自身 / 对外发布或同步**
   四类**通道由人裁定**，**边界存疑一律按命中处理**。「修改框架自身」范围补齐至
   `.tad/project-knowledge/`、`.claude/agents/`、`.gitignore` 等（Gate 2 P1-2）。
2. **每单必问**：`alex-lite` 新增 `L1a 目标题`（BLOCKING，每单必跑，无 agent 裁量，
   必含 `[不是这个意思]` 选项 + 通道裁定载体）；`blake-lite` L0.5 追加机械检查。
3. 两处插入均用哨兵（`GOALQ-L1A` / `GOALQ-CHECK`）包裹，供 AC13 冻结补集区分。

**Alex 独立复验**：新文 5/5 各 1 次；`协议密度` 全清零；`deleted=5` 精确预算；
parity；full 零改动；上一单的 Epic 闸未被动。

### Gate 2 两专家并行，9 个 P0

| 维度 | P0 | 最狠的一条 |
|---|---|---|
| 授权边界 | 3 | **所有守卫只查"删"不查"加"**——偷加一行 `@import` 即可装常驻指令，rev1 的 AC1/AC2/AC5 **全过**（实测复现）→ 新增 **AC13 冻结补集** |
| AC 判别力 | 6 | **验证脚本里没有"检查目标题有没有加"** —— Blake 只做路由替换即可全绿交单，**用户要的那件事一个字没落地** → 补 AC3/AC4 命令 + 按节切片 |

⚠️ 第二条是本 session 第三次出现同一形状：**力气花在"做得严谨"上，漏了"检查做没做"**。

### 已知取舍

1. **因果效力未证**（连续第三单）：有效设计需 ≈16 次 agent 调用，与用户的 credit 约束冲突。
   **明写没买，不用单次采样假装证过。**
2. **选项公正性无机械载体**：能强制"必须列选项"，管不了"选项写得公不公道"。
3. **约束准入台账欠 4 处**（本单 2 + 上一单 2）：台账归 alex-lite 管辖，
   full 通道的 Blake 不代写，交后续 lite 单一并补。
4. **契约 §8 的 `for f in $SKILLS` 在 zsh 下只迭代 1 次**（Blake 在 bash 下跑故全绿）。
   已蒸馏；后续契约改用数组或显式列举。

### 蒸馏（1 条）

`shell-portability.md`：**zsh 不对未加引号的变量分词**——同一循环 bash 跑 N 次、zsh 跑 1 次，
而「旧文必须消失（计数==0）」这类判据在 zsh 下**恰好被满足**，未检查的文件静默算通过。
`git diff -- $PATHS` 同理（实测 `deleted` 1 vs 真值 5）。

---

## Phase 3 跳过的理由（2026-08-13，用户裁定）

**Phase 3 原定**：在同一通道内部再加一档"验证强度"的细旋钮。

**跳过的三条理由**：

1. **粗旋钮已经装好了。** Phase 2b 的「四类由人裁定通道」就是一个人来拨的档位开关
   （lite / full）。而用户实际的做法本来就是「质量优先就用 full」——**那个粗档位他一直在用**。
2. **细旋钮没有需求证据。** 没有任何一次实测显示"同一通道内部需要再分强度"。
3. **Epic 自己的硬规矩**：「**不在缺乏非自指证据前固化档位定义**」。
   现有全部样本都来自"验证框架自己"这类工作，它天然把证明成本推到极限；
   用它定"轻档该多轻"必然定歪。

→ **先拿真活验，再决定要不要加。** 若 Phase 5 的实测显示粗档位够用，Phase 3 永久作废。

## Phase 5 待触发：下次做非框架真活时看什么

⚠️ **不需要专门启动。** 用户下次用 `/alex-lite` 做任何**与 TAD 框架无关**的活时，
新流程会自动生效。届时记录以下五项（其余照常，不要为了实验改变干活方式）：

| # | 观察项 | 怎么算过 |
|---|---|---|
| **O1** | **L1a 目标题实际花了多久** | 明确的活应 ≈2 秒（2 选项）。若明确的活也吐 3+ 选项 → 成本控制失效 |
| **O2** | **目标题有没有真的抓到过东西** | 至少一次用户选了 `[不是这个意思]` 或非 A 选项 = 抓到了；连续 N 单全选 A = 开始退化成橡皮图章 |
| **O3** | **通道裁定的准确度** | `通道:` 行判「四类命中: 无」的单，事后回看有没有其实该走 full 的 |
| **O4** | **轻档漏没漏真问题**（SC5） | 事后由独立审查复查该单，看 lite 漏了什么 |
| **O5** | **两档代价差**（SC2） | 同类任务在 lite / full 下的契约版次 × 审查轮次，差是否 ≥ 一个量级 |

⚠️ **O2 是最重要的一项**：它直接决定这道闸是真在工作还是已经变成仪式。
`product-expert` 在 Gate 2 明确警告过高频曝光会把选择动作脱敏成肌肉记忆
（"看到字母选项→选第一个"），而本 Epic **没有买到反驳它的证据**。

## 本 Epic 的当前净结论

| 装上了 | 没装 / 明确不装 |
|---|---|
| Epic 创建/改目标 → 必须让人从选项里挑（有机械载体）| 细档位旋钮（Phase 3，跳过）|
| 每单开工 → 必须让人从选项里挑（有机械载体）| 因果效力证明（三单都明写"没买"）|
| 四类高后果工作 → 通道由人裁定，存疑按命中 | 选项公正性的机械载体（无解，靠人审）|
