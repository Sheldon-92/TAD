# Epic: 让 Alex 和 Blake 轻盈起来 —— 只减加载与形式

**Created**: 2026-08-13
**Owner**: Alex（full 通道）
**Status**: ACTIVE
**Predecessors**（均已冻结归档 2026-08-13）:
`.tad/archive/epics/EPIC-20260804-lite-as-tad-body.md` →
`EPIC-20260809-full-capability-extraction-retirement.md` →
`EPIC-20260812-discipline-weight-separation.md`

---

## Objective（用户 2026-08-13 从选项中裁定）

**只减 Alex 和 Blake 的加载与形式，纪律一条不减。**

终点是两个可机械测量的数，不是judgment：

1. **激活即付**从实测 **107.7K tokens** 降到 **Layer 0**（在用户说第一个字之前读了多少）
2. **约束按类别计数逐类不变**（`MUST` / `MANDATORY` / `VIOLATION` / `forbidden` / `NOT_via_alex_auto`）

用户原话（2026-08-13）：

> "Alex 和 Blake 其实并没有问题，他的问题就在于加载了太多东西，然后有很多东西做的太重。
> ……先把我们现在做的所有 Lite 的尝试当成一场实验，然后重新思考怎么让 Alex 和 Blake
> 还是这样，但是变得更好、更轻盈。"

## 这不是什么

- ❌ 不是"精简 SKILL"——v2.7 那次精简把约束规则连同机械逻辑一起删掉，导致质量链系统性失效
  （`principles.md` SAFETY 条目「Judgment-Only Skill Files: Constraint Rules Are NOT Mechanical」）。
  **本 Epic 只改"什么时候读"，不改"读的内容"。**
- ❌ 不是合并 Alex/Blake 两个角色（`2026-07-27` 实测支持保留 planner + evaluator 分离）
- ❌ 不是继续演进 lite——lite 已定性为**已完成的实验**，冻结保留作对照物
- ❌ 不是省钱——`2026-07-27` 实测协议体积不是成本大头（TAD 会话 $620/千轮 < 非 TAD $684），
  **轮数才是**（≈$0.7/轮）

## 为什么这是真问题

`2026-07-27` 同一份研究里的 context rot 实证：18 个前沿模型，输入变长时准确率**非均匀下降 30-50%**，
机制包含 softmax 稀释——**早期定义的安全约束的注意力权重被逐渐摊薄**。

即：这 107.7K 预加载**正在稀释 `forbidden` / 反合理化登记 / 门禁本身**。加得越多，越不生效。
这就是"纪律一条不减"与"必须减加载"不矛盾的原因——**减加载正是为了让纪律生效**。

## 地基一：实测（2026-08-13，本机）

| 什么 | tokens | 备注 |
|---|---|---|
| 6 个启动扫描读的文件 | **45.2K** | `NEXT.md` 97KB（自订上限 500 行）、研究库 REGISTRY 42.7KB 全读 |
| `alex/SKILL.md` 全文 | 25.4K | 1934 行，**1270 行内联**，最大单块 `constraints` 464 行 |
| 5 个 config 模块 | 23.3K | `command_module_binding` 机制已存在，但绑定写成"全都要" |
| `CLAUDE.md` + 3 个存在的 `@import` | 9.7K | `principles.md` 16 条全载占 25KB；另有 **5 个 `@import` 指向不存在的文件** |
| 工具速查 + ROADMAP | 4.1K | |
| **合计（用户开口之前）** | **107.7K** | |

## 地基二：lite 实验的收获（本 Epic 的材料）

把 lite 砍掉的东西分成两类，`2026-07-30` 逐机制实测的结果是：

| lite 的处置 | 结果 |
|---|---|
| **改形式、留实质**（契约变一页纸、双 terminal 变单 terminal 人切角色） | ✅ **两次全过** |
| **砍纪律**（设计期专家审查、苏格拉底提问） | ❌ 一次实测泄漏、⚠️ 一次靠用户补位 |

**这就是 Objective 的实证来源：形式可以变轻，纪律不能砍。**

可直接用作本 Epic 材料的收获：

1. **一页纸契约够用** —— 契约规模该由后果定，不由模板定
2. **线性脊柱比模式菜单好懂** —— lite 是 L0→L5 一条道，full 是"先在 8 个模式定位、再从 32 个协议挑"

（另有两项收获**超出本 Objective**，见文末「候选后继」：Execution Mandate 反哺、三问旋钮搬家。）

## Phase Map

| # | Phase | 状态 | 交付 | 释放 |
|---|---|---|---|---|
| **P1a** | **路由脱钩·行为侧（止血）** | ✅ **COMPLETE**（Gate 4 PASS，2026-08-14，Alex 独立重算） | 11 个文件里的「默认走 lite」→ 默认 full；lite 标为**冻结实验**、保留显式调用；`version.txt` → 2.42.0；重生成 brain-index | 0（但每晚一天就多一批新工作被灌进冻结通道） |
| ~~P1b~~ | 路由脱钩·版本横幅 → **降为杂活**（见文末杂活清单） | — | `README.md`×9、`PROJECT_CONTEXT.md`、`docs/MULTI-PLATFORM.md`、`.tad/config.yaml` 首行的 `Version 2.41.0 - Lite is the Default Channel` | 0 |
| **P2a** | **补全纪律清单**（不动代码） | READY | 未扫语料 55 块逐块归宿 → 纪律清单从 15 条补到全集。⚠️ 原 15 条只扫过 3 个来源，**从未扫过 `gate/SKILL.md`（995 行）与激活时强制加载的 5 个 config** | 0 |
| **P2b** | **地板表**（不动代码） | BLOCKED by P2a | 每条纪律标 Layer 0/1/2 + 是否不可降级；含原 L3 实质 | 0 |
| **P3** | 启动扫描 → **缓存判定行** | BLOCKED by P2b | 扫描保留，激活时只读一行缓存判定，不读原始数据 | −45.2K |
| **P4** | **按用途加载**（config 绑定 + 知识 `@import` 改索引，合并原 P4/P5） | BLOCKED by P2b | 改 `command_module_binding` 的绑定不改机制；知识仿 `patterns/_index.md`（2.1KB 索引 10 文件）+ 删 5 个死引用 | −33.0K |
| ~~P6~~ | `NEXT.md` 归档 → **降为杂活**（见文末杂活清单） | — | 450 行 / 97KB → ≤500 行且 <20K | −24K |
| **P7** | **SKILL body 外置** | BLOCKED by P3-P6 | `constraints` 464 行 + 6 个模式专属协议 → `references/` + `load_when` | −~15K |

⚠️ **P7 排最后且标最高危**：它是唯一会触碰约束文本位置的一刀。验收**必须是逐类计数不变**，
不能是总量——`2026-06-01` 的教训：全局计数会被"合法删除"掩盖
（`principles.md`「A Coverage Gate's Global-Count Floor Cannot Detect Must-Cover SAFETY Loss」）。

⚠️ **P3 的措辞被 Alex 自己改过一次**：初稿是"砍掉启动扫描只留 session-state"。**错的。**
依赖扫描是唯一有真实事故记录的启动纪律（停跑 28 天漏掉 `gh` 4 个漏洞，含明文 token 打印），
砍成显式命令 = 没人会敲 = 把响亮失败换成静默失败。**正确诊断是"读得太早太全"，不是"多余"。**

## 每一刀的三条通用验收

1. **机械量**：脚本测「激活完成、用户开口之前读取的总字节数」，逐刀下降且不反弹
2. **纪律不流失**：约束**按类别** grep 计数不变（不是总量）
3. **真活验证**：每刀切完跑一个**非框架**真任务（`dps-harness` 插件是第一候选）
   —— 这条同时兑现了前 Epic 的「非自指实证」，不再单列 phase

## Success Criteria

| # | 判据 | 现值 |
|---|---|---|
| SC1 | 激活即付 ≤ 15K tokens | 107.7K（P1a 不改加载量，按设计） |
| SC2 | 5 类约束逐类计数不变 | 待 P2 建基线 |
| SC3 | 每刀切完跑过 ≥1 个非框架真活且未回滚 | 0/4 |
| SC4 | P1a 之后无新工作被路由进 lite | ✅ P1a 已落地（11 文件），待观察 |
| SC5 | 至少 1 个下游项目实测跑安装命令后拿到新版本 | 未测（**不是阻塞**，见 R4） |

## Risk Register

| # | 风险 | 缓解 |
|---|---|---|
| R1 | P7 重演 v2.7 质量链失效 | 排最后；逐类计数不变；先建基线 |
| R2 | 按需加载 → **"不知道自己需要什么"**。今天 Alex 被用户当场纠正 3 次，无一自我发现；`2026-07-27` 那份研究 9 次错判**全部**由外部纠正 | P2 地板里**放索引不放内容**（`patterns/_index.md` 用 2.1KB 索引 10 个文件）；碰凭据/发布/框架自身**不经判断强制加载** |
| R3 | 砍启动扫描 → 静默失败 | P3 改缓存判定行；负控：人为造一条 urgent_security，激活后必须仍看得见 |
| R4 | 存量下游若跑**自己目录里那份旧 `tad.sh`**（2.30.0–2.40.x），会看到假的"已是最新"并秒退 | **不是阻塞**：推荐路径本来就是 `curl -sSL …/main/tad.sh \| bash -s -- --yes`，它抓线上脚本、绕过坏副本，前 Epic 的 AC12b 已实测能正常升级。残留风险只是"有人习惯性跑本地脚本"，属文档与习惯问题 |
| R5 | 每刀契约膨胀回 577 行 | **硬上限 150 行**；超了说明这刀切得太大，拆。P1a 实测：初稿 167 → 定稿 144，水分全在"同一信息写两遍"和散文 |
| **R6** | ⚠️ **P1a 会让默认路径少掉一条纪律**：「改框架自身须由人裁定」实测只活在 `CLAUDE.md §2.5` + 两个 lite skill，三个 full skill 各 0 命中；P1a 把 §2.5 限定到"在飞单"后，默认路径上不再有这道门，且 `CLAUDE.md:43` 已允许"文档更新"跳过 TAD | **用户 2026-08-14 裁定不补**。与本 Epic Objective「纪律一条不减」相悖，**明写不掩盖**。未缓解 |

## 自加约束（前 Epic 的教训）

**每刀的契约上限 150 行。** 2026-08-13 为改 10 行写了 577 行契约，仪式与实质比例倒置。

## 同步方式（已定，2026-08-13）

**走拉取**：`*publish` 发到 GitHub，下游各自跑安装命令拉
（`feedback_no-sync-pull-based` 的既有决定，无需重新裁定）。
通路可用——见 R4。P1 改完路由后需发版，**发布是对外动作，须先经人确认**。

⚠️ 遗留观察：dps-harness 的 lite 比主仓落后 157 行而两边 `version.txt` 同为 2.41.0
——即**改了 skill 却没动版本号**，下游因此看不出自己是旧的。P1 发版时须一并处理版本号。

## 候选后继（超出本 Objective，不在本 Epic 做）

1. **Execution Mandate 反哺 full**（lite 的净发明，full 实测 0 处）——但必须按后果分档：
   本机文件改动 3 个字段，触及外部服务/凭据/发布才要全套。
   2026-08-13 在 dps-harness 实测：注册 4 个命令的小活被要求先写 14 个字段的授权块。
2. **三问旋钮搬进 full 的 `expert-criteria.yaml`**（现为关键词正则，比三问粗）
3. **DSH 插件路线**：把角色激活与按需加载从"模型自觉"变成"运行时确定性注入"


---

## 杂活清单（做但不走 phase 那套机器）

用户 2026-08-14 指出 phase 拆得过细。按今日三问对档，两件确实是杂活不是 phase
——**零新建判断、产物非判据、失败一眼可见**：

| 杂活 | 内容 | 什么时候做 |
|---|---|---|
| 版本横幅 | `README.md`×9、`PROJECT_CONTEXT.md`、`docs/MULTI-PLATFORM.md`、`.tad/config.yaml` 首行的 `Version 2.41.0 - Lite is the Default Channel` | 发版时顺手 |
| `NEXT.md` 归档 | 450 行 / 97KB → ≤500 行且 <20K | 随时 |
| brain-index 生成器 | 撞到缺 `task_type:` 的旧归档即 `set -e` 退出 → 「recent 50」段只出 11 条（缺 39）。**预存缺陷**，非本 Epic 引入 | 独立维护单 |

⚠️ **这条修正本身是本 Epic 的一次自我应用**：Epic 的目的是让重量与任务相称，
而初版给 8 个 phase 排了同一规格的机器——治病的方案里把病又犯了一遍。
现为 **6 刀（P1a 已完成，余 P2a/P2b/P3/P4/P7）+ 3 件杂活**。

## P1a 留下的账（Gate 4 认定，均为 Alex 侧契约缺陷）

1. **AC9 是自证的**：条文「不得改冻结基线」，唯一执行者即被约束方；pin 13 逼出必改时，
   解法是改基线并重新冻结，AC9 仍全绿。→ 已蒸馏。
2. **钉死表的 NEW 值含了禁词表里的词**（pin 13 的 `Default channel`），三专家未捕获。→ 已蒸馏为动工前预检。
3. **AC3 字面读法不可满足**（必改的 description 行不在豁免区间内）。同类形状，同样实现期才暴露。
4. **立项理由指向文档、却漏掉真正在跑的代码**（`tad.sh`）。→ 已蒸馏。


## ⚠️ 2026-08-15：P2 拆分与前提更正

原 P2 假设「TAD 的质量纪律共 15 条」，依据是上一个 Epic 的 `discipline-inventory.md`。
**Gate 2 穷举审查实测该前提不成立**：那份枚举的语料只有 3 个来源
（`CLAUDE.md` §3 / `alex`+`blake` SKILL / lite SKILL），**从未扫过**
`.claude/skills/gate/SKILL.md`（995 行，全仓强制条款密度最高）、
激活时强制加载的 5 个 `config-*.yaml`、以及 `AGENTS.md`（Codex 一等平台）。

审查员在未扫语料里独立找到 **4 条 P0 + 6 条 P1** 主表级强制纪律，例如：
研究先行（设计前必须搜已有方案，不搜即 VIOLATION）、技术决策透明（方案由谁定）、
平台绑定交互决策（"禁止代答、禁止把选项折叠成默认值"——它是 5 条"人一次/人多轮"纪律
在无 AskUserQuestion 平台上的**存在条件**）、六个带证据要求的强制问题。

⚠️ **原 P2 的 AC1 会把这个洞焊死**：它要求产物与 15 条逐字相等、不增不减 ——
本意防漂移，实际是把不完整集合盖章成"全集"并全绿。

**规模实测**：全语料命名块 192，未扫语料 55；强制标记行（宽词表）全语料 593、未扫 257。
P2a 按**块**归宿（55 块），行级扫描作交叉校验捕捉孤儿。

**另更正一处此前的事实错误**：Alex 曾两次称"改框架自身在 full 通道不触发安全审查"。
实测第三版才对——`expert-criteria.yaml` 的模式匹配的是**内容不是路径**，全仓
**4557/5239 = 86% 的 tracked 文本文件命中**，且**没有任何脚本解析它**（靠 agent 读散文执行）。
真正的缺陷是「无机械消费者 + 词表无判别力」，不是"不触发"。三种说法对应三种修法。
