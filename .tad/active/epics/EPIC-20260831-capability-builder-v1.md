# Epic: Capability Builder v1

**Epic ID**: EPIC-20260831-capability-builder-v1
**Created**: 2026-08-31
**Owner**: Alex

---

## Objective

Give any TAD-enabled subproject a TAD-native way to create, evolve, and optionally package a project-owned capability without introducing a new Capability Pack format or changing TAD's core delivery system. New capabilities are standard Agent Skills owned by the source project; existing Capability Packs remain untouched legacy artifacts.

## Success Criteria

- [x] A downstream project can create one locally owned Agent Skill at `.agents/skills/<name>/`, validate it, project it mechanically to `.claude/skills/<name>/`, and prove behavioral value with a fresh WITH/CONTROL run.
- [ ] A validated Skill can evolve fixture-first without regressing its prior behavior.
- [ ] A project owner can explicitly package one validated Skill into one OpenAI Plugin without creating a second editable copy of the Skill.
- [ ] Voice Studio materializes `eval-page-generator` from its accepted SCAND and completes the end-to-end Builder workflow.
- [ ] `tad.sh`, Alex/Blake role semantics, Gates, and all existing `.tad/capability-packs/*` content remain behaviorally unchanged.

---

## Phase Map

| # | Phase | Status | Handoff | Key Deliverable |
|---|-------|--------|---------|-----------------|
| 1 | Create | ✅ Done | `HANDOFF-20260831-capability-builder-phase1-create.md` | Project-local Agent Skill creation, validation, projection, and WITH/CONTROL proof |
| 2 | Evolve | ⬚ Planned | — | Signal-driven, fixture-first Skill evolution with regression protection |
| 3 | Package | ⬚ Planned | — | Explicit one-Skill/one-Plugin packaging and isolated validation |
| 4 | Voice Studio Dogfood | ⬚ Planned | — | Real `eval-page-generator` Candidate → Skill → Plugin proof |

### Phase Dependencies

All phases are sequential: Phase 2 depends on Phase 1; Phase 3 depends on the validated Skill contract from Phases 1–2; Phase 4 exercises the complete workflow without widening its product scope.

### Derived Status

Status and progress are computed from the Phase Map:
- **Status**: If all ⬚ → Planning | If any 🔄 or ✅ → In Progress | If all ✅ → Complete
- **Progress**: Count of ✅ Done / Total phases

---

## Phase Details

### Phase 1: Create

**Status:** ✅ Done
**Execution:** Gate 4 accepted and archived on 2026-09-02

#### Scope

Add a public `$capability-builder create` path that uses the existing Alex → handoff → Blake → Gates workflow to create exactly one Local-first Agent Skill in the current subproject. The editable authority is `.agents/skills/<name>/`; `.claude/skills/<name>/` is a generated byte-identical projection. This phase does not add Plugin packaging, retire legacy packs, or modify TAD's installer, roles, or Gates.

#### Input

Either an explicit human request with local project materials, or one existing SCAND plus its linked journal, knowledge, scripts, assets, and evidence. Existing `capability-upgrade` research/build knowledge and the single-fixture assertion path in `.tad/scripts/pack-eval-runner.sh` are available for bounded reuse.

#### Output

A discoverable `$capability-builder` entry with create protocol, a compatibility route from `$capability-upgrade`, deterministic Skill validation/projection mechanics, fresh behavioral evidence, and regression fixtures proving legacy pack evaluation remains unchanged.

#### Acceptance Criteria

- [x] A valid fixture project produces `.agents/skills/example-skill/SKILL.md` and byte-identical `.claude/skills/example-skill/SKILL.md`; `diff -rq` exits 0.
- [x] Invalid YAML/frontmatter, folder/name mismatch, unresolved scaffold placeholders, and a divergent existing `.claude` target each produce a non-zero validation/projection result without overwriting either tree.
- [x] A fresh no-Skill CONTROL fails its discriminative threshold and a fresh WITH-Skill run passes it; both raw outputs and the recomputed verdict live under `.tad/evidence/acceptance-tests/capability-builder-create/`.
- [x] Existing `pack-eval-runner.sh` single-fixture behavior remains backward compatible for legacy `pack:` fixtures while accepting the new Skill fixture label.
- [x] Scope proof shows no changes to `tad.sh`, `.claude/skills/alex/`, `.claude/skills/blake/`, `.claude/skills/gate/`, or `.tad/capability-packs/`.
- [x] Builder output contains no `CAPABILITY.md`, generated README, generated CHANGELOG, or per-Skill `install.sh`.

#### Files Likely Affected

- `.claude/skills/capability-builder/SKILL.md` (CREATE)
- `.claude/skills/capability-builder/references/create-protocol.md` (CREATE)
- `.agents/skills/capability-builder/` (CREATE generated framework mirror)
- `.claude/skills/capability-upgrade/SKILL.md` (MODIFY compatibility entry)
- `.claude/skills/capability-upgrade/references/legacy-pack-research.md` (CREATE preserved conditional reference)
- `.agents/skills/capability-upgrade/SKILL.md` (MODIFY generated framework mirror)
- `.agents/skills/capability-upgrade/references/legacy-pack-research.md` (CREATE generated framework mirror)
- `.tad/scripts/capability-skill.sh` (CREATE)
- `.tad/scripts/pack-eval-runner.sh` (MODIFY backward-compatible Skill fixture alias only)
- `CLAUDE.md` (MODIFY Capability routing line only)
- `.tad/evidence/acceptance-tests/capability-builder-create/` (CREATE)

#### Dependencies

None. This is the first independently useful delivery.

#### Notes

Framework-owned skills keep TAD's existing `.claude → .agents` release invariant. Builder-created target-project skills use `.agents → .claude`; the existing framework-owned/target-only local-skill distinction prevents TAD updates from taking ownership. The implementation must preserve useful deep-research material from `capability-upgrade` behind conditional progressive disclosure rather than deleting it.

Completed: 2026-09-02, Handoff: `HANDOFF-20260831-capability-builder-phase1-create.md`, Commit: `2d7e359b765f1f05896b4b41cd442662791b003e`. Gate 4 PASS with 12/12 ACs, 54/54 structural cases, and 7/7 eval-compat cases. Accepted P2 follow-ups remain non-blocking.

### Phase 2: Evolve

**Status:** ⬚ Planned
**Execution:** pending

#### Scope

Add `$capability-builder evolve` for existing project-owned Agent Skills. Evolution is triggered only by a real failure, human correction, behavioral regression, relevant external change, or explicit new requirement; it begins by capturing a regression fixture and makes the smallest evidence-supported edit. This phase does not perform scheduled refreshes, Plugin packaging, or legacy pack upgrades.

#### Input

Phase 1's validated Skill authority/projection contract, an existing project Skill, its prior fixtures/evidence, and a concrete evolution signal.

#### Output

An evolve protocol that preserves the failure before editing, reruns old and new fixtures, projects only after the canonical Skill passes, and stops on target drift.

#### Acceptance Criteria

- [ ] A fixture demonstrates the triggering failure before the Skill edit and passes after the edit.
- [ ] All pre-existing fixtures retain their prior PASS verdict; any regression blocks projection and Gate 3.
- [ ] No evolution occurs when the request lacks one of the approved trigger signals; the workflow exits without modifying the Skill.
- [ ] A manually changed `.claude/skills/<name>/` projection is detected and preserved rather than overwritten.

#### Files Likely Affected

- `.claude/skills/capability-builder/SKILL.md` (MODIFY mode router)
- `.claude/skills/capability-builder/references/evolve-protocol.md` (CREATE)
- `.agents/skills/capability-builder/` (MODIFY generated framework mirror)
- `.tad/evidence/acceptance-tests/capability-builder-evolve/` (CREATE)

#### Dependencies

Phase 1

#### Notes

External research is conditional evidence gathering, never a calendar-driven maintenance obligation. The first new regression fixture is part of the change, not an after-the-fact documentation task.

### Phase 3: Package

**Status:** ⬚ Planned
**Execution:** pending

#### Scope

Add explicit `$capability-builder package` support for packaging one validated project Skill into one OpenAI Plugin. Only the Plugin's `skills/<name>/` subtree is generated from the canonical Skill; Plugin-specific manifest/MCP/App content remains project-owned platform code. This phase does not auto-package every Skill, aggregate multiple Skills, modify a personal marketplace, or add DeepSeek Harness support.

#### Input

A Phase 1/2 validated Skill, explicit project-owner packaging intent, and optional existing MCP/App requirements.

#### Output

A valid Plugin scaffold, generated Skill projection, deterministic validation, isolated install/discovery evidence, and overwrite/drift protection.

#### Acceptance Criteria

- [ ] The Plugin has a valid `.codex-plugin/plugin.json`, normalized matching folder/name, and exactly one generated `skills/<name>/` subtree.
- [ ] `diff -rq .agents/skills/<name> <plugin>/skills/<name>` exits 0 immediately after packaging.
- [ ] Plugin validation and isolated installation/discovery pass without writing the user's personal marketplace.
- [ ] A packaging failure leaves `.agents/skills/<name>/` and `.claude/skills/<name>/` byte-unchanged.
- [ ] MCP/App paths are absent unless explicitly requested and backed by real project files.

#### Files Likely Affected

- `.claude/skills/capability-builder/SKILL.md` (MODIFY mode router)
- `.claude/skills/capability-builder/references/package-openai-plugin.md` (CREATE)
- `.agents/skills/capability-builder/` (MODIFY generated framework mirror)
- `.tad/scripts/capability-plugin.sh` (CREATE)
- `.tad/templates/openai-plugin/` (CREATE minimal manifest template)
- `.tad/evidence/acceptance-tests/capability-builder-package/` (CREATE)

#### Dependencies

Phases 1 and 2

#### Notes

The package path follows the current OpenAI Plugin manifest contract, but the canonical Capability judgment remains the Agent Skill. Marketplace registration is a separate authorized external action, not a hidden packaging side effect.

### Phase 4: Voice Studio Dogfood

**Status:** ⬚ Planned
**Execution:** pending

#### Scope

Run the complete Builder workflow inside Voice Studio using the accepted, unmaterialized `eval-page-generator` SCAND and its four existing real-use evidence anchors. Create and validate the project-owned Skill, exercise fixture-first evolution if a concrete defect is observed, and explicitly package it as a one-Skill Plugin. This phase does not redesign Voice Studio, generalize the Skill prematurely, or add DSH support.

#### Input

The accepted Voice Studio SCAND, four validated evaluation-page examples, Builder Phases 1–3, and the project's existing scripts/assets/evidence.

#### Output

A Voice Studio-owned `eval-page-generator` Agent Skill, Claude projection, optional project-owned Plugin wrapper, raw WITH/CONTROL evidence, and human business acceptance.

#### Acceptance Criteria

- [ ] `voice-studio/.agents/skills/eval-page-generator/SKILL.md` exists, validates, and is byte-identical to its generated Claude projection.
- [ ] A no-Skill CONTROL fails and a WITH-Skill run passes the agreed discriminative fixture against a representative evaluation-page task.
- [ ] The generated evaluation page preserves the candidate's required behavior: single-file operation, safe text insertion, exportable JSON, and visible completion counts.
- [ ] The accepted SCAND records the project-local materialized path without transferring ownership to the TAD repository.
- [ ] The Plugin wrapper validates and installs in isolation, while the canonical Voice Studio Skill remains unchanged.

#### Files Likely Affected

- `voice-studio/.agents/skills/eval-page-generator/` (CREATE)
- `voice-studio/.claude/skills/eval-page-generator/` (CREATE generated projection)
- `voice-studio/plugins/eval-page-generator/` (CREATE project-owned Plugin)
- `voice-studio/.tad/active/skillify-candidates/SCAND-20260606-eval-page-generator.md` (MODIFY status/materialized path)
- `voice-studio/.tad/evidence/capability-builder/eval-page-generator/` (CREATE)

#### Dependencies

Phases 1, 2, and 3

#### Notes

Phase 4 is executed from the Voice Studio project under its own TAD boundary. If dogfood exposes a Builder defect, the phase reports a bounded blocker/follow-up; it must not silently widen its write scope back into TAD.

---

## Context for Next Phase

### Completed Work Summary

- Phase 1 Create is Gate 4 accepted. TAD can now create one project-owned Agent Skill,
  validate its canonical `.agents` tree, project it safely to `.claude`, and prove value
  with fresh discriminative WITH/CONTROL evidence.

### Decisions Made So Far

- New work retires Capability Pack as an artifact concept; `Capability` remains the outcome, Agent Skill the canonical carrier, and OpenAI Plugin an optional wrapper.
- Source projects own their Skills. TAD supplies the build/evolve/package workflow but does not absorb domain content.
- Existing 25 Capability Packs remain legacy and out of scope until a later retirement program.
- Phase 1 deliberately uses cooperative local locking and bounded one-Skill fixture trees;
  stale-lock recovery and large-tree hashing remain non-blocking future hardening.

### Known Issues / Carry-forward

- Codebase-memory MCP was unavailable during initial discovery; exact shell/config paths were verified by bounded local reads instead.
- Current `pack-eval-runner.sh` terminology is legacy, but its single-fixture discriminative assertion is reusable. Renaming or replacing it is explicitly not required for Phase 1.
- Phase 1 accepted P2s: SIGKILL may leave a conservative stale lock; acceptance hashing is per-file; eval-regex resource complexity is unbounded.

### Next Phase Scope

Phase 2 `evolve` remains planned but parked. It requires a separate human decision and
a concrete evolution signal; Phase 1 acceptance does not authorize automatic start.

---

## Notes

The user explicitly rejected a TAD-wide architecture change. Every phase is constrained to the capability-development surface, and each phase must independently prove that core TAD behavior remains unchanged.

**Parked by Human — 2026-09-02:** Phase 1 already provides the usable core capability.
Keep this Epic in `active/epics/` as parked context; do not start Phases 2–4 until
real downstream use creates a concrete need and the Human explicitly resumes it.
