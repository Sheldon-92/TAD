---
task_type: code
gate3_verdict: pass
epic: EPIC-20260531-tad-lean-trustworthy.md
phase: 5/5
from: Blake
date: 2026-05-31
---

# COMPLETION: P5 Capability pack behavioral eval runner + 14-pack fixtures

**Handoff:** HANDOFF-20260531-tad-lean-trustworthy-phase5.md
**Verdict:** PASS (Blake deliverables: runner + fixtures + side-file + synthetic self-test. AC5.6 is Conductor-driven in Y6, not Blake.)

---

## Summary of Deliverables

| Task | Deliverable | Status |
|------|-------------|--------|
| 1 | `.tad/scripts/pack-eval-runner.sh` (assertion engine, advisory, BSD-safe) | DONE |
| 2 | 14 behavioral fixtures (1 per installed capability pack lacking one) | DONE |
| 3 | `.tad/capability-packs/behavioral-eval-status.yaml` (side-file, NOT registry flag) | DONE |
| 4 | Synthetic self-test (PASS + FAIL cases) of the assertion engine | DONE |

Every installed capability pack now has ≥1 behavioral fixture (video-creation kept its 2).

---

## Task 3 — Registry Flag Approach: SIDE-FILE (chosen)

**Chosen:** `.tad/capability-packs/behavioral-eval-status.yaml` (side-file), NOT a field in pack-registry.yaml.

**Why:** `pack-registry.yaml` is AUTO-GENERATED by `scan-packs.sh` via `cat > "$OUTPUT"` — a full regen from CAPABILITY.md/SKILL.md frontmatter that emits a fixed field set per pack. Verified: `grep -c behaviorally_verified pack-registry.yaml` = 0 and the generator has no such field. Any `behaviorally_verified` flag written into the registry would be CLOBBERED on the next `scan-packs.sh` run. The side-file is conductor/user-owned persisted decision state and survives regeneration (consistent with the registry desync / "separate derived scan data from persisted decision state" pattern). All packs default `pending`; only a passing `pack-eval-runner.sh` result justifies the Conductor flipping a pack to `verified` (AC5.6, Y6). `ml-training` is `no-fixture` (registered in capability-packs/ but not installed as a `.claude/skills/` skill).

---

## AC5.1 — Synthetic self-test (assertion engine) — RAW OUTPUT

The runner's assertion mechanism was tested mechanically (no Claude sub-agent — Blake cannot spawn one). Two synthetic captured-output files were created against the real video-creation `photo-to-beat-sync` fixture (min_marker_count = 4).

### PASS case — synthetic output containing the 5 named markers (first_frame, last_frame, montage, view-specific, camera-tree)

```
$ bash .tad/scripts/pack-eval-runner.sh .claude/skills/video-creation/examples/photo-to-beat-sync.md /tmp/pack-eval-test/synthetic-5markers.md
PACK video-creation FIXTURE photo-to-beat-sync: 6/4 → PASS
```

Raw recompute of distinct matches (the runner's own `grep -oE … | sort -u`):
```
camera-tree
first_frame
last_frame
montage
parent and child shot
view-specific
```
6 distinct ≥ min 4 → PASS. ✅

### FAIL case — synthetic output containing only 2 markers

```
$ bash .tad/scripts/pack-eval-runner.sh .claude/skills/video-creation/examples/photo-to-beat-sync.md /tmp/pack-eval-test/synthetic-fail.md
PACK video-creation FIXTURE photo-to-beat-sync: 3/4 → FAIL
```
(Synthetic had "montage" + "first_frame" + a "decomposition" near-miss → 3 distinct < min 4 → FAIL.) ✅

Both branches of the assertion engine (≥ min → PASS, < min → FAIL) confirmed.

---

## AC5.2 — Fixture coverage (every installed capability pack has ≥1) — RAW OUTPUT

Enumerated `.claude/skills/*/`, excluded framework skills + `_archived`, counted fixtures with a valid `## Verification Command` + `grep -oE` pattern:

```
academic-research          1   (systematic-review-depth)
ai-agent-architecture      1   (multi-agent-design-decisions)
ai-evaluation              1   (llm-judge-ab-eval)
ai-prompt-engineering      1   (hallucination-diagnosis)        [+ pre-existing doc example, not a fixture]
ai-tool-integration        1   (cli-vs-mcp-decision)
ai-voice-production        1   (audiobook-tool-selection)
code-security              1   (sast-dast-triage-pipeline)
product-thinking           1   (pressure-test-verdict)          [+ pre-existing doc example, not a fixture]
research-methodology       1   (five-phase-pipeline)
video-creation             2   (photo-to-beat-sync, single-clip-narration)  [pre-Phase-5]
web-backend                1   (api-pagination-review)
web-deployment             1   (cicd-sha-pin-oidc)
web-frontend               1   (design-token-consumption)
web-testing                1   (test-pyramid-strategy)
web-ui-design              1   (anti-slop-landing-design)
--- capability packs counted: 15 ---  (0 missing)
```

14 NEW fixtures authored + video-creation's 2 existing = 16 behavioral fixtures total across 15 installed capability packs. No pack skipped.

> Note: `ai-prompt-engineering/examples/system-prompt-template.md` and `product-thinking/examples/pressure-test-example.md` are PRE-EXISTING annotated documentation examples (shipped v2.16.0), NOT behavioral fixtures — they have no `## Verification Command`. The runner correctly SKIPs them as "bad fixture" without crashing. Each of those two packs received a proper NEW behavioral fixture alongside the doc example.

---

## AC5.3 — Discriminative-marker spot-check (5 fixtures) + grep -oE confirmation

All fixtures verified to use `grep -oE … | sort -u | wc -l` (NOT `grep -c` — would trip P4 lint Rule A). Per-fixture: `-oE:1  grep-c:0  structural:≥1  ❌:≥3`. Full table confirmed for all 16 fixtures.

### Spot-check — would a NO-PACK agent emit these markers? (must be NO)

1. **ai-evaluation / llm-judge-ab-eval** — markers `self-enhancement bias`, `10-15%`, `n=20 → ±10pp confidence interval`, `determinismLevel → run ≥3/≥10x`, `Judge ≠ Optimizer`. A no-pack agent reviewing an A/B eval would say "use more cases / a good judge" — it would NOT name *self-enhancement bias* with the 10-15% inflation figure or the determinism-class sampling counts. DISCRIMINATIVE ✅

2. **code-security / sast-dast-triage-pipeline** — markers `Four-Gate fastest-fail-first`, `Detection ≠ Remediation / 72% / security theater`, `reachability over CVSS / dead code`, `TruffleHog exit 183`. A no-pack agent would say "fix critical CVSS first" (the exact naive approach the pack corrects) and would NOT cite exit code 183 or the 72%-orgs stat. DISCRIMINATIVE ✅

3. **web-testing / test-pyramid-strategy** — markers `ice-cream-cone / inverted pyramid`, `per-module coverage auth 90/logic 80/UI 60`, `automated a11y 30-50% (n=550 / 57%)`, `mutation testing (Stryker)`. A no-pack agent says "add more tests / increase coverage"; it would NOT produce the per-module split or the n=550 a11y figure. DISCRIMINATIVE ✅

4. **ai-voice-production / audiobook-tool-selection** — markers `ACX 44.1kHz / 192kbps / RMS -23 to -18 dB`, `per-tool reference duration 3s-15s`, `.pt speaker embedding / seed reset`, `16GB VRAM / MPS fallback / float32`. A no-pack agent says "use a TTS tool, make it sound good"; it would NOT cite exact ACX mastering specs or the MPS-fallback Apple-Silicon rule. DISCRIMINATIVE ✅

5. **web-deployment / cicd-sha-pin-oidc** — markers `SHA-pin actions (not @v4)`, `OIDC over stored long-lived secrets`, `immutable deploy / Docker image SHA vs in-place`, `blue-green / canary / atomic rollback`. A no-pack agent says "set up CI/CD, use env vars"; it would NOT flag the `@v4`-tag supply-chain risk + SHA pin + OIDC-vs-stored-secret distinction as a coordinated rule set. DISCRIMINATIVE ✅

Every fixture's Anti-Slop Check additionally lists the ❌ generic terms (e.g. "video", "make it secure", "add more tests", restated-input words) that a no-pack agent WOULD emit and that are therefore excluded from the marker patterns.

---

## AC5.4 — Side-file present + runner --all batch mode — RAW OUTPUT

`.tad/capability-packs/behavioral-eval-status.yaml` present (16 registry packs mapped; 15 `pending`, `ml-training` `no-fixture`).

```
$ bash .tad/scripts/pack-eval-runner.sh --all /tmp/pack-eval-outputs
=== Pack Behavioral Eval — batch (--all) ===
outputs dir: /tmp/pack-eval-outputs
-------------------------------------------------------------
PACK academic-research FIXTURE systematic-review-depth: no output captured → SKIP
PACK ai-agent-architecture FIXTURE multi-agent-design-decisions: no output captured → SKIP
PACK ai-evaluation FIXTURE llm-judge-ab-eval: no output captured → SKIP
PACK ai-prompt-engineering FIXTURE hallucination-diagnosis: no output captured → SKIP
PACK ai-prompt-engineering FIXTURE system-prompt-template: no verification pattern → SKIP (bad fixture)
PACK ai-tool-integration FIXTURE cli-vs-mcp-decision: no output captured → SKIP
PACK ai-voice-production FIXTURE audiobook-tool-selection: no output captured → SKIP
PACK code-security FIXTURE sast-dast-triage-pipeline: no output captured → SKIP
PACK product-thinking FIXTURE pressure-test-example: no verification pattern → SKIP (bad fixture)
PACK product-thinking FIXTURE pressure-test-verdict: no output captured → SKIP
PACK research-methodology FIXTURE five-phase-pipeline: no output captured → SKIP
PACK video-creation FIXTURE photo-to-beat-sync: 6/4 → PASS
PACK video-creation FIXTURE single-clip-narration: no output captured → SKIP
PACK web-backend FIXTURE api-pagination-review: no output captured → SKIP
PACK web-deployment FIXTURE cicd-sha-pin-oidc: no output captured → SKIP
PACK web-frontend FIXTURE design-token-consumption: no output captured → SKIP
PACK web-testing FIXTURE test-pyramid-strategy: no output captured → SKIP
PACK web-ui-design FIXTURE anti-slop-landing-design: no output captured → SKIP
-------------------------------------------------------------
1 pass / 0 fail / 17 skipped (no output captured)
```

Batch mode emits a results table + summary, matches captured outputs by fixture basename, and SKIPs packs with no captured output (expected until the Conductor drives real eval in Y6). Pre-existing doc examples degrade to "bad fixture" SKIP, never a crash.

---

## AC5.5 — Runner advisory + BSD-safe — RAW OUTPUT

```
bash -n .tad/scripts/pack-eval-runner.sh  → exit 0 (clean)
active grep -P occurrences                → 0  (only documented in SAFETY comment as forbidden)
SAFETY HEADER present                     → YES (lines 2-19)
active `set -e`                           → 0  (set -u only; SAFETY header explains why set -e is forbidden)
runner own match form                     → grep -oE "$pattern" … | sort -u | wc -l   (NOT grep -c)
```

The runner is advisory: `main()` returns 0 in single-fixture mode regardless of PASS/FAIL (never fail-closed at exit), `assert_one` returns verdict codes only for the batch tallier, and every parse path degrades to skip/default-3/0-count rather than aborting. It is not a hook.

---

## Files Changed

CREATE:
- `.tad/scripts/pack-eval-runner.sh` (236 lines — assertion engine)
- `.tad/capability-packs/behavioral-eval-status.yaml` (side-file status tracker)
- `.claude/skills/academic-research/examples/systematic-review-depth.md`
- `.claude/skills/ai-agent-architecture/examples/multi-agent-design-decisions.md`
- `.claude/skills/ai-evaluation/examples/llm-judge-ab-eval.md`
- `.claude/skills/ai-prompt-engineering/examples/hallucination-diagnosis.md`
- `.claude/skills/ai-tool-integration/examples/cli-vs-mcp-decision.md`
- `.claude/skills/ai-voice-production/examples/audiobook-tool-selection.md`
- `.claude/skills/code-security/examples/sast-dast-triage-pipeline.md`
- `.claude/skills/product-thinking/examples/pressure-test-verdict.md`
- `.claude/skills/research-methodology/examples/five-phase-pipeline.md`
- `.claude/skills/web-backend/examples/api-pagination-review.md`
- `.claude/skills/web-deployment/examples/cicd-sha-pin-oidc.md`
- `.claude/skills/web-frontend/examples/design-token-consumption.md`
- `.claude/skills/web-testing/examples/test-pyramid-strategy.md`
- `.claude/skills/web-ui-design/examples/anti-slop-landing-design.md`

No existing files modified. No pack left as TODO — all 14 capability packs lacking a fixture now have one.

---

## pack-eval-runner.sh — full script

See `.tad/scripts/pack-eval-runner.sh` (committed). Key contract:
- `pack-eval-runner.sh <fixture.md> <output-file>` → `PACK {pack} FIXTURE {name}: {actual}/{min} → PASS|FAIL`
- `pack-eval-runner.sh --all <outputs-dir>` → iterate every `.claude/skills/*/examples/*.md`, match `<outputs-dir>/{basename}.md`, emit results table + `{P} pass / {F} fail / {S} skipped` summary.
- Assertion: `actual = grep -oE "$pattern" "$output" | sort -u | wc -l`; PASS iff `actual ≥ min_marker_count`.
- Advisory: no `set -e`, never fail-closed, not a hook, BSD/macOS-safe (no `grep -P`).

---

## Note for Conductor (AC5.6, Y6)

To complete behavioral verification: for each pack, spawn a pack-loaded sub-agent on its fixture's Input Scenario, capture the agent's reply to `<outputs-dir>/{fixture-basename}.md`, run `pack-eval-runner.sh --all <outputs-dir>`, and for each PASS flip that pack `pending → verified` in `behavioral-eval-status.yaml`. Never hand-set `verified` — only a passing runner result justifies it (count ≠ signal).
