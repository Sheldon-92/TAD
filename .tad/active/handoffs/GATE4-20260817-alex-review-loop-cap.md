# Gate 4 (Acceptance): full Alex 专家审查终止条件

**Gate**: 4（业务验收）｜**执行**: Alex｜**Date**: 2026-08-17
**实现**: `dab4daf1`（Blake, Terminal 2）｜**判定**: ✅ **PASS**

---

## 1. 验收方式

不复述 AC（那是 Gate 3）。Gate 4 问一件事：**循环真的会停吗？**

方法：枚举「审查能继续下去」的全部出口，逐个查是否堵死。

## 2. 四个出口的复算

| 出口 | 堵死方式 | 实测 |
|---|---|---|
| **① 进入第 3 轮** | violations 第 3 条 + `round_protocol` 内硬禁止 | 各 **1** 处 ✅ |
| **② 人类 override 放行** | 该分支已删；`:844` 明写「不提供」并附三条理由 | 唯一出现即禁止语句本身 ✅ |
| **③ CONDITIONAL PASS 夹带未修 P0** | `:859-862` 补分支：仅在无遗留 P0 时算 resolved | **1** 处 ✅ |
| **④ 把 FAIL 单交给 Blake** | violations 第 4 条（与「重发全文」合并） | 存在 ✅ |

**四个出口全部堵死。** 人类在 `after_round_2` 的三个选项（缩小范围 / 换方案 / 撤单）**无一导向继续审**。

## 3. 对 Blake 报告的独立核实

| # | Blake 报的 | 核实 |
|---|---|---|
| P1-1 悬空的「或」 | ✅ **属实，且缺陷逐字来自我的 §3.1**：`(b) …；或` 后直接跟否定句，字面读作存在 (c)，而 (c) 正是两行后声明「没有第三种」的那个。**该文本已过两轮 Gate 2 审查仍带此缺陷** |
| P1-2 CONDITIONAL PASS 绕过 | ✅ **属实，是真漏洞**。协议自身 `:590`/`:805` 规定 verdict 为**三值**，我只写了 FAIL/非 FAIL 二分 → CONDITIONAL PASS 带未修 P0 落进「非 FAIL」侧直接算 resolved。**lite 有此分支，我声称照抄却漏了** |
| Note 2 §3.1 五条 vs AC-N6 四条 | ✅ **是我的工单自相矛盾**。Blake 合并两条 round-2 禁令、以 §7 为准，两名 reviewer 独立判为非偏离。**判断正确** —— §7 是绑定契约，§3.1 是被两轮审查取代的草稿 |
| Note 3 baseline SHA 分歧 | ✅ 属实，我抬头写 `35d69228`、消息写 `35187243`。Blake 采后者正确 —— 前者会把 handoff 文档自身计入而使 AC-N5 误红 |
| Note 4 AC-N5 判据范围改过 | ✅ **处置正确**：先查清成因（COMPLETION+NEXT 属第二个提交被误计）再改判据，**没有反过来**。实现区间 `dab4daf1` 实测恰 4 文件 |

**范围核对**：`dab4daf1` = 4 files changed, +128/-2，路径全部在允许集内 ✅

## 4. 判定：**PASS**

原缺陷（full Alex 审查无终止条件）已消除，且四个可能的绕行出口全部封闭。

### 4.1 ⚠️ 本单无法自证，Gate 4 附带一条待验条件

静态 AC 只证明约束**被写下**，证明不了它**生效**。
两名 reviewer 与 Blake 各自独立提出同一点。

**待验**：下一张真实 handoff 的专家审查轮次是否 ≤2。
→ 已记入 NEXT.md，Gate 4 不因此挂起（本单交付物本身完整）。

### 4.2 遗留（另立单）

`handoff-creation-protocol.md:522` 的 HTML 注释使该文件自 baseline 起即无法被 `yq` 解析
（报错 `line 516`），两侧镜像相同。长期未被发现是因 `skill-body-verify.sh` **只做 diff 不做解析**。
本单以 AC-N7 增量判据确保未恶化。

---

## 5. 这次交付里最有价值的东西

**不是那 63 行 YAML，是 P1-1 揭示的事实**：

> 我的 §3.1 那段文本经过**两轮 Gate 2 审查**（4 名 reviewer），仍带着一个字面矛盾 ——
> 而 Blake 的 Layer 2 一轮就抓到了。

**原因**：Gate 2 的 reviewer 审的是「这个设计对不对」，Blake 的 reviewer 审的是
「照这段文字写出来的东西对不对」。**后者会逐字读，前者不会。**

→ **可迁移判据**：设计审查与实现审查捕获的缺陷类型不同，前者无法替代后者。
「已审文本」≠「已验证文本」。
