# HANDOFF: 地板表 —— 30 条纪律的载体该常驻还是按需

**Epic**: `EPIC-20260813-alex-blake-lightening.md`（P2b / 6）
**From**: Alex（full） **To**: Blake **Created**: 2026-08-16
**Rev**: **rev2** —— Gate 2 两专家（AC 判别力 / 地板逻辑+静默失败），**7 个 P0**，全部修在本 rev
**配套**（均 commit，AC12 守其哈希）：`*.step0.sh` · `*.keys30.tsv`（30 条判别词，Alex 手写）

## 1. 这一单是**测量**，不是优化

产出 `.tad/discipline-floor.md`：30 条纪律各自的**载体**、**触发串**、**Layer**，
以及**分通道**的 Layer 0 实测字节。

⚠️ **不设字节预算。** 上一版设了 8192，而该预算是按"全部常驻"算出来再乘 2.8 倍余量的
——结构上不可能触发。本单只测量，**这个数是 Epic SC1 的输入，不是本单及格线**。

## 2. Layer 判定规则（27 行推导 + 有外部参照的裁量）

| 既有判定 | 触发条件（inventory 第 7 列） | Layer | 定档依据 |
|---|---|---|---|
| **地板**（7） | — | **0** | `既有判定` |
| **可缩放**（3） | — | **1 或 2**，不得 0 | `既有判定` |
| **待判**（20） | 循环 或 无法判定 | **0**（fail-safe） | `待判默认` |
| **待判** | **非循环** | **1 或 2** 允许 | `循环触发实测` + 须给"该触发事件在何处被 agent 独立知晓"的载体 |

⚠️ rev1 让 20 条待判**一律**判 0。审查员指出这与 §3 判据自相矛盾：表上 27 个 0 里
7 个测过、20 个没测且**无法区分** → 后三刀只能全当不能动，**Epic 目标被默认值锁死而非被证据锁死**。
而这半个答案 **inventory 已付过钱**：其「触发条件」列就是 `load_when`，实测 20 条待判里
≥8 条是外部可自知事件（`角色激活时` / `上下文压缩后` / `版本发布` / `Gate 3/4 证据检查` /
`无 AskUserQuestion 的 harness 出现` / `Human 纠正`），仅 3 条明显循环。

## 3. Layer 0 的判据（现成）

`principles.md`「Circular Trigger Test」：**若加载条件定义在被加载物内部，不加载它则触发永不成立**
→ 必须常驻。据此把载体拆成 **触发串**（让 agent 知道"这件事存在"的最短逐字文本）与**本体**。

## 4. 产物 `.tad/discipline-floor.md`

### 4.1 主表 —— 恰好 30 行，TAB 分隔 12 列（**首尾空白一律 strip 后计**）

`纪律 ⇥ 既有判定 ⇥ 通道 ⇥ 触发条件 ⇥ 循环? ⇥ 载体路径 ⇥ 锚点串 ⇥ 触发串 ⇥ Layer ⇥ 定档依据 ⇥ 不可降级 ⇥ 解除待判需要什么`

- 第 1/2/3/4 列逐字取自 Step 0 的 `verdicts.tsv`（纪律/既有判定/通道/触发条件），**零裁量**
- **循环?** ∈ `{是,否,无法判定}`；`否` 须在「解除待判」列写明触发事件的独立知晓点
- **锚点串**：在载体路径 `grep -Fq` 命中、≥12 字节、**且命中该纪律在 `keys30.tsv` 的判别词**
- **触发串**：**包含锚点串**、≥12 字节、**本身命中宽词表**
- **不可降级** ∈ `{是,否}`；**解除待判**仅 `既有判定=待判` 行必填

### 4.2 副表 —— Layer 0 里的**非纪律**常驻项（≥6 行）

至少含：身份与角色分离载体 · 意图路由 · 复杂度判定 · 压缩恢复锚点 ·
**反合理化登记**（`alex/SKILL.md` `anti_rationalization_registry:BEGIN/END`）· **Forbidden 清单本体**。
⚠️ 后两项经审查员独立确认循环触发成立。
⚠️ rev1 把「平台绑定交互决策」也列进副表 —— **错的**，它是主表 30 条之一（会双计），
且实测**非循环**（`alex/SKILL.md:335` 已有内联指针，触发条件"无 AskUserQuestion 的 harness"激活时可自知）。
列：`项 ⇥ 载体 ⇥ 锚点串 ⇥ 触发串`（同样过 §4.1 的锚点/触发串规则）。

### 4.3 汇总节 `## Layer 0 实测`（本单真正的产出）

```
full 激活 Layer0: 行数 {n} 触发串合计 {b} bytes
lite 激活 Layer0: 行数 {n} 触发串合计 {b} bytes
共有(both)      : 行数 {n} 触发串合计 {b} bytes
副表            : 行数 {m} 触发串合计 {c} bytes
触发串所在载体文件当前整读合计: {x} bytes   ← P7 前的真实下界
```
⚠️ **口径警告必须逐字写入**：`本数为触发串之和（bytes），SC1 的 107.7K 为单次 full 激活的
整读量（tokens 口径），两者不可直接比较。` 且 `lite` 行**不计入 SC1**（lite 已冻结，默认路径不付）。

### 4.4 两节必写

- `## 清单缺口`：**凭据/密钥** 与 **修改框架自身** 两类在 30 条中**无对应纪律行**（实测命中 0），
  须逐类写明并引用 `EPIC …#R6` 与 `CLAUDE.md:50`。⚠️ rev1 要求"不可降级组含这三类"是**不可满足**的
  （AC1 钉死 30 名、§5 禁改 inventory），真实结局是 Blake 拉一行盖三类全绿，**把 R6 的洞洗白**。
- `## 承接单`：`.tad/ralph-config/expert-criteria.yaml` 的 security 触发模式**匹配内容不匹配路径、
  且无任何脚本解析它**。须写明 pattern 出处、**Blake 当场实测的命令与分子/分母/百分比**、承接单名。
  ⚠️ 不得抄 Alex 给的数字——实测两位审查员得出 4557/5239 与 4754/6155 两个不同分母，**口径未定**。

## 5. 不做

❌ 不改 `discipline-inventory.md` 任何一列｜❌ 不实施 Layer 迁移（P3/P4/P7）｜
❌ 不改 `expert-criteria.yaml` 或任何 skill/config/hook/代码｜❌ 不设也不判字节预算｜❌ 不发布

## 6. 写权限（编号即全集；git 只允许只读子命令）

1. `.tad/discipline-floor.md`｜2. `.tad/evidence/acceptance-tests/discipline-floor/`｜
3. `.tad/archive/handoffs/COMPLETION-20260816-discipline-floor.md`

## 7. Acceptance Criteria

**「红」= `exit ≠ 0` 且末行 `RESULT=FAIL`。** TAB 分隔，禁 `#`/`:` 切分。

| # | AC |
|---|---|
| AC1 | 主表恰好 30 行；第 1–4 列与 `verdicts.tsv` **逐字相等**（`LC_ALL=C comm -3` 双向空） |
| AC2 | 「循环?」∈`{是,否,无法判定}`；`否` 行的「解除待判」非空 |
| AC3 | **Layer 机械推导**：`地板`→0；`可缩放`→1\|2 且非 0；`待判`+循环`是`\|`无法判定`→0；`待判`+循环`否`→1\|2。「定档依据」须与之一致（`既有判定`/`循环触发实测`/`待判默认`） |
| AC4 | **锚点绑定纪律**（rev1 最大缺口）：锚点串在载体路径 `grep -Fq` 命中、≥12 字节、**且 `grep -iE` 命中该纪律在 `keys30.tsv` 的判别词**。⚠️ rev1 只验"像不像强制行"，审查员把 30 条互不相关的行配给 30 条纪律，AC1–AC7 全绿 |
| AC5 | **触发串双向锚定**：包含其锚点串（`grep -Fq`）、≥12 字节、**本身命中宽词表**。⚠️ rev1 无下限，同一张全绿表可以是 108 也可以是 16119 字节（150 倍），且 Blake 的激励是写小 |
| AC6 | **载体分散度**：`LC_ALL=C cut -f6 \| sort -u` ≥ **5** 个不同文件，且单一载体文件 ≤ **10** 行。⚠️ rev1 的 AC7 只查"路径+锚点"组合唯一，实测 30 行全指同一文件用 30 个不同锚点照样绿 |
| AC7 | 主表与副表的 `载体路径+锚点串` 合并 `LC_ALL=C sort -u` 计数 == `30 + 副表行数`（防双计） |
| AC8 | 副表 ≥6 行且含逐字 `反合理化` 与 `Forbidden`；**不得**含 `平台绑定交互决策`（主表已有）；各行过 AC4/AC5 同款规则 |
| AC9 | 20 条「待判」各有非空「解除待判」，**不得**含 `TBD`/`待定`/`N/A`，且**去重后 ≥10 种**（禁一句抄 20 遍） |
| AC10 | `## 清单缺口` 节在文，逐字含 `凭据`、`修改框架自身`、`R6`、`CLAUDE.md:50`；**不可降级=是** 的行中含逐字 `版本发布管理` |
| AC11 | `## 承接单` 节在文，含 `expert-criteria`、一条可复算的**测量命令**、Blake 实测的**分子/分母/百分比**、承接单名。**不得**出现 `86%` 或 `4557`（Alex 给的数不可复现） |
| AC12 | **契约 / step0.sh / keys30.tsv 三者均未变**：`git diff --quiet ${T0} -- <三文件>`。**AC 红只能改实现；判定某 AC 不可满足 → 停下退回 Alex** |
| AC13 | 围栏：改动集 − (§6 三项 ∪ Step0 基线 ∪ glob `.tad/evidence/{traces,decisions}/*.jsonl`) 为空 |
| AC14 | **汇总真实**：§4.3 五行数字 == 脚本从主/副表重算；口径警告逐字在文；`lite` 行未计入 full 合计 |
| AC15 | **三份对抗表全红**，落盘 `negative-controls/`：(a) **27 行填 0、3 可缩放行填 1** 的"接近全 0 合法表"，锚点取自同一文件的无关强制行 → 须被 **AC4/AC6** 拦红（rev1 此表全绿）(b) 全 `Layer=2` → AC3 拦 (c) 触发串全填 `MUST` → AC5 拦。任一为绿 → **停下退回 Alex** |

## 8. Step 0

运行 `*.step0.sh`：冻结 `verdicts.tsv`（纪律⇥既有判定⇥通道⇥触发条件）· `names30.txt` ·
`wide-markers.txt` · `fence-baseline.txt`，并断言分布 **地板 7 / 可缩放 3 / 待判 20**、
`keys30.tsv` 名字与 `names30.txt` 逐字相等（不符即中止退回 Alex）。

**逐条 AC 单独跑负控**（非整脚本一次）：任一 AC 在未实现态即绿 = 永真，**停下退回 Alex**。

## 9. 已知取舍

1. **判别词由 Alex 手写**，无现成参照可用——`discipline-provenance.md` 记的是**事故证据路径**
   不是协议载体（实测），故不能当参照。判别词的质量未经第二双眼睛校准；
   实测在 486 条候选强制行上，最松的一条（门禁）命中 47 行 = 9.7%，最紧的为 0 行。
2. **"最短触发串"无法机械验证**：AC5 只锁上下限与原文性，锁不了"最短"。
3. **副表边界由 Alex 划定**，AC8 只保证点名的两项在场。复活条件：P3/P4/P7 发现新常驻物 → 补入。
